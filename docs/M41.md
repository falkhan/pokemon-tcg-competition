# M41 diary

Plan: [M41-plan.md](M41-plan.md). Trigger: the 2026-08-03 leaderboard census
(top 250 is 49.5% grim; we are 2-13 against it) and the deck question it
re-opened in [BACKLOG.md](BACKLOG.md).

## 2026-08-03 — Deck Lab shipped; and the damage model is blind to our own win condition (but the net learned around it)

**Deck Lab built and driven end to end.** `tcg/cardpool.py` (card pool, the
attack join, facets, lazy engine text) + `tcg/decklab.py` (counts, summary,
notes, IO, subprocess smoke runner) + `app/deck_lab.py` (Streamlit, zero logic).
125 new tests. Launch:
`uv run --with streamlit streamlit run app/deck_lab.py`.

Verified with Streamlit's own `AppTest` harness rather than by eye: load
`decks/lucario.csv` → 60 cards / legal / correct warning / deck_hash; Stats
reads 16/31/13 and 24 prizes; deleting Mega Lucario ex → 56 cards and "not
legal, +4 to legal"; the Test tab saved a deck, ran 10 real games vs grim in a
subprocess and reported 4W-6L.

### Three card-data traps the lab caught on first contact

1. **`is_ex` and `is_mega_ex` are disjoint** (121 / 30, zero overlap) — "an ex"
   is `is_ex or is_mega_ex`.
2. **Evolution is by NAME, not id.** `decks/lucario.csv` runs Riolu #677 while
   Mega Lucario ex points at #974, so an id-membership check reports a false
   "missing basic" on a deck we ship.
3. **Cinderace starts from hand.** All 237 harvested `archaludon` seats run 4x
   Cinderace (Stage 2, from Raboot) with no Raboot and no Rare Candy — because
   its "Explosiveness" reads *"if this Pokémon is in your hand when you are
   setting up to play, you may put it face down in the Active Spot."* Without
   that exemption the lab errors on the most-played build of a whole archetype.
   Detected from ability text, with the id as a fallback so behaviour is
   identical under `tests/fake_cg.py`.

### The real finding: `_best_damage` cannot see our own attacker

Building the fixed-pilot deck probe, `generic:alakazam_v2_h4` won **1 game in
60** against everything — while `generic:lucario` went 0.425 vs the same grim
deck. Cause: `rl/combat.py::_best_damage` reads an attack's **printed** damage.

Our deck's entire plan is **Alakazam #743 `Powerful Hand`, printed damage 0**,
which really places 2 damage counters per card in hand — **20 x hand size**, so
340 with the 17-card hands this deck actually holds. The rule pilot therefore
ranks our own win condition below **Dudunsparce at 90** and plays our deck as a
Dudunsparce deck.

**This reaches the shipped agent.** `rl/encoders.py::_combat_features` builds 11
features with the same `_best_damage` and feeds them to the net; `rl/plan.py:118`
uses it for return-KO estimation. Measured directly: with Alakazam active it
hands the net

    my_dmg = 0        my_ko = 0.0        my_can_attack = 0.0

Pool-wide, **174 of 1556 attacks (11.2%) scale, and 142 of those print ≤30** —
the model treats them as near-harmless. Partially known and never followed up:
`docs/M36-plan.md:450` has a one-line note ("`_attack_damage` blind to variable
attacks") from the gust-veto verification, never quantified and never connected
to the encoder or to the fact that it is our own deck's plan.

### But the policy compensated — measured, not assumed

`scripts/m41_scaling_probe.py` over 158 cached live episodes of subs
55172160 / 55182097 / 55185485:

| | |
|---|---|
| turns with Alakazam active and ATTACK legal | **617** |
| of those, we attacked | **617 (100.0%)** |
| of those, `_best_damage` said "can attack" | **0 (0.0%)** |
| turns where 20 × hand ≥ their remaining HP | 534 |
| lethal turns we attacked on | **534 (100.0%) — 0 missed** |
| control: other attackers active, ATTACK legal | 148 turns, attacked 77.7% |

The feature says "this Pokémon cannot attack" on 100% of those turns and the
policy attacks on 100% of them — and more reliably than with flat-damage
attackers (100% vs 77.7%). **BC from real winners plus the raw hand-size and
HP inputs is enough to learn the scaling the engineered feature hides.** So
this is a wrong-and-wasted input feature, not a live defect: no ship, no gate,
no urgency. Logged to BACKLOG for a future encoder pass.

**Instrument correction, caught before it was reported as a result.** The first
cut of the probe counted PROMPTS and reported *1,046 missed lethals*. A turn
holds several prompts but allows only one attack, so every play-a-card prompt
in a turn that ended in an attack scored as a declined attack. Per TURN the
number is **0 missed of 534**. Same failure shape as the four M40 instrument
errors: a new instrument gave a confident wrong answer before the code under
test did.

### Consequence for the deck probe

The fixed-pilot design is sound but its pilot is not deck-neutral: it reads
grim 180 / lucario 270 / garchomp 260 / wall 160 correctly and reads ours as 90
(wrong card), grass-ogerpon as 30, rocket as 160-scaling. It would have
"proved" grim dominant and Grass weak — precisely the conclusion under test.
Fix pending Piotr's call; the probe is written and its roster exported
(`data/kaggle/ogerpon_356c16bd_deck.csv`, `grim_3121746f`, `rocket_59e27a5e`).

### UI pass (Piotr's review of the first build)

The first build was correct and unusable: an empty deck rendered six red error
boxes before you had added a card, the table was undifferentiated text, and
card selection needed a second dropdown under it. Four changes:

- **Legality is asserted only on Save.** While building, the sidebar shows a
  progress bar (`n / 60`), a kind breakdown and the deck's energy identity —
  progress, not judgement. `Save` validates name + legality, writes nothing if
  either fails, and reports each reason as `st.error`. Advisory notes
  (evolution lines, energy coverage) moved to a collapsed expander in the Deck
  tab. An empty deck now produces zero error boxes; pressing Save on it
  produces exactly the two real ones.
- **✨ New deck** starts an empty deck in memory; nothing reaches disk until Save.
- **Click a row to inspect it.** `st.dataframe(on_select="rerun",
  selection_mode="single-row")` replaces the dropdown; the detail pane renders
  under a colour bar in the card's own energy hue.
- **Icons and colour.** Energy symbols per type (🌿🔥💧⚡🔮👊🌑⚙️🐉⭐), trainer
  glyphs (🎒🔧🧑🏟️🔋), tier badges (🌟 Mega ex, ✨ ex, 💎 Tera, 🅰 ACE SPEC), HP
  and damage as proportional bars. Colours are `ENERGY_COLORS` lifted verbatim
  from `notebooks/card_pool_eda.ipynb`, where they were validated
  CVD-separable — and because that palette carries two residual WARNs
  (Fire↔Lightning ΔE 7.9, Colorless contrast 2.45:1), the type name and symbol
  always appear alongside the colour. A test pins that every energy type has
  both a colour and a symbol, and that no card in the pool renders blank.

131 lab tests, 917 suite-wide (the same 4 known failures).

### Save-destination clarity (Piotr: "so it's clearer that saving doesn't overwrite an existing deck")

A **✨ New deck / 📂 From existing** segmented control now owns the sidebar's top
slot. New hides the source picker and starts blank; From existing shows it and
captions `editing a copy of decks/<x>.csv`. Under the name field the exact
destination is always printed — `saves to decks/custom/<name>.csv` — and turns
into a ⚠️ when that file already exists.

Three real protections behind the cosmetics:

- `save_deck` raises `FileExistsError` unless `overwrite=True`; the app turns
  that into an error plus an explicit `⚠️ Overwrite <name>.csv` button. Saving
  twice cannot silently discard the first version.
- `custom_deck_path` is the only path builder and is `NAME_RE`-gated, so
  `../lucario`, `a/b` and `..\lucario` cannot escape `decks/custom/`. Curated
  decks in `decks/` and harvested lists in `data/kaggle/` are structurally
  unreachable, not merely avoided by convention.
- `suggest_name` returns the first free `stem`, `stem_2`, … so loading
  `decks/lucario.csv` proposes `lucario`, and once that copy exists, `lucario_2`.

**Bug this surfaced, found by driving the app rather than reading it.** After
Load, the destination caption still said `untitled.csv`: a keyed widget owns its
value after first render, so the `value=` argument is ignored from then on and
`w_deck_name` kept the stale name while `deck_name` held the new one. The fix is
to make the widget key the single source of truth and assign it from the Load
callback (legal — callbacks run before widgets re-render). Verified end to end:
Load lucario → name `lucario`, destination `decks/custom/lucario.csv`, save
lands there, and `decks/lucario.csv` is untouched in git.

134 lab tests, 920 suite-wide (same 4 known failures).

## 2026-08-03 (later) — scaling damage fixed; the probe runs, and Grass beats grim

`rl/scaling.py` (NEW) is a curated `attackId -> (mode, per_unit, base)` table in
the spirit of `combat.SPREAD_ATTACKS` / `CONDITIONAL_ATTACKS`: rules text cannot
be parsed reliably, so the handful of scaling attacks the census says decide
real matchups are encoded by hand. Wired in as an **opt-in fix token**, the
pattern the repo already uses for pilot variants — `generic-scale:` /
`solver-scale:` spec kinds in `_FIXED_KINDS`, off by default.

**The shipped agent is untouched by construction**: `score_attack(scaling=False)`
is the default, nothing in `submission/` imports `rl.scaling`, and the four
bundled twins are byte-identical. That matters because the live net was
BC-trained against the current (wrong) features and demonstrably learned around
them — changing what it is fed without retraining would be a silent
distribution shift for no measured gain.

**A first draft of the table guessed two of its three ids** and pointed at
Trapinch and Magcargo ex instead of the cards its own comments named. Caught by
printing the engine's rules text for each id before wiring it in.
`tests/test_scaling.py` now re-derives every entry from the card PARQUETS
(engine-free, so it runs under the conftest stub) and asserts each attack's text
actually contains "for each".

Curated entries, all verified against `cg.api`:

| id | card | attack | printed | real |
|---|---|---|---|---|
| 1072 | Alakazam #743 | Powerful Hand | 0 | 20 × cards in hand |
| 120 | Teal Mask Ogerpon ex | Myriad Leaf Shower | 30 | +30 × energy on both actives |
| 560 | Team Rocket's Spidops | Rocket Rush | 0 | 30 × Team Rocket's in play |
| 608 | Team Rocket's Mewtwo ex | Erasure Ball | 160 | +60 × energy discarded |

### Second instrument bug, same session: the weighting was not comparable

The probe excluded mirror cells "because they are 0.5 by symmetry". That drops a
DIFFERENT slice of the field per candidate — grim's own mirror is **49.5%** of
the top 250 while ogerpon's is 3.6%, so it was comparing
grim-minus-its-hardest-matchup against everyone else's full slate. It read
grim 0.814 vs grass 0.816, i.e. a tie. Including mirrors (they measure ~0.50
anyway) puts every candidate on the same 90.1% of the field:

| candidate | weighted vs top-250 field | vs grim alone |
|---|---|---|
| **grass (Teal Mask Ogerpon ex)** | **0.776 ±0.035** | **0.750 ±0.060** |
| grim (Marnie's Grimmsnarl) | 0.659 ±0.039 | — |
| wall (Great Tusk / Crustle) | 0.376 ±0.029 | 0.138 ±0.048 |
| ours (Alakazam) | 0.162 ±0.021 | 0.040 ±0.027 |

So the biased weighting hid an 11.7pp gap. **Grass beats grim head-to-head
0.750 under an identical pilot** — the printed-weakness prediction (the whole
Grimmsnarl line is weak to Grass) survives its first measurement.

**Do not read `ours 0.162` as evidence about our deck.** It is the documented
rule-pilot artifact: even with the scaling fix the pilot gets Alakazam to the
Active spot ~1.7 times a game against ~3.9 live, because the deck's plan needs
setup play a weak generalist does not execute. The probe measures "how well does
a simple pilot do with these 60 cards" — a lower bound, and a fair comparison
only between decks a simple pilot can actually operate.

**Not actionable yet.** n=200/cell resolves ~7pp and the run is single-seed;
G-12 says `--workers 8` is not run-reproducible. Needs n>=800 with replication
before it informs a deck decision.

### Fill with energy

A `🔋 Fill N slots with <type>` button under the sidebar progress bar tops a
deck up to 60 with basic energy of the type it needs. The type is inferred from
**attack demand** (copies-weighted energy symbols the deck must actually pay),
falling back to the Pokemon's own types for an all-Colorless deck. Only the
eight printed basic energies exist — there is no basic Colorless or Dragon — so
those types are skipped rather than silently mis-filled, and basic energy's
exemption from the 4-copy rule means any shortfall can be covered (kyogre needs
35).

Validation that the inference is real rather than plausible: strip the energy
out of six shipped decks and the attack costs alone lead back to the type each
author chose — lucario→Fighting, grim→Darkness, alakazam→Psychic,
iono→Lightning, archaludon→Metal, kyogre→Water. Six for six, pinned as tests.

**A pre-existing state bug this surfaced.** Adding a card from Browse that was
already in the deck left the Deck tab's `cnt_<id>` number_input showing the old
count — `＋1` on a 13-copy energy gave `deck=14, widget=13`, and the stale
widget would have pushed 13 back on the next interaction. Same root cause as
the deck-name bug: a keyed widget owns its value after first render. Fixed with
`_set_count`, which writes the deck and the widget key together, plus
`_forget_counts` on load/new/clear so a previous deck's widgets cannot leak into
the next one. The count widget no longer takes a `value=` at all — the key is
the value, seeded once.

948 tests pass (+13), same 4 known failures.

## 2026-08-03 (later still) — the replay bridge closes the review loop

`scripts/watch_games.py` (NEW). `rl.matchrunner` drives `cg.game` directly for
speed and so never builds the `visualize` payload the official viewer needs —
you get a win rate and nothing to look at. `rl.eval.play_games` goes through
`kaggle_environments`, which does build it, but was written for shipped file
agents. They join cleanly and nobody had joined them: `play_games` accepts
CALLABLES, and `make_pilot` returns `(agent(obs_dict), deck_ids)` — exactly the
kaggle agent contract, including returning the deck when `obs.select is None`.

So any spec matchrunner understands is now watchable, and one run gives both
halves of a review loop:

    replays/<tag>_NNN.html        a self-contained page per game whose button
                                  POSTs to ptcgvis — "watch it and spot the
                                  obvious mistake"
    replays/<tag>/ep_gNNN_aS.json cached-episode shape, read directly by
                                  `python -m rl.postmortem --batch` — "is it
                                  systematic"

    uv run python scripts/watch_games.py --a generic-scale:decks/alakazam_v2_h4.csv \
        --b generic-scale:data/kaggle/grim_3121746f_deck.csv -n 4 --postmortem

Verified end to end: 4 games, 4 valid pages (2.7 MB each, `const vis = [...]`
embedded, ptcgvis form action present), manifest and index regenerated, and the
batch postmortem reading the same games back.

**And its first run corroborated the probe's documented limitation without
being asked to.** Over 4 games of the rule pilot on our own list: 2 of 4 losses
were DECK-OUT, and the flags include *"fetched Kadabra whose basis Abra is
nowhere in play or hand"* — the pilot cannot assemble the Abra → Kadabra →
Alakazam line, which is exactly why `ours 0.162` in the deck probe is an
artifact of the pilot and not a measurement of the deck. The hypothesis was
stated from `~1.7 vs ~3.9 Alakazam-active turns per game`; this is the
mechanism behind that number.

Two Windows fixes it needed: stdout reconfigured to UTF-8 (the `▶` in its own
output crashed cp1252 — the same class of bug as `leaderboard()`'s team names),
and `kaggle_environments`' OpenSpiel registry banner silenced at import.

957 tests pass (+9), same 4 known failures.

## 2026-08-03 — PILOT vs DECK, decomposed: the heuristics pivot is refuted by measurement

Piotr raised moving away from cloning and investing in the rule pilot +
heuristics instead, on the grounds that we may lack the time and compute to
teach a net from scratch. Three n=400 cells decompose it. Same engine, same
seeds, slot-swapped.

| cell | matchup | wr | ELO |
|---|---|---|---|
| A | net(ours) vs **rule**(ours) — deck held constant | 0.945 ±0.022 | **+494** |
| C | rule(ogerpon) vs rule(ours) — **pilot** held constant | 0.900 ±0.029 | **+382** |
| B | rule(ogerpon) vs **net**(ours) — best deck + dumb pilot vs our stack | 0.198 ±0.039 | **−243** |

**The pilot gap is +112 ELO LARGER than the deck gap**, and cell B is the
decision: our net on a mid-tier deck beats the deck-agnostic rule pilot on the
**#1 deck on the ladder**, 0.802. Replacing the net with heuristics would
surrender ~+494 to gain at most ~+382 — and B says the realised gain is far
less than that.

So the pivot as stated is refuted. But it is right in a different form: the
campaign's best measured ROI has been the RULES LAYER **on top of** the net —
M39's `conserve`, one token in a config string, paid **+115 live ELO**
(664.8 → 780.4). "More heuristics" is a layer over the policy, not a
replacement for it.

### And a caveat this puts on the whole deck probe

Cell C measures ogerpon's deck edge at +382 ELO **weak pilot on both sides**.
Cell B runs the same ogerpon list against a competent opponent and it manages
only 0.198. Deck advantage does not transfer intact against a strong pilot: a
good deck is worth much more when your opponent is also bad at defending it.

Every number `scripts/deck_probe.py` produces is measured in that weak-vs-weak
world, so its deck effects are an UPPER bound. That is now two independent
reasons to treat the probe as a lower-bound screen rather than a deck oracle —
this one, and the piloting-complexity confound (its ranking tracks distinct
Pokemon count and Stage-2 lines, and it scores an archetype sitting at ladder
rank #6 at 0.175).

### Trainability, if a deck switch is still wanted

Harvested opponent seats we could clone a pilot from:

| family | all | 900+ | 1000+ | **1000+ winners** |
|---|---|---|---|---|
| grim | 1053 | 815 | 577 | **339** |
| garchomp | 684 | 414 | 371 | 181 |
| rocket | 469 | 412 | 337 | 165 |
| dragapult | 279 | 145 | 133 | 78 |
| **ogerpon** | 79 | 62 | 58 | **43** |
| mirror (ours) | 1052 | 506 | 67 | 40 |

`m38_w9294_cont3` was built from 231 winner seats, so ogerpon at 43 is thin —
though 73% of every ogerpon seat ever harvested is 1000+, because essentially
only strong players run it. Its real asset is elsewhere: **one distinct
Pokemon, no evolution line, one energy type**, so there is far less for a clone
to fail to retain (X5: BC keeps 0.20 of demonstrator edge).

### The Fire idea, checked and corrected

All six "Fire" teams in the top 250 are **Dragapult** decks — the census's own
replay-evidence column reads `played=Dragapult ex` for every one, including the
rank-#11 list our decklist heuristic labelled "Blaziken ex". Blaziken ex is a
2-of TECH inside a Dragapult shell, never a main attacker. So "Fire has the
highest mean score (1056)" is evidence for Dragapult, not for a Fire deck.
Worth keeping: **Dragapult ex has no weakness at all**, which is the
general-purpose property the shell is built on.

## 2026-08-03 — a Fire anti-Ogerpon deck, built and killed in one sitting

Piotr asked whether a brand-new Fire deck could beat the Ogerpon champion
(rank #1, 1255.2). Teal Mask Ogerpon ex is weak to Fire, so on paper it should
be easy. It is not, and the lab turned the question around in about an hour.

**The paper case was excellent.** `Gouging Fire ex` #46: a BASIC (no evolution
line, so maximally pilotable — the ogerpon lesson), 230 HP, `Blaze Blitz` [RRC]
**260 → 520 into Ogerpon**, one-shotting it through Hero's Cape (340). Its
second attack `Heat Blast` [RC] 60 has no restriction, and 260+60 = exactly the
320 HP of both Grimmsnarl ex and Dragapult ex. It is weak to Water, and Water
is **2 of 250 teams** — an unpunished weakness.

Built it with the lab (`decks/custom/m41_gouging_fire.csv`, 4 attackers + a
generic consistency shell, topped up by `fill_with_energy` to 20 Fire).

**Measured, it loses to Ogerpon 0.210 ±0.040** (n=400) and to grim 0.170.
Weighted over the top-250 field: **0.419**, against ogerpon's 0.791.

**The replay bridge gave the mechanism in six games: 4 of 6 losses are
BENCHED-OUT.** The deck never gets to use its 520. Ogerpon's real weapon is not
its damage ceiling, it is **Teal Dance** — a free energy attachment AND a card,
every turn. Gouging Fire attaches one energy a turn from hand and needs three.
It loses the tempo race, and a 4-Pokemon deck has no margin once it is behind.
A bigger number does not beat an engine.

### Then the data answered the question properly

Real harvested games against ogerpon seats (n=79 total):

| family | W-L vs ogerpon | rate |
|---|---|---|
| dragapult | 5-1 | 83.3% |
| **mirror (ours)** | **5-5** | **50.0%** |
| rocket | 6-12 | 33.3% |
| grim | 2-7 | 22.2% |
| garchomp | 5-25 | 16.7% |

All tiny n — G-9 bars matchup-level gating from live data at any n — but the
direction is clear and it kills the premise: **we do not have an Ogerpon
problem.** Our own live record is **2-0 vs ogerpon** and ogerpon is **3.6%** of
the top 250. We have a grim problem: **2-13, and 49.5% of the field.**

### The convergence that matters most

| ogerpon vs grim | result |
|---|---|
| real harvested games | **7-2 (77.8%)**, n=9 |
| fixed-pilot probe | **0.782 ±0.040**, n=400 |

Two independent instruments — real ladder games between real pilots, and our
weak-pilot simulation — agree to within a point. That is the first time the
probe has been corroborated from outside, and it answers the weak-vs-weak
caveat for this specific matchup: **ogerpon really does beat grim ~78%**.

Which reframes the deck question entirely. The case for Ogerpon is not that it
counters the champion — it IS the champion. It is that it beats the 49.5% of
the field we currently go 2-13 against, it is the simplest deck in the format
to pilot (1 distinct Pokemon, 0 evolution lines, 1 energy type, so least lost
to BC retention), and our own archetype is already even with it.

## 2026-08-03 — PRE-REGISTERED: can our lineage pilot Ogerpon? (written before training)

Piotr: "let's give it a shot." This is the M40b Track A experiment aimed at
ogerpon instead of grim — the missing measurement behind the whole deck
question, because pilot beats deck by +112 ELO and a deck we cannot pilot is
worth nothing.

**Corpus, built first and it is thin.** `replay_bc build --deck-hash 356c16bd
--min-score 900 --hand-aware` yields **47 seats / 3,326 decisions**, one shard.
For scale: `m38_w9294_cont3` came from 231 winner seats, M39's corpus A from
172 seats / 12k decisions, the M40 loss-family beds from 140-330 seats. This is
roughly a quarter of the smallest corpus that has ever produced a working net
here. Recorded now so a negative result is not later read as "ogerpon is
unpilotable" when it may only mean "3.3k decisions is not enough".

**Arm**    `model:checkpoints/m41_ogerpon.pt:data/kaggle/ogerpon_356c16bd_deck.csv`
**Control** `model-c-pkg:checkpoints/m39_retain_b.pt:alakazam_v2_h4` (the live M40 floor)
**Battery** the M40 panel roster, identical beds for both arms.

Note on the weighting: `m40_decide.py` weights by `data/m39_live_mix.json`, the
mix OUR agent has played. That is the right denominator for comparing the two
arms head to head (same opponents, same weights), and the WRONG one for asking
"how would ogerpon do on the top-250 ladder". Do not conflate them.

**Pre-registered branches** (Piotr's Track A bar was weighted >= 0.55; Track A
itself scored 0.436 and was killed):

* **weighted >= 0.55 AND >= control** — the lineage can pilot ogerpon and it is
  at least as good as what we ship. Candidate for a live probe slot.
* **weighted >= 0.55 but < control** — pilotable, not better. Hold; the deck
  edge does not survive our own piloting.
* **weighted < 0.55** — KILLED on the same bar as Track A. Given the corpus
  size, read this as "not on 3.3k decisions", not as a verdict on the deck; the
  follow-up would be more harvest, not a different deck.

Confounder recorded in advance: ogerpon has no `_RACEMODE`/`conserve`-style
rules support (our fix stack keys on OUR cards), so the arm runs bare `model:`
while the control runs `model-c-pkg`. The control therefore carries a rules
package worth +2.37pp weighted (M39 P2) that the arm cannot have. A near-miss
should be read with that in mind.

**Third arm added before any result was seen** (same battery, same prefix):
`bare` = `model:checkpoints/m39_retain_b.pt:alakazam_v2_h4` — our net on our
deck with NO rules package. The ogerpon arm cannot carry `conserve`/`racemode*`
(they key on our cards), so `oger vs bare` is the clean deck+pilot contrast and
`oger vs ship` is the "what would actually ship" contrast. Reading both.

## 2026-08-03 — CENSUS CORRECTION: the label was the wrong submission, and "a team's deck" is not always well-formed

Piotr, reading the live board: "Mega Lucario ex (first place) ... Teal Ogerpon
ex (FINALLY - but at 8th place)". My census said Ogerpon was #1. He was right
and the tool was wrong.

**Bug.** `team_submissions()` picked each team's submission with the latest
EPISODE time — "the sub that most recently played" — not the newest submission.
Team 16374395 (rank #1) holds sub 55186239 (lucario, submitted 08-02 12:57,
last episode 13:23) and sub 55147326 (ogerpon, older, still playing at 22:44).
End-time picked the old one. Submission ids increase with submission time, so
`max(id)` is the newest agent. Fixed.

After the fix the top 10 reproduces Piotr's live reading: **#1 Mega Lucario ex,
#2/#3 grim, #4 Mega Lopunny ex, #6 Alakazam, #8 Teal Mask Ogerpon ex** (#7 is
the Crustle/Mega-Kangaskhan wall list he called "Kanga"). An independent human
observation caught an instrument error that no test would have.

**The deeper problem the bug exposed.** Kaggle gives every top team
`SubmissionCount = 2` — two CONCURRENT active submissions, which may be two
different decks. "What deck is this team playing" is therefore not always a
well-formed question. Measured: **41% of labelled teams (89/215) have been seen
on more than one deck family**, rising to **50% of the top 10**, where teams
iterate fastest. The census now carries `n_decks_seen` and flags them.

**Which submission SHOULD own the label?** Piotr: "the meta may have switched
and the strongest submission, say, a month ago, may yield suboptimal results
nowadays." Correct, and the data settles it — score-matching would be actively
WORSE. Team 16374395's leaderboard score is 1251.8; their closest-scoring
submission is 55137818 (stall) at 1278.6, last seen 08-01 and three submissions
retired. Their newest reads only 1070.5 because our cache captured 30 minutes
of its life. **Newest-by-id is right; score-matching would resurrect a dead
agent.** Neither works well on stale data — see the freshness note below.

### What survives the correction

Re-derived over the **126 unambiguous single-deck teams** only:

| family | share (unambiguous teams) | earlier claim |
|---|---|---|
| **grim** | **50.0%** | 49.5% |
| mirror | 19.0% | 18.0% |
| wall | 12.7% | 9.5% |
| dragapult | 4.0% | 3.2% |
| **ogerpon** | **2.4%** | 3.6% |

**The headline holds: grim is ~half the top of the ladder**, and that was never
carried by the ambiguous teams. **What does NOT survive is the Ogerpon thesis.**
It is 2.4% of the field, sits at #8, and is not the champion — the #1 team plays
Mega Lucario ex and has run four different decks. "Ogerpon is the deck to beat"
was built on a mislabelled row.

### Freshness is the binding limit, not coverage

Our replay cache stops at **2026-08-02T23:32** — nothing from today. Ranks and
scores are fetched live; every DECK label is inferred from data up to a day old.
`kaggle competitions leaderboard pokemon-tcg-ai-battle -d` (Piotr's tip) returns
the FULL 6,206-team ladder with `LastSubmissionDate` and `SubmissionCount`,
which is strictly better than the paginated API walk — worth adopting as the
census's leaderboard source, and it is how `SubmissionCount = 2` was discovered.

### Two new scaling attackers, from Piotr's Dipplin read

"Dipplin deck is Festival Grounds deck — festival pokemon can attack twice if
the festival stadium is in play." Confirmed and now modelled:

* **Dipplin #93 `Do the Wave`** — prints **0**, really 20 per BENCHED Pokemon
  (bench only, unlike Spidops' "in play"), and its `Festival Lead` ability lets
  it attack **twice** while Festival Grounds is out. A full bench is 100 x2 per
  turn off a single {G}. New mode `bench_only`.
* **Hydrapple ex #150 `Syrup Storm`** — prints 30, +30 per {G} Energy across
  ALL our Pokemon. New mode `team_nrg`, threaded through the pilot.

Dipplin is **3.7% of the top 250 and climbing** with the highest mean score of
any deck at that share (1007.5), so it would have been the next thing the probe
misread as harmless. 961 tests pass.

## 2026-08-03 — Festival Grounds (Dipplin), read from Piotr's tip

"Dipplin deck is Festival Grounds deck — festival pokemon can attack twice if
the festival stadium is in play." Confirmed, and the list is the most elegant
thing in the format. Dominant build `e3ec0ca2` (45 of 85 harvested seats),
exportable with `export_opp_deck.py --hash e3ec0ca2`:

    17 Pokemon   4x Applin  4x Dipplin  4x Grookey  4x Thwackey  1x Fezandipiti ex
    36 Trainer   4x Festival Grounds  4x Buddy-Buddy Poffin  4x Bug Catching Set
                 4x Lillie's Determination  4x Poke Pad  4x Pokegear 3.0
                 3x Brave Bangle  3x Night Stretcher  2x Dawn  + 4 singles
     7 Energy    7x Basic {G}

**The engine.** `Do the Wave` costs a single {G} and does **20 x your BENCHED
Pokemon** — bench count IS the damage. `Festival Lead` lets Dipplin use it
**twice** while Festival Grounds is out, and if the first attack KOs, the second
comes after the opponent promotes, so it can take two prizes in a turn.
`Thwackey`'s `Boom Boom Groove` tutors **any card, every turn**, gated on the
active having Festival Lead. `Buddy-Buddy Poffin` searches two Basics of <=70 HP
— which is exactly Grookey (70) and Applin (40). The bench filler is the damage,
the tutor is free, and the whole deck runs on 7 energy.

### Real-game record (harvested seats, festival = any deck running Festival Grounds)

Overall **44-41 (51.8%)**, mean seat score 958, max 1132.

| vs | W-L | rate |
|---|---|---|
| wall | 5-1 | 83.3% |
| garchomp | 16-4 | 80.0% |
| **grim** | **8-6** | **57.1%** |
| mirror (ours) | 10-9 | 52.6% |
| rocket | 4-14 | 22.2% |
| **dragapult** | **0-5** | **0.0%** |

Small n throughout (G-9), but the 0-5 is mechanically forced, not variance:
**Dragapult's `Phantom Dive` puts 200 on the active PLUS 60 of spread counters
on the BENCH**, and festival's bench is Applin at **40 HP** and Grookey at 70.
Killing the bench removes the damage source, because Do the Wave scales on bench
count. `rl/combat.py::SPREAD_ATTACKS` already curates Phantom Dive at 60/"any" —
the counter was sitting in our own tables.

That completes a rock-paper-scissors the census had not shown:
**ogerpon > grim (78%) > most of the field; festival > grim (57%), garchomp,
wall; dragapult > festival (0-5) and > ogerpon (5-1); rocket > festival (22%).**
Nothing dominates, which is a better description of this ladder than "grim is
half of it and therefore the only target".

**Taxonomy gap flagged, deliberately not fixed.** `classify()` has no Dipplin or
Festival signature, so all 85 seats land in `other` and an archetype at 3.7% of
the top 250 is invisible to the gate weighting. `FAMILY_SIGNATURES` is
gate-critical and triplicated across `m39_live_mix.py` / `m39_decide.py` /
`m40_decide.py` with a recorded drift incident, so adding a family changes bed
weights and is Piotr's call, not a ride-along.

## 2026-08-03 — cache refreshed; the census now reproduces an independent human read

Snowball refresh (`refresh --subs <ours> --opp-subs <24 top targets> --max-new
400`): episodes **8,059 -> 8,459**, cached replays **2,912 -> 3,312**, and the
newest episode moved from **2026-08-02T23:32 to 2026-08-03T19:01** — from a day
stale to an hour. Re-harvest lifted opp_decks from 5,824 to **6,624** seat-rows
and census coverage from 86% to **91%** (227/250 labelled).

**The census top 10 now matches Piotr's live reading position for position**,
including the two the stale cache got wrong:

| # | census (fresh) | Piotr's read |
|---|---|---|
| 1 | Mega Lucario ex | Mega Lucario ex |
| 2 | Marnie's Grimmsnarl ex | Grim |
| 3 | Marnie's Grimmsnarl ex | Grim |
| 4 | **Mega Froslass ex** | **Froslass/Lopunny ex** |
| 5 | **Mega Lopunny ex** | **Lopunny ex + Fan Rotom** |
| 6 | Alakazam | Alakazam |
| 8 | Teal Mask Ogerpon ex | Teal Ogerpon ex (8th) |
| 10 | Mega Kangaskhan ex | Kanga |

An instrument is trustworthy when it reproduces an observation it was not
tuned to. This one now does.

### grim across three measurements

| version | grim share |
|---|---|
| original census (end-time attribution, stale cache) | 49.5% |
| after the newest-submission fix, stale cache | 46.0% (50.0% unambiguous) |
| **after the fix + fresh cache** | **42.7% (46.8% unambiguous)** |

The number drifts down as the instrument improves, but the finding is robust:
**grim is a little under half the top of the ladder**, several times any other
archetype, and far above the 9.7% our gate beds weight it at. That was the
census's reason to exist and it survives every correction.

Also up on fresh data: garchomp 5.7%, ogerpon 5.3%, **Dipplin 4.0%** — the
festival deck is still climbing.

**Third cp1252 crash of the milestone, same shape as the other two.**
`kaggle_ingest harvest` wrote opp_decks.parquet successfully and then died
printing the summary frame to a Windows console — a success that looked like a
failure. Wrapped in `console_safe`, which now guards `leaderboard()`, the
harvest CLI and `watch_games`. Worth a standing note: any CLI in this repo that
prints card, team or archetype text needs it.

## 2026-08-03 — GATE RESULT: the ogerpon clone clears the bar and beats our live ship

The pre-registered branch that fired: **weighted >= 0.55 AND >= control**.

**WEIGHTED pool (live mix, the gate number): arm 0.6981, control 0.6612,
delta +0.0369 +/-0.0074, z=+9.80.** MDE at this roster 1.1pp. Roster coverage
83.2% (floor 80%). Valid.

Reweighted by the **top-250 ladder mix** — the population a deck switch would
actually face, and the denominator the pre-registration flagged as the right one
for this question:

| weighting | ogerpon clone | our ship | delta |
|---|---|---|---|
| our live mix (gate default) | 0.6981 | 0.6612 | **+3.7pp** |
| **top-250 ladder mix** | **0.8208** | **0.6204** | **+20.0pp** |

The gap between the two rows is the census's whole point: the live mix carries
archaludon at 13.6% and wall at 11.0%, which are ogerpon's two bad matchups,
while the ladder carries grim at 42.7%, which is its best.

Per family (arm vs control, same fixed beds):

| family | share (live) | arm | control | delta |
|---|---|---|---|---|
| grim | 0.097 | **0.984-0.993** | 0.620-0.724 | **+27 to +36pp** |
| garchomp | 0.052 | 0.941-0.962 | 0.598-0.642 | +32pp |
| rocket | 0.052 | 0.927-0.954 | 0.547-0.608 | +32 to +38pp |
| dragapult | 0.077 | 0.963-0.968 | 0.893-0.912 | +6pp |
| archaludon | 0.136 | 0.702-0.854 | 0.876-0.897 | −3 to −19pp |
| **wall** | 0.110 | **0.146-0.367** | 0.252-0.427 | **−6 to −11pp** |
| iono | 0.006 | 0.376 | 0.441 | −6.5pp |

**From 47 seats and 3,326 decisions** — a quarter of the smallest corpus that
has ever produced a working net here. val_acc 0.813 against 0.662/0.744 for beds
trained on far more data. The simplicity thesis (1 Pokemon, 0 evolution lines,
1 energy type => less to lose to BC retention) is the best explanation on offer
and it was stated before the result.

### What must NOT be read from this

* **The absolutes are void.** These beds are clones, and X5 measured clone
  retention at 0.20 — a "grim bed" is an ~800-band opponent. Our control reads
  0.62-0.72 against them while going **2-13 (13%) against real grim live**.
  Checked the 0.99: 20 games at n=20 --diag are legitimate 76-96 move prize
  races (ogerpon takes 6 prizes, grim 0-2; neither deck runs out), so it is not
  a crash artifact — the bed is simply weak. Against REAL grim, both the harvest
  (7-2, n=9) and the fixed-pilot probe (0.782, n=400) say ogerpon wins ~78%, not
  99%. Use 78%.
* **Deltas are the trustworthy part** (same fixed draw serves both arms, so bed
  weakness cancels).
* **wall is a genuine liability** — 0.15-0.37, and wall is 10-13% of the ladder.
  archaludon too, but that is 0.9% of the ladder against 13.6% of our live mix.
* **The control carries `conserve,racemode2,racemode4`** (+2.37pp, M39 P2) that
  ogerpon structurally cannot have — our fixes key on our cards. The `bare` arm
  quantifying that is still running.
* G-13: archaludon and wall panels are WIDE (0.152 / 0.222 spread), so those two
  family absolutes are draw-dependent.

**Not a ship recommendation.** CLAUDE.md requires an explicit instruction and a
named deck before any submit, and this is a bed result, not a live one. It says
the lineage CAN pilot ogerpon from a tiny corpus, and that on the population the
census measured it would be a large improvement — which is exactly the
measurement the deck question was blocked on.

## 2026-08-03 — festival probed, and it maps the probe's blind spot exactly

Two reference decks promoted out of the gitignored harvest dir so they survive:
**`decks/ogerpon.csv`** (hash 356c16bd, 4x Teal Mask Ogerpon ex + 36 trainers +
20 Grass) and **`decks/festival_dipplin.csv`** (hash e3ec0ca2). Both
validate_deck-legal. Festival added to the probe's opponent roster with a
`SHARE_ALIAS` to the census `other` bucket — `FAMILY_SIGNATURES` left alone.

**Probe (n=400, generic-scale both sides), weighted by top-250 share:**

| candidate | weighted | covering |
|---|---|---|
| ogerpon | **0.772** | 92.1% |
| grim | 0.640 | 92.1% |
| **festival** | **0.275** | 92.1% |
| ours (alakazam) | 0.181 | 92.1% |

**Do not read festival 0.275 as a deck verdict.** Against the real-game record
the probe splits in a very specific pattern:

| vs | probe | harvested games | |
|---|---|---|---|
| rocket | 0.250 | 22% | agrees |
| dragapult | 0.083 | 0% (0-5) | agrees |
| **grim** | **0.135** | **57% (8-6)** | contradicts |
| **garchomp** | **0.228** | **80% (16-4)** | contradicts |

Six games through the replay bridge give the mechanism, and it is not subtle:

* `fetch-dead-evolution` x4 — *"fetched Dipplin whose basis Applin is nowhere in
  play or hand"*: the pilot never assembles Applin -> Dipplin, so the Festival
  Lead attacker never comes online.
* `trainer-hoarded` — **Buddy-Buddy Poffin playable across 4 turns and never
  played**. Poffin IS the engine: it fetches the <=70 HP Basics that make up the
  bench, and the bench IS the damage.
* `attach-off-racer` x4 — energy onto Thwackey rather than the attacker.

`Do the Wave` with an empty bench does **zero**. The deck needs four things
simultaneously (Dipplin active, Festival Grounds down, bench full, energy on the
right body) and the weak generalist assembles none of them.

**The pattern is the useful finding, not the number.** The probe agrees with
reality on matchups festival loses ANYWAY (rocket, dragapult) and contradicts it
on matchups festival wins BY EXECUTING A COMBO (grim, garchomp). So the
instrument measures "can a simple pilot lose with this deck" reliably and "can a
simple pilot win with it" not at all. That is a sharper statement of the
piloting-complexity confound than the earlier alakazam evidence, and it explains
why ogerpon tops this probe: a deck whose combo is "attach one energy to the one
Pokemon you own" cannot be misplayed.

## 2026-08-03 — the scaling fix was incomplete: the CARD ladders still read printed damage

Piotr, on the fetch question: "a strong deck-agnostic pilot should know that
having only evolved pokemon in hand if there's nothing on the bench is a loser
move." The rule pilot HAS that logic — `_fetch_value` (M7.5, from kaggle ep
85469339: "Poké Pad fetched a basis-less Hariyama twice over live basics; the
benched-out loss followed") sinks dead evolutions to 60, adds +400 to a basic on
an empty bench and +300 to one whose evolution waits in hand.

It still fetched wrong, and the guard was not the problem. Four real menus,
pulled from replays:

    menu ['Dipplin','Dipplin','Dipplin','Thwackey','Thwackey'] -> Dipplin   (constrained, fine)
    menu ['Applin','Thwackey','Dipplin']                       -> Thwackey  (WRONG)

`_fetch_value` falls through to `_card_usefulness` -> **`_attacker_quality`,
which reads PRINTED damage**, and `rl/scaling.py` only ever patched
`score_attack`. So every fetch, promote, discard and attach-recipient decision
still ranked scaling attackers last:

| card | quality / fetch (printed) | with scaling | role |
|---|---|---|---|
| Thwackey | 50 / 350 | 50 / 350 | tutor engine |
| Grookey | 30 / 330 | 30 / 330 | bench filler |
| **Dipplin** | **0 / 300** | **80 / 380** | **the win condition** |
| **Alakazam** | **0 / 300** | **160 / 460** | **OUR win condition** |
| Kadabra | 30 / 330 | 30 / 330 | its pre-evolution |
| Dudunsparce | 90 / 390 | 90 / 390 | a sideshow |

Fixed by threading an opt-in `scaling` flag through `_attacker_quality`,
`_card_usefulness`, `_fetch_value`, `_attach_recipient_value`,
`_hand_holds_keepers` and the promote/discard branches of `score_card`.
Threaded explicitly rather than via a module flag because matchrunner's
in-process path runs BOTH pilots in one process, so a global would leak across
sides.

`rl/scaling.nominal_damage` supplies a context-free estimate for cards in the
DECK, where hand size and bench count are unknowable — `NOMINAL_UNITS` per mode,
documented as ranking heuristics, not damage predictions.

**The worst instance was not the fetch ladder at all.** `_op_board_harmless`
decides whether to enter CLOSE MODE (stop developing, race). On printed damage a
lone opposing **Alakazam or Dipplin board reads as harmless** — the pilot races
a deck that is about to hit it for 200+. Both now classify as threats.

### And it did NOT move the probe

| candidate | before | after | delta |
|---|---|---|---|
| ours (alakazam) | 0.181 | 0.198 | +1.7pp |
| festival | 0.275 | 0.284 | +0.9pp |
| ogerpon | 0.772 | 0.777 | +0.5pp |

Every delta is inside the n=400 cell resolution (~5pp, G-12), so by this
project's own discipline this is **no detectable change**, not a gain. Festival
still reads 0.133 against grim where real games say 57%.

The reason is that the fetch ranking was one piece of a multi-part combo
failure. Festival needs Festival Grounds down AND a full bench AND
Applin->Dipplin sequenced AND energy on the right body, simultaneously; ranking
Dipplin correctly in a search menu fixes one of four. The probe therefore
remains a LOWER BOUND on combo decks, exactly as documented — the fix removes a
verified defect without changing that conclusion.

Kept anyway: the misplay was real and reproduced from replays, the CLOSE MODE
case is a genuine correctness bug, and the whole thing is default-off so the
shipped pilot is byte-identical. 972 tests pass.

## 2026-08-03 — bare control lands: the ogerpon edge is NOT the rules package

The third arm, added before any result was seen, isolates the confounder the
pre-registration flagged: the control ships `conserve,racemode2,racemode4`
(M39 P2, +2.37pp weighted) that an ogerpon arm structurally cannot carry,
because our fixes key on our own cards.

| weighting | ogerpon | ship (with rules) | bare (no rules) | rules worth | oger vs ship | oger vs bare |
|---|---|---|---|---|---|---|
| our live mix | 0.6981 | 0.6612 | 0.6432 | **+1.8pp** | +3.7pp | **+5.5pp** (z=+14.66) |
| top-250 ladder | 0.8208 | 0.6204 | 0.6154 | **+0.5pp** | +20.0pp | **+20.5pp** |

**The ogerpon advantage is not the rules package.** Removing it moves the
control 1.8pp on our live mix and 0.5pp on the ladder mix, against a gap of
5.5pp / 20.5pp. The gate's conclusion stands with the confounder priced.

Two side findings worth keeping:

* **+1.8pp reproduces M39's independently measured +2.37pp** for the same rules
  package on a different battery and a different net. The rules layer is real
  and it replicates.
* **On the ladder mix that same package is worth +0.5pp.** It was tuned against
  our live mix, which is wall- and archaludon-heavy; the population above us is
  grim-heavy, where it barely fires. That is the census's core finding showing
  up a third time, now inside our own shipped configuration: **we have optimised
  against the distribution we are stuck in rather than the one we are trying to
  reach.**

## 2026-08-03 — the net cannot see its own win condition, and it over-charges it

Piotr, from replay observation: *"it keeps attaching more and more energy into
the active pokemon, even if the attack costs only 1 energy (and it doesn't
scale with energies attached, it scales with the number of cards in hand)."*

The card is **Alakazam #743 `Powerful Hand`: cost `{P}`, printed damage 0, real
damage 20 x cards in hand.** Forensics on the live ships confirms the read and
locates the cause upstream of the policy.

### One `if` blinds seven features

`rl/combat.py:78`, inside `_charged_best`:

    dmg, cost = _ATK[aid]
    if dmg <= 0:
        continue

`_ATK` holds PRINTED damage. Alakazam has exactly one attack and it prints 0,
so `_charged_best` returns `(0, 0)` and `_turns_to_ready` returns UNREACHABLE —
**at every energy count, forever**:

    Alakazam 0e -> _charged_best=(0,0) _turns_to_ready=99 _best_damage=0
    Alakazam 4e -> _charged_best=(0,0) _turns_to_ready=99 _best_damage=0

Over **703 live prompts with Alakazam as our active** (55216935, 40 games):

| feature | value |
|---|---|
| `_combat_features` my_dmg > 0 | 0 (0.0%) |
| `_combat_features` my_can_attack | 0 (0.0%) |
| `_slot_extras` active "can attack NOW" | 0 (0.0%) |
| `_race_features` turns_to_first_ko < cap | 0 (0.0%) |
| `enumerate_plans` returning ONLY the null plan | 394/703 |

The M19 anti-over-attach block (`rl/encoders.py:428`) is the feature built for
exactly this defect, and two of its three slots are constants on our own win
condition: gap pinned at `1.0` ("needs 5+ more"), saturated pinned at `0.0`
("keep going"). On 100% of the surplus attaches measured below, the encoder
told the net to continue. It is behaving correctly given its inputs.

### The rate, and what it costs

Scaling-aware surplus attach = attaching onto a Pokemon that could already fire
a damaging attack. The M19 flag in `rl/postmortem.py:427` cannot measure this —
it routes through the same blind `_turns_to_ready`, reports 1.5%, and blames
Kadabra and Abra. **Our forensics tool shares the agent's defect.**

| ship | Alakazam attaches | surplus | rate |
|---|---|---|---|
| 55216935 M40b Ship 1 | 89 | 30 | 33.7% |
| 55216941 M40b Ship 2 | 71 | 16 | 22.5% |
| 55172160 M39 Ship A | 156 | 55 | 35.3% |
| 55182097 M39 Ship B | 163 | 54 | 33.1% |
| 55185485 M40 floor | 139 | 43 | 30.9% |
| 55146658 M38 | 159 | 67 | 42.1% |

Peak energy on Alakazam, ship 1: 1e in 16 games, **2e in 19, 3e in 3, 4e in 1**
— 59% of games stack past what the attack can ever use.

Two costs. The first is the ugly one, because hand size IS the damage formula:

1. **Every surplus attach is -20 damage on the same turn.** In the 22 surplus
   turns that also attacked with Powerful Hand: 268 mean damage dealt where 288
   was available, 440 damage discarded across 40 games. Not a wasted action —
   a self-inflicted wound.
2. **The deck holds 7 energy total.** 2.00/game is destroyed with KO'd Pokemon
   (95 KO events), 0.40/game of it surplus.

The one theoretical justification — Alakazam's retreat cost is 1 — is empty:
**we retreated Alakazam 0 times in 40 games** (5 Fezandipiti, 1 Shaymin). And
137 of 174 attacks were Powerful Hand, so the net did learn to ATTACK with a
card its damage model calls harmless. It learned to attack. It never learned to
stop charging.

### The decisive measurement: the corpus is clean, the net is not

Same predicate at three points in the pipeline:

| where | Alakazam attaches | surplus | rate |
|---|---|---|---|
| **BC corpus `bc_m38_w9294_v4`** (cont3's own labels) | 529 | 25 | **4.7%** |
| **Rival Alakazam pilots** (398 subs, 1284 seats, 3371 replays) | 2591 | 139 | **5.4%** |
| **Our shipped clone** (55216935 live) | 89 | 30 | **33.7%** |

The teacher labels are FINE. The ladder consensus is ~5%, several rival pilots
sit at exactly 0.0%, and the corpus energy distribution is `{0e: 504, 1e: 24,
2e: 1}`. **The clone serves a 7x amplification of a defect its training data
does not contain.**

This kills the bad-demonstrations hypothesis and settles the diagnosis: the net
did not learn the distinction because its inputs cannot express it. No amount
of better labels fixes an input that is a constant.

### The rule, rebuilt twice under falsification

Both dead ends are worth keeping, because both were caught by the same
pre-registered falsifier ("did we ever LATER use energy above the cap?").

* **v1** derived "which attacks deal damage" from rules text. **Cruel Arrow
  prints 0** — all 100 of Fezandipiti ex's damage lives in its effect text — so
  v1 capped it at retreat cost 1 and would have blocked **7 real attacks** in
  Ship A.
* **v2** asked "does the NEXT energy unlock an attack?" — the greedy trap
  `rl/combat.py:59` already documents for `_best_damage`. Cruel Arrow costs 3,
  so at 1 attached no single attach unlocks it, and v2 again killed Fezandipiti
  after one energy.

**v3 never asks about damage at all.** An attach is dead iff (1) every attack
this Pokemon has is ALREADY affordable — exact for typed costs and for
incremental charging — and (2) `attached >= retreatCost`, and (3) no attack of
its scales on its OWN attached energy. Then the energy buys nothing the rules
offer. No damage model, no curation beyond the scaling bit.

    Alakazam       retreat=1  dead at >=1   [Powerful Hand(cost1)]
    Kadabra        retreat=1  dead at >=1
    Fezandipiti ex retreat=1  dead at >=3   [Cruel Arrow(cost3)]  <- correctly permissive
    Dunsparce      retreat=1  dead at >=2   [Trading Places(1), Ram(2)]
    Dudunsparce    retreat=3  dead at >=3

Simulated as a hard mask over **316 live games across six ships**: blocks
13.4-23.4% of all attaches (Alakazam and Kadabra only), with **zero falsifiers
— no attack and no retreat, in any game, ever needed energy above the cap.**

### The scaling exception, audited for false negatives

25 of the pool's 1556 attacks scale on OWN attached energy; those have no
ceiling and the rule must never fire on them. The dangerous direction is a
false NEGATIVE (wrongly banning a real charge), so every attack mentioning
own-energy AND damage was pulled: 29 candidates, 25 caught, **4 near-misses —
Torrential Pump, Jungle Whip, Chrono Burst, Sonic Ripper — all flat "shuffle
your energy away for +80" bonuses whose printed cost already covers them.
Zero true false negatives.**

Consequence worth recording: **Teal Mask Ogerpon ex is an own-energy scaler**,
so the rule is inert on the M41 ogerpon clone by design.

### Decision (Piotr, 2026-08-03): capability, not a ship

No shippable net this milestone. Build the code so the agent and the net CAN
see that a scaling attack is real rather than 0, keep it behaviourally inert
for the current bundle, and spend M42 exploring it.

Cost note that shaped the decision: `plan_iter collect` writes only ENCODED
arrays (`rl/plan_iter.py:309`) — no raw observations. The champion half of any
corpus therefore cannot be re-encoded after an encoder change; it must be
re-collected. Only the Kaggle-winners half rebuilds from `data/kaggle/raw`.

## 2026-08-03 — QC replays for the ogerpon pilot; `over-attach` was a false positive

Built with the replay bridge rather than a bundle export, so nothing in
`submission/` was touched: `model-pz:` already exists as a spec kind, so the
exact ship candidate (net + deck + planzero) is watchable directly.

    scripts/watch_games.py --a model-pz:checkpoints/m41_ogerpon.pt:decks/ogerpon.csv \
                           --b <opponent> -n 4 --postmortem

| leg | result | replays |
|---|---|---|
| vs grim (`m39_bc_grim`) — 43% of the ladder | **4-0** | `replays/m41qc_oger_vs_grim_*.html` |
| vs wall (`m38_bc_wall`) — its worst bed matchup | **4-0** | `replays/m41qc_oger_vs_wall_*.html` |
| vs our live ship (`m39_retain_b` + package) | **3-1** | `replays/m41qc_oger_vs_ourship_*.html` |

**Piotr, on the first read: "over-attach works in its favour since it scales
the attack based on the attached energies of our and opponent active pokemon."
Correct, and the flag was wrong.** `Myriad Leaf Shower` is paid at 3 energy and
gains **+30 damage per further attachment** — 180 at 3 energy against a
2-energy defender, 300 at 7. So "best attack already charged" is not
"saturated"; the pilot was maximising damage and the instrument called it a
defect.

`_attach_saturated` (M19) was written for the live
3-energies-on-a-1-cost-Solrock case, where cost-satisfaction and
damage-satisfaction are the same thing. They are only the same for FLAT
attackers. It now skips attackers whose damage scales with their own attached
energy (`my_nrg` / `both_nrg` / `team_nrg` in `rl/scaling.py`), with a test
pinning both directions — Ogerpon exempt, the flat case still caught.

Effect on the QC read: **17 `over-attach` flags across the three batches -> 0.**
That is the fourth instrument this milestone that read printed/flat damage
semantics and produced a confident wrong answer; the others were the deck
probe's attacker key, the fetch/promote/discard ladders, and CLOSE MODE.

**The one real flag left** is `trainer-hoarded`: *Boss's Orders playable across
8 turns and never played* (also Judge, 4 turns). Worth a look during the replay
review — it won all three legs regardless, so it is a missed-value question, not
a losing one.
