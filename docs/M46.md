# M46 diary — band-faithful offline opponents + pivotal-mistake guardrails

Plan: `docs/M46-plan.md` (r2). Post-mortem basis: `docs/m44-live-postmortem.md`.
Branch `feature/m46`. Hard deadline: comp ends 2026-08-16.

## 2026-08-14 — session kickoff

- Piotr resolved the two open r2 questions at plan ingestion:
  1. **Final standing = BEST submission** (Kaggle best-sub scoring) → a late
     08-15 ship is free option value; the early-burst decay artifact is not a
     standing risk.
  2. **Bank the guards-only ship EARLY on 08-15 AM** — before the panel
     candidate. Two QC + review cycles accepted.
- Ship-commit archaeology (commit `4094166`): the 810 incumbent 55265099
  shipped `m41b_wide_prod` + fixes `conserve,planzero,ash,ashguard` — the
  EXISTING `model-cz-ashw` matchrunner token is exactly that set. The
  early-bank bundle is therefore a clean single variable:
  `model-cz-ashw-dg0` = incumbent config + `dudguard0`. The post-mortem §5
  behavioral fingerprint corroborates the string (Sacred Ash window 4-12,
  Fez 0/59 at deck≤6, Dudunsparce free at low deck → ash+ashguard+conserve,
  no deckguard).
- A0t design settled (recorded here because it refines the r2 plan):
  wrapper decoder `scripts/m46_band_gate.py` over an UNTOUCHED
  `scripts/gate_spec.py` — the band spec (weights INSIDE, as integer live-game
  counts summing to n=83) is hashed, and the band hash is embedded in the
  derived per-seed spec *name*, which gate_spec hashes and stamps into every
  cell: editing weights after the fact breaks every cell's stamp. Remainder
  split DECIDED as 5/5/5 games (garchomp/archaludon/rocket). Weight-bootstrap
  stability floor 0.95: a PASS-shaped delta that is weight-UNSTABLE downgrades
  to INCONCLUSIVE (one step beyond the r2 letter, pre-registered in the
  approved session plan = Piotr sign-off). A0v arm/control carry each net's
  OWN live string (K = `model-c-pkgz`, m41b = `model-cz-ashw`) — the live
  810-vs-735 truth came from the full bundles, not a shared string.

## Observations

(diaried at production time; kills included)

### C3 full-corpus mine (2026-08-14 ~16:20) + C1 freeze

- 7,565 raw episodes, both seats, 4 workers ~25 min. Tables:
  **self-removal** Dudunsparce 22,128 obs + THREE meta-wide discoveries
  (Dusknoir 121, Dusclops 111, Abra-109 83 — the Run-Away-Draw class the
  plan hoped C3 would find); **gust** Boss's Orders 7,413 + Team Rocket's
  Giovanni 532 + Lisia's Appeal 361 + Prime Catcher 21 + Pokémon Catcher 5;
  **zero-damage** 303 all-zero pairs incl. the Crustle ex-immunity
  (Ogerpon-vs-Crustle 369 obs = the live 17-turn tunnel, discovered without
  effect text); **deck costs** 79 cards, cross-validating the hand measures
  (Enriching 4 = m37's 4.0/attach; Fezandipiti 3 = m30's 2.8).
- Known noise, diaried: Tool Scrapper reads as gust-class at n=4 (some
  same-turn coincidence — NOT a forced switch; any C2 gust-registry
  consumer should floor at n≥20 or intersect with trainer type);
  worst-case deck costs inherit co-occurring triggered draws (Rare Candy
  reads 5 though the card itself costs 0) — conservative in the safe
  direction for a deck-out veto, registered semantics.
- C1 freeze: `_SELF_REMOVAL_ABILITY_IDS` {66,109,132,133} and
  `_DECK_COST_WORST` (51 entries ≥2) frozen into rl/plan; O25 `lastmon`
  + O26 `deckzero` implemented on them; subset-of-mined-tables tests pin
  the freeze. rule_facts.json committed (git add -f).

### Floors (2026-08-14 ~17:20) — bank pair PASS

- n=200 × 2 seeds per cell vs tuned + iono (floor-gate-sample-size law):
  ctl (cz-ashw) tuned .743 / iono .627; **dg0 (bank) tuned .730 (−1.3pp,
  z≈−0.4) / iono .682 (+5.5pp, z≈+1.6)** → NO regression, B1's adoption
  bar (tests + floor) is fully met. t0 composite tuned .708 / iono .613
  (−3.5/−1.4pp, inside noise) → Tier-0 screen clean; benchzero/lastmon/
  deckzero meet their registered bar too, string inclusion decided at
  candidate compose time.
- Pool-wedge note: the A0v battery wedged once at arm__wall_d2 (frozen
  55 min from 15:49 — caught by the stale-line rule, live-growth check).
  Bounced by the M43 procedure, now automated in scripts/m46_run_watch.sh
  (3-min zero-growth detector); A0v resumed clean under it.

### Bank ritual COMPLETE (2026-08-14 ~17:45) — WAITING ON PIOTR

- Bundle: `m41b_wide_prod` + `conserve,planzero,ash,ashguard,dudguard0` on
  alakazam_v2_h4 → `dist/submission_neural_20260814_163154.tar.gz`.
- ci_gate PASS (suite + bundle + instruments) · tarball deck md5 ✓ ·
  ship_verify PASS (incl. 3b fix-set equality vs `model-cz-ashw-dg0`, 6a
  plan parity on the w143 BC champion corpus).
- QC battery 18W-9L, ZERO crashes/timeouts/illegal actions: tuned 3-0,
  iono 3-0, dragapult 3-0, wall 2-1, wall_hard 1-2, **grim 0-3 SWEEP**,
  archaludon 2-1, top900 2-1, prev-ship (K tarball) 2-1. Bespoke stall leg
  (first ever, uncalibrated bed): 2W-1L.
- **Sweep investigated per the pre-registered blocker rule** (n=100 × 2
  seeds, dg0 vs ctl on the SAME m39_bc_grim bed): dg0 pooled **0.653**
  (.625/.680), ctl pooled **0.685** (.710/.660) → Δ −3.3pp z≈−0.7, inside
  noise; both healthy vs the bed. P(0-3 | wr≈0.67) ≈ 3.6% per opponent —
  with 9 opponents, some sweep is ~25% likely per battery. VERDICT: n=3
  luck, not a dudguard0 regression, not a bundle defect. Flagged to Piotr
  with this evidence; his call stands.
- STOP state: replays in `replays/m46_qc_bank_*.html` — waiting for manual
  replay review + explicit go + deck confirmation (alakazam_v2_h4). NO
  SUBMIT until then.

### 08-15 battery pre-registration (2026-08-14 evening)

- B2-B4 panel probe specs generated + hashed BEFORE any cell runs:
  af `42a0afb94c566951`, rg `2b7b66d543dcf10b`, bc `c317b32b155aaa71`
  (single-variable over `model-cz-ashw` on m41b, family_z −2.2, n=800×2s).
- **B5 (K-line string restore) DEPRIORITIZED under deadline**: K sits 75
  ELO below m41b live, Q1 closed the line to alakazam-on-m41b, and a B5
  panel run costs ~1h of the 08-15 budget. Revisit only if A0v surprises.
  Recorded as a skip, not a kill.
- C1 Tier-0 (dudguard0/benchzero/lastmon/deckzero) adoption bar is tests +
  no-regression floor (NOT panel probes) per plan; m46_probes.sh part 1
  floors the bank pair and the `model-cz-ashw-t0` composite vs tuned+iono
  at n=200×2 seeds. Probe decodes are exit-code-gated on A0v acceptance.

### Track B/C guard code (2026-08-14 evening)

- O20-O24 landed in `rl/plan.py` (+ twin sync): `dudguard0`, `benchzero`,
  `attackfloor`, `retreatguard`, `bosscombo`; shared `_attack_armed` primitive
  on `combat._best_damage(..., board_ids, scaling=True)`. Chain order:
  benchzero inserted BEFORE tempo, attackfloor LAST (non-turn-ending
  promotions outrank turn-ending — the registered precedence law).
  12 new tests incl. the stacked bench0+armed+END case and the
  ash-beats-dudguard0 early-return case; 67/67 test_attach_override green,
  ship_verify + fixset_equality suites green. The armed test cells run the
  REAL fake-card damage math (nominal_damage falls back to printed for
  non-curated ids), no monkeypatching.
- Probe tokens: `model-pz-{dg0,af,rg,bc,bz}` over the `model-pz` control,
  `model-cz-ashw-dg0` (early-bank arm) over `model-cz-ashw`, and the B5
  composite `model-c-pkgz-b5` over `model-c-pkgz`.
- `core` meta-token: `CORE_FIXES` registry + `expand_core_fixes()` in
  tcg/shipping.py, wired at the single `--fixes` compose point; `core` can
  never reach a bundle string or frozenset (check 3/3b preserved). Registry
  EMPTY until probes adopt guards.

### A1 corpora (2026-08-14 evening, running)

- 5 deck CSVs exported clean (stall_e234578d, wall_6f87e9d4, wall_e510e4d4,
  dragapult f2b4039a + 7c605fb2).
- `grim1100` (3121746f @1100 winners hand-aware): **9 shards, 238,934 rows**
  (dropped: 461 blank_obs, 207 no_next_action) — comfortably above the
  lucario-ship precedent (602 seats). Sliced to w100.
- Seat-count sanity vs opp_decks.parquet matched every plan pin EXACTLY
  before launch: grim 781/461w, mirror 936/532w, drag 189+74+91 @1100,
  stall 136, wall 88+84 @900.
- `drag_a` (f2b4039a @1100): 4 shards, 108,567 rows.

### A1+A2 complete (2026-08-14 15:33)

- All 8 corpora + 15 G-13 bed draws done. Yields (shards; all hand-aware,
  sliced to w100 for training): grim1100 9 (238,934 rows), mirror1100 8
  (39,373 decisions / 532 winner seats), drag_a 4 (108,567 rows) + drag_b 2
  (49,146) + drag_c 3, stall1000 3, wall_a 1, wall_b 1.
- Final val_acc per draw: mirror .680/.683/.691, grim .610/.610/.603,
  drag .578/.585/.569, stall .544/.526/.549, wall .486/.510/.506. Thin
  families read low as expected — bed STRENGTH is judged by A3 calibration,
  not val_acc (registered lesson).
- ANOMALY: the in-script mirror1100 build died silently (0-byte log, no
  shards, no traceback — process vanished; script soft-failed as designed
  and completed everything else). A by-hand rerun of the IDENTICAL command
  built 8 shards clean, and the idempotent script rerun filled the 3 mirror
  beds. One-off process death, cause unknown (transient); the soft-fail +
  idempotent-rerun design contained it.

### A0t band instrument (2026-08-14 evening)

- `scripts/m46_band_gate.py` (hash/run/decode) + `scripts/m46_make_band_specs.py`
  landed; 13 golden tests green (incl. THE chain property: a weights edit
  after cells ran = refusal exit 2, no verdict) + registered in ci_gate
  tier_instruments. BED_FAMILY synced (mirror_d*/stall_d*/lucario) into BOTH
  m40_decide.py and m39_live_mix.py — keys only, no statistic changed; the
  m46 instrument reads neither copy.
- **Pre-registered band hashes** (BEFORE any cell runs):
  `m46_null_panel` = `d7a151364fc9ef92`, `m46_A0v` = `ecf6021511c4f683`.
  Weights committed INSIDE the specs as integer game counts (n=83):
  grim 32, mirror 19, wall 5, lucario 5, stall 4, dragapult 3,
  garchomp/arch/rocket 5/5/5.
- **Determinism smoke PASS** (null-design validity guard): two identical
  20-game m41b-vs-m41b runs, same seed 1, produced different result vectors
  (11W-9L vs 9W-11L; vectors diaried in the smoke files) — engine RNG makes
  same-seed reruns independent samples; the control-vs-control null panel is
  statistically valid.

### Null calibration panel (2026-08-14 ~15:50) — decoded

- 54 cells (27 beds × 2 sides, m41b bundle vs itself, n=200, seed 1),
  band hash `d7a151364fc9ef92`, coverage 100%.
- **No family fired at the registered −2.0 bar** (family-level min z −0.70;
  worst single cell stall_d1 −1.65). Exit 0 — bar technically stands.
- **Bootstrap null min-z 5th percentile: −2.20** → ADOPTED as the calibrated
  `family_z` for A0v/gate (re-registered BEFORE any candidate cell ran —
  the exact escalation A0t pre-registered; A0v band hash is now
  `b40697317ee0ded7`, family_z −2.2). The null spec itself stays at −2.0 as
  the record of what calibrated it.
- CAUTION diaried: under this TRUE null the weighted pool read
  **+2.7pp z=+2.30** (grim family +4.9pp, WIDE draw spread 0.103, share .39
  amplifies it). One draw at n=200/cell; the real gate runs n=800×2 seeds
  (8× the games, ~half the SE, MDE ~±1.2pp vs ±3.3 here) — but it is live
  evidence that a +2pp weighted delta alone is NOT a strong signal at small
  n, and validates the stability-floor/INCONCLUSIVE machinery.
- A3 pre-read (null cells double as bed-strength reads vs the m41b bundle):
  wall beds hold m41b to **0.22-0.25** (live 780+ wall wr 0.00 — the new
  wall beds look band-faithful); grim clones 0.73-0.78, mirror 0.70-0.72,
  stall 0.77-0.79, dragapult 0.86 (stall/dragapult still weak-side proxies
  pending A3 calibration vs the K bundle).
- OMP PINNING now baked into m46_band_gate.py cmd_run (M43 box law,
  docs/M43.md:258): the first null launch ran unpinned at ~1 cell/2.5 min;
  pinned = 16 cells/90 s (~15×, matching the M43 measurement).

### A0v gate: session crash + watchdog recovery (2026-08-14 ~17:15)

- The Claude session babysitting the A0v run crashed mid-gate (~17:00). On
  reattach: NO orphan cleanup was needed — `m46_run_watch.sh` (PID 78632,
  launched 16:25) survived the session and kept supervising exactly as
  designed. s1 was complete (54/54 cells); s2 arm side complete; s2 control
  side filling.
- Wedge pressure is HIGH in s2: four zero-growth bounces in ~25 min
  (16:52 arm__garchomp_d2, 16:58 arm__rocket_d1, 17:07 control__wall_d1,
  17:13 control__dragapult_d1) vs one in all of s1 (arm__wall_d2). Every
  bounce named a different suspect cell → real forward progress; chunk-
  granular resume returned completed cells instantly each relaunch. Band
  hash confirmed `b40697317ee0ded7` in the relaunch banners (tamper chain
  intact across bounces).
- Leftover-process sweep after the crash: only ONE true stale — the
  `hermes_heartbeat.sh m46-beds` reporter (PID 35906) still attached to
  `runs/m46_build_beds.log`, a run that completed 15:33. Killed by exact
  PID per the no-broad-pkill rule. Watchdog + gate left running (watchdog
  exits with the gate's rc on natural completion).

### A0v acceptance decode (2026-08-14 17:20) — INSTRUMENT INVALID

- Run complete: 108/108 cells, both seeds, coverage 100%, band
  `b40697317ee0ded7` verified in every cell (no refusals despite 5
  watchdog bounces — chunk-granular resume held the tamper chain).
- **WEIGHTED pool: arm (K) .7467 vs control (m41b) .7443, delta +0.24pp
  ±0.82pp, z=+0.58** — the panel reads K ≈ m41b, while live truth is
  m41b +75 ELO. Not the required control-wins ordering → **A0V FAILED,
  instrument INVALID** per the pre-registered rule. MDE at this n was
  ±1.2pp, so a real live-sized gap should have been visible: this is a
  measurement-validity failure, not underpowering (delta is positive,
  not merely insignificant-negative).
- Formal verdict was KILL, but only via the **stall family-kill
  (z=−9.79**, all 3 draws consistent: −5.6/−11.0/−9.7pp, spread .103
  WIDE) — the pooled number itself never reproduced the ordering.
- Families favoring K (contradicting live): mirror +3.1pp z+3.5,
  wall +3.3pp z+3.7, dragapult +3.3pp z+4.6. Grim (share .39) flat
  −0.8pp z−0.9. Advisory anchors also favor K (tuned +3.7pp, iono
  +8.4pp) — the M44 anchors-love-K/live-hates-K signature again.
- Weight bootstrap: 95% band [−0.33pp, +0.79pp], verdict agreement .806
  (< .95 floor) — the read is weight-unstable on top of invalid.
- Echoes the null-panel CAUTION (true null read +2.7pp z+2.3): the
  weighted pool leans positive under both null and known-negative truth.
- CONSEQUENCES (all pre-registered): panel demoted to ADVISORY; B2-B4
  probe decodes stay exit-code-LOCKED; guards-only fallback is the ship
  path; B5 revisit clause ("only if A0v surprises") is now live.
  ESCALATED to Piotr — no ship-path action until his call.

### B5 revisit (2026-08-14 17:25, Piotr's post-A0v call)

- Piotr's decision on the A0v escalation: run **B5 (K-line string
  restore) BEFORE committing to guards-only** — the pre-registered
  "revisit only if A0v surprises" clause, now live. Read is
  ADVISORY-class by construction (the pooled-weighted instrument failed
  A0v), but B5 is same-net single-string A/B — the per-family reads
  (esp. the huge stall kill) are the actionable surface.
- **DEFECT FOUND while preparing the spec: all three B2-B4 probe specs
  are corrupt.** Arm strings mangled at generation time by zsh
  expansion in the `--arm=...` args: `-af:checkpoints` →
  `-afheckpoints`, `-bc:checkpoints` → `-bcheckpoints`, and rg's token
  literally became `/usr/bin/rg...` (zsh ate `:c` as a modifier /
  command-path expansion). The pre-registered hashes 42a0afb9/2b7b66d5/
  c317b32b therefore cover garbage arm strings. No cell ever ran under
  them (probes were exit-gated), so regeneration + re-registration is
  clean IF they are ever revived. Left on disk as the record; lesson:
  single-quote matchrunner spec strings in shell args, verify the
  emitted JSON.
- **B5 spec pre-registered** (before any cell): `docs/specs/
  m46_probe_b5.json`, band hash `bd780ea33bf0231b`, family_z −2.2.
  Arm `model-c-pkgz-b5` (= K package + ash,ashguard,tempo,deckguard,
  hammer) vs control `model-c-pkgz`, both on ppo_best_m44_K_r1 /
  alakazam_v2_h4, n=800×2 seeds, full 27-bed roster + advisory anchors.
  Bars: pre-registered B2-B5 rule — pooled-weighted Δ ≥ 0 AND no family
  kill at −2.2. Launched under m46_run_watch.sh (wedge pressure in A0v
  s2 was 4 bounces/25 min — watchdog is mandatory now).

### B5 decode (2026-08-14 18:07) — KILL, string restore not adopted

- Run: 108/108 cells, 2 seeds, 1 wedge bounce, coverage 100%, band
  `bd780ea33bf0231b` intact, decode exit 1.
- **WEIGHTED pool: b5 arm .7359 vs model-c-pkgz control .7455 —
  delta −0.96pp ±0.82, z=−2.29. Bootstrap 95% band [−1.11, −0.81pp],
  verdict agreement 1.000** — a stable KILL, not a weight artifact.
- No family favors the restore: grim −1.7pp z−1.88, stall −1.8pp
  z−1.94 (no family kill fired; min z above −2.2), everything else flat
  to slightly negative. The restore does NOT fix K's stall hole
  (arm .684 vs control .702 on stall beds).
- Anchors again split from beds: tuned +1.0pp, iono +1.1pp (advisory).
- Read is ADVISORY-class post-A0v, but the hypothesis "K's regressions
  are string-inherited" finds NO support anywhere on the panel — and
  the direction is consistent across 7/9 families. B5 recorded as a
  KILL at production time per the diary law.
- CONSEQUENCE: B5 dead → the pre-registered guards-only fallback
  (m41b line) resumes as the ship path.

### SHIPPED (2026-08-15 00:29 UTC) — sub 55516471

- Piotr's replay-review go + deck confirmation received → submitted the
  EXACT QC'd tarball `dist/submission_neural_20260814_163154.tar.gz`
  (md5 054f72adae718a9b4f5891b7d0caabb3; deck.csv md5-verified ==
  decks/alakazam_v2_h4.csv; NO re-export — the QC'd bytes shipped).
- Kaggle sub **55516471**, status PENDING at submit. Bundle:
  m41b_wide_prod + `conserve,planzero,ash,ashguard,dudguard0` on
  alakazam_v2_h4 — the guards-only bank, per the A0v-fail fallback rule.
- MODELS dict updated (55516471) in notebooks/model_monitor.ipynb; deck
  already in DECK_META. MILESTONES M46 row marked shipped.
- WATCH LIST (observation run): (1) hour-1 μ trajectory vs the 810
  incumbent 55265099 and K 55485268 — best-sub scoring means this sub
  must EXCEED 810 to matter; (2) grim matchups live (QC sweep was ruled
  n=3 luck — verify); (3) dudguard0 fire rate + any bench-out losses;
  (4) ~15-18 rated games expected in hour 1, then ~1.3/h (dwell table).
