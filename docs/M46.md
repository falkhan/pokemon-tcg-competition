# M46 diary — band-faithful offline opponents + pivotal-mistake guardrails

Plan: `docs/M46-plan.md` (r2). Post-mortem basis: `docs/m44-live-postmortem.md`.
Branch `feature/m46`. Hard deadline: comp ends 2026-08-16.

## 2026-08-14 — session kickoff

- Piotr resolved the two open r2 questions at plan ingestion:
  1. **Final standing = BEST submission** (Kaggle best-sub scoring) → a late
     08-15 ship is free option value; the early-burst decay artifact is not a
     standing risk.
  2. **Bank the guards-only ship EARLY on 08-15 AM** — before the panel
     candidate. Two QC + review cycles accepted.
- Ship-commit archaeology (commit `4094166`): the 810 incumbent 55265099
  shipped `m41b_wide_prod` + fixes `conserve,planzero,ash,ashguard` — the
  EXISTING `model-cz-ashw` matchrunner token is exactly that set. The
  early-bank bundle is therefore a clean single variable:
  `model-cz-ashw-dg0` = incumbent config + `dudguard0`. The post-mortem §5
  behavioral fingerprint corroborates the string (Sacred Ash window 4-12,
  Fez 0/59 at deck≤6, Dudunsparce free at low deck → ash+ashguard+conserve,
  no deckguard).
- A0t design settled (recorded here because it refines the r2 plan):
  wrapper decoder `scripts/m46_band_gate.py` over an UNTOUCHED
  `scripts/gate_spec.py` — the band spec (weights INSIDE, as integer live-game
  counts summing to n=83) is hashed, and the band hash is embedded in the
  derived per-seed spec *name*, which gate_spec hashes and stamps into every
  cell: editing weights after the fact breaks every cell's stamp. Remainder
  split DECIDED as 5/5/5 games (garchomp/archaludon/rocket). Weight-bootstrap
  stability floor 0.95: a PASS-shaped delta that is weight-UNSTABLE downgrades
  to INCONCLUSIVE (one step beyond the r2 letter, pre-registered in the
  approved session plan = Piotr sign-off). A0v arm/control carry each net's
  OWN live string (K = `model-c-pkgz`, m41b = `model-cz-ashw`) — the live
  810-vs-735 truth came from the full bundles, not a shared string.

## Observations

(diaried at production time; kills included)
