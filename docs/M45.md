# M45 diary — improve the lucario pilot (wide + fix probes + iono-pool leg)

Plan: `docs/M45-plan.md` (r1, approved by Piotr 2026-08-13 evening, Track
A+B). Baseline: `m45_bc_lucario_d3.pt` (md5 2514a4f3…, 8ep off the 1000+
band of `mega_lucario_ex+solrock`) — floors 0.585 vs tuned / 0.372 vs iono
(n=400). Scouting record in docs/M44.md (late entries).

## 2026-08-13 evening — setup

- Branch `feature/m45` (off feature/m44 — reuses the M44 league/ritual
  scripts; stacks on PR #31).
- Ship deck adopted: `decks/lucario_solrock.csv` (= harvested
  `lucario_ed7c14ba_deck.csv`; 60 cards, hash roundtrip `ed7c14ba` OK).
  NOTE it differs from `decks/lucario.csv` (tuned's list): Dusk Ball ×4 /
  Carmine ×4 / Gravity Mountain ×2 in place of Ultra Ball / Judge /
  Wally's Compassion.
- Fix-probe tokens added to `_MODEL_FIX_KINDS`: `model-pz-bf`
  (planzero+benchfloor), `model-pz-gv` (planzero+gustveto) —
  test_matchrunner 43 passed, parse roundtrip verified.
- A1 wide panel launched: 3 draws `--init-wide` from d3 (the corpus is
  already width-143 with 40,451 decisions; d3 truncated 43 columns —
  docs/M45-plan.md evidence pin 1), then A2 floors chained.

## 2026-08-13 night — A1/A2: WIDE IS A KILL for this lineage

| draw | val_acc | vs tuned | vs iono | pooled |
|---|---|---|---|---|
| d3 narrow (incumbent) | 0.595 | **0.585** | 0.372 | **0.479** |
| wide_e14s3 | 0.609 | 0.535 | 0.383 | 0.459 |
| wide_e11s1 | 0.603 | 0.530 | 0.357 | 0.444 |
| wide_e8s2 | 0.607 | 0.535 | 0.350 | 0.443 |

All three wide draws sit in a tight 0.530–0.535 band vs tuned (−5pp vs d3,
consistent across seeds/epochs = real, not draw noise) while val_acc went
UP (0.602–0.609 vs 0.595) — the val_acc→strength gap bites again. iono
moved nowhere (e14s3's +1.1pp is inside ±4.9). Registered reads:
1. The m41b "width needs ≥10k decisions" law is not sufficient — 40k
   decisions did not reproduce the +2.78pp here. Difference vs m41b: that
   gain came from widening a long-lineage champion on its own corpus;
   this was widening a fresh 3-draw-selected clone (d3's 0.585 carries
   winner's-curse inflation — its siblings were 0.480/0.549, and the wide
   cluster may be closer to the lineage's true mean).
2. Per the pre-registered A2 rule (best pooled), **d3 remains the Track A
   net**. Wide checkpoints kept on disk for the record; not used.
A3 fix probes (benchfloor / gustveto, single-variable on d3) launched.

## 2026-08-13 night — A3: both fix probes NEGATIVE, ship config = planzero

| config | vs tuned | vs iono | pooled | Δ |
|---|---|---|---|---|
| model-pz (d3 baseline) | 0.585 | 0.372 | 0.479 | — |
| model-pz-bf (+benchfloor) | 0.552 | 0.347 | 0.450 | −2.9pp |
| model-pz-gv (+gustveto) | 0.532 | 0.362 | 0.447 | −3.2pp |

Neither meets the pre-registered adoption bar (pooled Δ ≥ 0). benchfloor's
bench-forcing costs more vs these anchors than the 14% bench-out losses it
prevents; gustveto's blanket demote gives up too much tempo. **Ship config
= `model-pz` (planzero) on d3, exactly as floor-measured.** Track A's
product is two clean kills + a confirmed incumbent → A4 gate diagnostic.

## 2026-08-13 night — A4 gate: KILL −24.69pp z=−36.12 — NO LUCARIO SHIP

`m45_A_ship_diag` (spec a33504a8, arm = d3+planzero on lucario_solrock,
control = m43a incumbent, 25 beds × 400 × 2): **POOLED arm 0.5048 vs
control 0.7517 — Δ −0.2469, z=−36.12.** Bed structure: mirror(m28-on-
alakazam) 0.20 (−52.5pp), m28-clone 0.23 (−55.8), walls 0.245–0.275
(−19→−28), garchomp 0.36–0.40 (−40+), grim 0.42–0.52, topgrim 0.43–0.47;
respectable only on dragapult 0.82, rocket 0.76–0.82, tuned 0.545, iono
0.368. The deck+pilot PAIR is field-wide far below both live incumbents.

**Registered lesson (the milestone's core finding):** a two-anchor floor
(0.585/0.372 — "best non-champion seed of the campaign") is a SCREEN, not
a field read. O's seed floored WORSE (0.475/0.365) yet gated +5.51pp,
because the ogerpon pair is strong across the field; the lucario pair is
not. The gate caught it before a slot was spent — instruments worked.

Consequences per kill criteria: Track A ship DEAD (both improvement levers
also killed); Track B's ship path dead by arithmetic (a 15-it leg moves
±3pp, the gap is 25pp). Escalated to Piotr with a no-ship recommendation +
an optional zero-stakes overnight leg-B science run (does an iono-0.20
pool move the iono cell — tests the pool-exposure law on a fresh deck).
The clone RETAINS value: the campaign's first real lucario gate bed, the
L seat for future leagues, the ed7c14ba corpus + harvest machinery.
