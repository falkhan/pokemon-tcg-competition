# M43 — honest PPO + Lane B self-play (diary)

Plan: `docs/M43-plan.md` (+ 2026-08-12 amendment: box migration, r1 specs).
Specs: `m43_laneB_r1` `249a36090018c78f` · `m43_laneA_base_r1`
`b01885e1111300ed` · `m43_laneA_phi_r1` `b7ab4a2513acf803`.
Branch: `feature/m43`. Incremental — entries land the moment results do.

## 2026-08-12 · box migration + Part-1 code fixes

New machine: i7-11800H (16 threads, worker cap stays 8), RTX 3070 8GB,
torch 2.12.1+cu130 (cuda available). Kaggle auth via env vars — verified
(`kaggle competitions list` resolves; we are entered). Local state audited:
checkpoints top out ~M33; the M38+ artifact chain is absent (see amendment);
episode cache 5,807 raw episodes, newest 2026-08-03, **zero episodes of subs
55265099/55265105** (max sub id seen 55218719 < both) — Phase 0 needs a
fresh `refresh`.

Code fixes landed on `feature/m43` (details in the amendment §6):

- **Invariant shaping (BACKLOG #5)** — `rl/collector.py` `_shaping_step`:
  per-step `coef·(γΦ′ − Φ)` + terminal step at Φ=0; `SHAPING_GAMMA = 0.99`
  pinned to `rl.ppo.GAMMA` by test. 5 new tests incl. exact telescoping at
  γ=1 and under per-step discounting.
- **`--shaping-coef` alias (#6)** — same dest as `--race-shaping`,
  `default=SUPPRESS` so the original's 0.0 stands when absent.
- **Gate/ship fix-set equality (#4)** — `tcg.shipping export --fixes`
  templates the gated package into `submission/main.py` (refuses if the
  anchor moved); `ship_verify --gate-arm` check 3b requires set-equality
  with the kind token's `_MODEL_FIX_KINDS` package; threaded through
  `build_submission.sh`. Unknown kind = FAIL, never "no fixes".
- **GPU plumbing** — `rl/policy.resolve_device`; `--device auto|cpu|cuda`
  on `plan_iter train` + `rl.ppo`; batches/models to device, GAE stays
  numpy, all checkpoint saves forced to CPU tensors. Verified: cuda
  `ppo_update` (incl. KL-anchor leg) + 1-epoch cuda `plan_iter train` on a
  real corpus → checkpoint loads `map_location="cpu"`.
- **Finding that wasn't:** the review-flagged sampler width crash in
  `scripts/m40_s2_collect.py` does not exist — `OptionScorerV3.forward` has
  carried the M27 width shim (`options[..., :option_dim]`) all along; "one
  shim covers every consumer" includes the s2 private net. Only the missing
  `oger_mirror` bed was real (added, `model-pz` on the recovered ogerpon
  net, matching the live 55265105 config).

## 2026-08-12 · R0 — m41_ogerpon recovered exactly

`submission/policy_weights.npz` (tracked, commit `3db93a8`) →
`checkpoints/m41_ogerpon.pt`. Sniffed arch: OptionScorerV3, option_dim
**100**, n_state_ids 20, v3 (no `enc_ver`) — exactly the M41 ship.
md5 `4eae59d049787b658438cfeea38794b9`. 20-game smoke vs `rule:tuned:lucario`
at workers 8: 9W-11L (0.450, n=20 — a liveness check, not a read).

## 2026-08-12 · R1 — fresh ingest

- Loss-family deck CSVs re-exported from the LOCAL cache before any refresh,
  at the exact historical hashes: dragapult `3631d393` (112 seats), garchomp
  `c7b3253f` (672 seats), rocket `59e27a5e` (150 seats). The two
  artifact-dependent test failures cleared (suite 1292 green, ci_gate PASS,
  KNOWN_FAILURES emptied — all 4 old-box/Windows entries pass here).
- `refresh --subs 55265099 55265105`: **121 new episodes**, parquet 8533 →
  8654 rows. Live-sub coverage now **53 games (55265099)** and **68 games
  (55265105)** — both clear the Phase-0 G-10 floor (n ≥ 45). Newest episode
  2026-08-07.
- Live leaderboard top (2026-08-12): 1232.9 — the 1000+ campaign bar is
  ~230 below the summit.
- Snowball refresh of the top-30 opponent subs (`--max-new 400`) running;
  `harvest --min-games 3` and the corpus builds follow it so every corpus
  reads one parquet state.

## 2026-08-12 · Phase 0.1/0.2 — first live reads on the M41b ships

(53 and 68 games; both clear the G-10 n≥45 floor. Matchup cells are
forensic-only per G-9.)

- **55265099 (wide alakazam): implied ELO 823, 95% CI [729, 911]**, WR
  0.558 (29W-23L, avg_opp 782). Point estimate at parity with the all-time
  best settled (817). **Consequence 0.a: NOT a fourth offline→live
  inversion — the width-143 substrate is live-validated; both lanes proceed
  on it.** (CI is wide; the band claim stays provisional until n≥150.)
  Forensic holes: grim 4-6 (0.40 — the known family), mirror 6-5,
  munkidori/grim-adjacent 3-6.
- **55265105 (ogerpon): implied ELO 650, 95% CI [570, 732]**, WR 0.463
  (31W-36L, avg_opp 676). Weak. **Loss modes are NOT what M41b measured
  offline: grim is WON live 8-2 (0.80); the holes are wall 1-6 (0.14) and
  archaludon 0-5 (0.00).** Also crustle/dwebble (wall-family) 1-5 pooled.
- **Consequence 0.b applied as pre-registered:** archaludon is a new
  dominant live loss family for the ogerpon pilot → the arch beds
  (`arch_d1–d3`) JOIN Lane B's B1/B2 collection pool (wall was already in).
  The grim beds stay (they anchor the offline-measured pilot gap), but the
  live read reframes Lane B's payoff surface: wall+arch are where live
  points are bleeding.
- Both live subs stay up through further accrual (plan default).

**Postmortems (0.2, ≥3/sub; full texts `runs/m43_pm_<ep>.txt`, replays in
`replays/`):**
- 55265099: `90148914` PRIZES (opp took theirs), `90154454` **DECK-OUT —
  we could not draw** (the standing live loss family), `90161390` PRIZES
  (vs grim list `3121746f`).
- 55265105: `90144041` vs Archaludon ex = **BENCHED-OUT — final KO with an
  empty bench, opponent still had 4 prizes to take**. This is the M41b
  offline `empty-bench` mode, live — **consequence 0.b's first branch also
  fires: Lane B's target is live-confirmed**, and it presents vs ARCH, not
  grim. Several other losses (`90139857`, `90144757`, `90146833`,
  `90148245`, `90137017`) come back "unclassified (timeout/error? check
  statuses)" from the classifier despite 0 err in forensics — a classifier
  gap on these terminals, noted, not chased in M43.
- **PENDING HUMAN STOP: Piotr's replay review of these losses** (0.2).
  Machine-side work (rebuild, E0, Lane B collection) proceeds — nothing
  ships or gates before the review + explicit go.

## Rebuild manifest (fills in as artifacts land)

| artifact | source recipe | corpus rows | epochs | val_acc | md5 |
|---|---|---|---|---|---|
| m41_ogerpon.pt | exact recovery from tracked bundle | — | — | — | 4eae59d0… |
| (R2–R6 pending) | | | | | |
