# M26 — stop the self-mill: ATTACH-class fidelity (diary)

Plan: `docs/M26-plan.md` (refined 07-22 from the stub). Antecedents: `docs/M25.md`
(ship diary, sub 54897966 + first-9-games post-mortem). Branch: `feature/m26`.

## 2026-07-22 ~11:00 — Phase 0 + lever 1 (planning session): thesis CONFIRMED on n=40; per-class report lands the smoking gun

**Lever 1 — per-class fidelity report (read-only, run during planning).** The
train/val split is deterministic (`default_rng(0)`, 10% of games,
`rl/plan_iter.py:822-829`) — reconstructed it exactly and broke down
`m25_bc_alakazam_v3h`'s 0.668 headline val_acc by teacher OptionType
(type = `options[starts+labels, :17].argmax`, acted id = `option_ids[..., 0]`):

| class | corpus share | val acc |
|---|---|---|
| CARD | 28.9% | 0.704 |
| **PLAY** | **23.0%** | **0.486** |
| EVOLVE | 13.4% | 0.724 |
| YES | 7.9% | 1.000 |
| ABILITY | 7.2% | 0.874 |
| ATTACK | 6.4% | 0.608 |
| **ATTACH** | **5.9%** | **0.408** |
| END | 3.2% | 0.653 |

Within ATTACH: **Telepath 23/67 = 0.343** (worst cell; Basic-P 0.441, Enriching
0.484). Top Telepath confusion: model picks **ATTACK instead** (11/44 misses) —
it rushes the attack (which ends the turn) and the free attach evaporates.
**Stub hypothesis CONFIRMED**, and a second weak class surfaced: **PLAY at 0.486
is the largest class in the corpus** — the supporter-hoarding flags (Boss's
Orders, Hilda) live there. Corpus mass: 28,651 rows; 1,704 ATTACH (774
Telepath-ATTACH); 6,583 PLAY.

**Phase 0 — live refresh (44 new episodes fetched, 54897966 now n=40):**
- **20W–20L**; score peaked **820.1**, now **725.9** and drifting down.
- **Deck-out losses 11/20 (55%)** — clears the ≥50% kill-gate bar; thesis holds.
  18/40 games end with our deck ≤3.
- Energy attached **46.4%** of turns (teacher 57%); Telepath **6.4%** of
  attach-opportunity decisions (teacher 15.2%); contested attaches (Telepath AND
  another energy attachable): Telepath 14 : 71 other-or-no-attach → **16%** of
  the attach picks vs teacher 45%.
- Caveat vs the n=9 post-mortem: avg turns 17.8 (was 21.8 on n=9; teacher 16.6)
  and WR is 0.500 — the first-9 read overstated severity, but the causal chain
  (under-attach → slow setup → Dudunsparce self-mill) stands.
- Low-deck (≤6) Dudunsparce draw discipline: took 100/158 offered (63%) —
  teacher-level recklessness (61%), NOT a fidelity gap; unchanged target.

**Pipeline gap (why this shipped):** the offline battery
(rule:lucario / rule:dragapult / random:kyogre) has **no stall/mill opponent** —
a deck-out defect is structurally invisible pre-ship. M26 adds a grim-clone mill
opponent (Phase 2) + behavior diagnostics to close the loop.

**Piotr decisions (this session):** lever 2 (ATTACH weighting) and lever 3
(attach overrides) run as **separate A/B arms**, compose only if both survive;
conditional **PLAY×2 arm pre-approved** (fires only if an ATTACH arm promotes
with PLAY unimproved). Lever 3 gains a second variant on Piotr's ask: **O2
attach backstop** — never end a turn (END or ATTACK chosen) with a legal energy
attach unplayed — guaranteeing per-turn attachment when possible; O1 stays the
Telepath-priority rule. Both override-law-gated, off by default, mandatory
sign-off before ship.

## 2026-07-22 ~11:30 — Phase 1 DONE (class_report.py, acceptance exact) + Phase 2 first read

**`scripts/class_report.py`** reproduces the pinned numbers exactly (0.668 /
ATTACH 0.408 / Telepath 0.343 / PLAY 0.486) — split reconstruction verified.
New signal from the PLAY-by-card breakdown: **Boss's Orders 2/31 = 0.065, Hilda
7/32 = 0.219, Xerosic's Machinations 8/32 = 0.250** — the hoarded supporters are
the worst PLAY cells, and the top confusion is again ATTACK-instead (Boss's
Orders → ATTACK ×16). The clone systematically spends its turn-ending action
(ATTACK) while free actions (attach, supporter) sit unplayed — one behavioral
defect expressed across two classes. O2's backstop targets exactly this shape
for energy; a supporter analogue is a future lever (not this milestone).

**Phase 2, grim mill baseline seed 1 (n=200):** shipped ckpt vs
`m25_bc_grim_54861775` on the mill deck: **121W–78L–1D, wr 0.608**. Live vs the
real grimmsnarl family we were 1W–4L — the killed clone does NOT reproduce the
stall punish at full strength (as flagged: val 0.565/0.580 clone quality).
Opponent stays ADVISORY; pin = pooled n=400 pending seed 2.

## 2026-07-22 ~12:00 — Phase 2 pin set; Phase 3 arms trained; per-class RUN NOISE calibrated

**Phase 2 grim pin (advisory):** shipped ckpt vs `m25_bc_grim_54861775` (mill
deck) pooled n=400 = **0.6175** (seeds 0.608/0.632); alt clone `54863653` is
weaker (0.740 @ n=100) → **canonical grim opponent = 54861775**. We win
comfortably offline while losing 1–4 live vs the real archetype — the killed
clone does not reproduce the stall punish. Pin stays ADVISORY (regression watch,
not a gate).

**Phase 3 trains (`--class-weight`, new in `plan_iter train`, unit-tested):**

| ckpt | headline | ATTACH | Telepath | PLAY | EVOLVE | ATTACK |
|---|---|---|---|---|---|---|
| shipped `m25_bc_alakazam_v3h` | 0.668 | 0.408 | 0.343 | 0.486 | 0.724 | 0.608 |
| `m26_bc_control` (re-run, NO weighting) | 0.666 | 0.452 | 0.358 | 0.494 | 0.646 | 0.709 |
| `m26_bc_attach3` (×3) | 0.660 | 0.497 | 0.433 | 0.469 | 0.619 | 0.683 |
| `m26_bc_attach5` (×5) | 0.661 | 0.541 | 0.493 | 0.437 | 0.688 | 0.593 |

**Calibration lesson (the control row is the story):** re-running the IDENTICAL
recipe swings EVOLVE −7.8pp, ATTACK +10.1pp, ATTACH +4.4pp — per-class deltas of
±5–10pp on the 150–400-row classes are RUN NOISE (torch init/shuffle unseeded;
the SPLIT is seeded — default_rng(0) — correcting the M25 caveat). The
pre-registered "−3pp any-class" kill bar is uncalibrated at this n: it killed
both arms on deltas the control reproduces without any weighting. What survives
noise: **ATTACH and Telepath dose-responses are MONOTONIC across
control→×3→×5** (0.452→0.497→0.541; 0.358→0.433→0.493) — a real fidelity
effect. Headlines all within noise of the 0.668 pin. Strength screens decide
(the fidelity gate demotes to directional-only for per-class deltas; keep the
headline-floor condition).

**Arm B (×5) promoted to screen** (best target profile): n=200 s1 vs
`rule:lucario` running. Arm A (×3) held as backup — screen only if B dies.

## 2026-07-22 ~13:00 — Arm B battery (strength unchanged, as designed); Phase 4 infra landed

**Arm B (`m26_bc_attach5`) confirm battery** (n=200×2 seeds unless noted):

| opponent | seeds | pooled n=400 | pin |
|---|---|---|---|
| `rule:lucario` | 0.645 / 0.660 | **0.6525** | 0.660 (−0.75pp, inside MDE) |
| `rule:dragapult` | 0.320 / 0.290 | **0.305** | 0.3075 (match) |
| `random:kyogre` | 0.960 (n=200) | — | ≥0.95 ✓ |

Strength statistically indistinguishable from the shipped pin on every axis
while ATTACH fidelity is up 0.408→0.541 and Telepath 0.343→0.493 — the
weighting bought fidelity for free, it did NOT buy strength. Whether the
fidelity translates to live behavior (attach rate → 57%, fewer deck-outs) is
the offline_behavior + grim question. (zsh lesson re-learned: `$CKPT:clone`
ate the colon — brace+quote, M23 law.)

**Phase 4 infra landed (commit 7179747, 621 tests green):**
`rl/plan.apply_attach_overrides` — O1 `telepath` (Telepath ATTACH legal + bench
space → attach it), O2 `backstop` (chosen action is END/ATTACK with a legal
energy attach unplayed → attach first; attack re-offered next MAIN). Single
source of truth: bundle-pure in `rl/plan.py`, imported by BOTH matchrunner
fn3/fn4 (spec kinds `modelt:`/`modela:`/`modelta:` — kinds not env vars, so
jsonl run keys can't alias) and `submission/main.py` (`PKM_ATTACH_FIXES` env
flag, SHIP DEFAULT OFF per override law). `test_bundle_twins` pins the
rl/plan.py ↔ submission/rl/plan.py copy byte-identical.
`scripts/offline_behavior.py`: instrumented single-process engine games —
attach-rate / Telepath share / deck-out metrics screens can't see.

## 2026-07-22 ~13:45 — override screens: O1 non-inferior (early "win" was seed luck), both overrides lift the grim axis; behavior confirms all levers move the defect

**O1 (`modelt`, Telepath-priority on the shipped ckpt) vs `rule:lucario`:** seeds
0.680/0.745/0.590/0.670 → **pooled n=800 = 0.671** vs pin 0.660 (+1.1pp, inside
noise; the first-pair 0.7125 was seed luck — spread 15.5pp). NON-INFERIOR, no
proven strength gain. **O2 (`modela`, backstop): 0.665/0.625 → pooled 0.645** —
clears the 0.625 non-inferiority bar. The override-law losing streak is not
broken, but for the first time an override is FREE (every prior one cost
mirror strength).

**Grim mill axis (n=200 s1, advisory pin 0.6175):** O1 **0.695**, O2 **0.708**,
attach5 pooled n=400 0.635 — both overrides at the edge-of-MDE improvement,
consistent with the mechanism (attach discipline punishes mill).

**Behavior probes (`offline_behavior.py`, n=24 vs grim clone, seed 5, same
setup for all — RELATIVE reads):**

| arm | attach rate | Telepath opp. taken | contested Telepath | deck-out losses | WR |
|---|---|---|---|---|---|
| shipped baseline | 58.0% | 11.2% | 2:35 | 1/8 | 0.667 |
| attach5 (lever 2) | 67.1% | 20.2% | 10:39 | 2/10 | 0.583 |
| O1 (baseline+telepath) | 68.0% | **66.4%** | **37:6** | 0/3 | 0.875 |
| O2 (baseline+backstop) | **72.8%** | 18.4% | 5:28 | 1/7 | 0.708 |

All three levers move the defect axes. O1 overshoots the teacher's Telepath
preference (45% contested → 86%) by construction; whether overshoot is good is
a strength question, answered above as "at least free, maybe better vs mill."
Caveat: offline vs the weak grim CLONE the baseline already attaches 58%
(teacher-level) — the live 46.4% under-attach is distribution-dependent, so
these are relative A/B reads, not live predictions. Telepath suppression IS
visible offline (11.2% vs teacher ~15% opportunity-take with 2:35 contested)
and every lever lifts it.

Composed candidate `modelta:m26_bc_attach5` (lever 2 + O1 + O2) full battery
launched — the ship-decision matrix completes with it.

## 2026-07-22 ~14:30 — FINAL CANDIDATE MATRIX (ship decision to Piotr)

| candidate | lucario | dragapult (EVAL floor) | kyogre | grim (advisory) | behavior: tele-contested / attach-rate |
|---|---|---|---|---|---|
| shipped baseline (= live 54897966) | 0.660 n=400 (pin) | 0.3075 (pin) | 0.965 | 0.6175 n=400 | 2:35 / 58.0% |
| B `m26_bc_attach5` (lever 2) | 0.6525 n=400 | 0.305 n=400 | 0.960 | 0.635 n=400 | 10:39 / 67.1% |
| **O1 `modelt` on shipped ckpt** | **0.671 n=800** | **0.3375 n=400** ← new campaign record | **0.975** | **0.6875 n=400 (+7pp, ~real)** | **37:6 / 68.0%** |
| O2 `modela` on shipped ckpt | 0.645 n=400 | — | — | 0.708 n=200 | 5:28 / 72.8% |
| C composed (attach5+O1+O2) | 0.6275 n=400 (marginal) | 0.3125 n=400 | — | 0.632 n=200 | 34:6 / 70.7%, probe WR 0.458 |

**Read:** O1 — the shipped checkpoint plus ONLY the Telepath-priority rule — is
the dominant profile: non-inferior-to-slightly-up vs lucario (n=800), the best
out-of-loop dragapult number in campaign history (0.3375 vs the 0.3075 M25
record), +7pp on the grim mill axis at pooled n=400 (~beyond pooled MDE),
floors clear, and the most minimal delta from the live sub (identical weights,
one card-fact rule; cleanest live A/B attribution). Stacking more (O2,
weighting) consistently made things worse — the composed arm is the weakest
lucario number and its probe WR collapsed. Dose lesson echoed: one precise
intervention beats three stacked ones.

**Override law compliance:** O1 is off by default everywhere; shipping it means
flipping `PKM_ATTACH_FIXES` default to "telepath" in submission/main.py (the
predicate itself is untouched, single-sourced in rl/plan.py, test-pinned to the
bundle copy). MANDATORY Piotr sign-off pending — no ship without it.
