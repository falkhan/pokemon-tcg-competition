# M30 — deck-economy O-rules + single-teacher data arm (diary)

Plan: `docs/M30-plan.md`. Branch `feature/m30`. Piotr approved the plan
(2026-07-23 morning) with the review findings incorporated: split ship bars
(rule arms = non-inferiority, arm D weights change = resolved-better),
tempo-only fallback arm, ability-option resolution check in P0, behavior pins
relative to a measured offline baseline.

## 2026-07-23 ~11:30 — housekeeping + P0 start

M29 post-mortem committed on feature/m29 (2252a97: docs + refreshed
episode/opp_decks caches), branched `feature/m30`. Post-mortem "M28-O1" label
corrected to M26 (54903635 = m25_bc_alakazam_v3h + O1, per docs/M26.md:202).
Loss-forensics probe rescued from the session scratchpad to
`scripts/loss_forensics.py`.

## 2026-07-23 ~11:40 — P0.4 semantics checks PASS (40 cached replays, our seats)

Measured at the next MAIN prompt after the action (selection sub-prompts
resolved first — the naive next-decision read shows Sacred Ash at deck delta 0
because the pick-from-discard prompt intervenes):

- **Dudunsparce ability**: dominant delta deck −1 / hand +3 (104 obs) — draw 3,
  then itself + basis shuffle back. Net −1/use but −3 gross draw; at deck ≤3 a
  use draws the deck to 0 → lethal next turn-start draw. `deckguard ≤6` is
  conservative and correct.
- **Sacred Ash**: deck +5/+4/+3/+2/+1 (15 obs), capped by Pokémon in discard —
  confirmed deck-REFILL. The `ash` rule's premise holds in this engine.
- **ABILITY options**: always `area` (BENCH=5/ACTIVE=4/STADIUM=7) + `index`,
  `cardId=None` — `_hand_card_id` cannot resolve them; the board-area resolver
  (per plan review Finding 3) is required. STADIUM-area abilities exist too;
  the Dudunsparce predicate must check the resolved board Pokémon id, never
  assume.

## 2026-07-23 ~11:50 — P0.2+P0.3: instruments extended, offline baselines land
— Finding 4 vindicated

`offline_behavior.py` extended: END-with-playable-item rate (+ declined item
ids), per-side deck-out split, A-deck≤3 share, avg A deck at end. First
baseline pass on the live config (`modelt:m25_bc_alakazam_v3h:clone54618168`),
n=60 seed 1:

| axis | vs rule:lucario | vs grim clone |
|---|---|---|
| wr | 0.567 | 0.667 |
| END-with-item | **2.5%** (3/118) | **10.3%** (18/175, Poké Pad 17) |
| telepath attach | 91.8% | **56.4%** |
| self deck-out losses | 0/26 | 1/20 |
| avg turns | 10.4 | 16.4 |

The live-replay absolutes (12.0% END-item / 84.9% telepath overall) do NOT
transfer per-matchup — pins must be relative to these same-instrument
baselines, exactly as pre-registered. Two substantive reads: (1) the deck-out
defect barely reproduces vs the grim clone offline (1/20 — the M26 advisory
caveat again), so P3's deckguard/ash counters lean on the more sensitive
deck≤3 share + rocket test bed; (2) telepath contested share vs grim is 56.4%
at BASE — the "regression" axis is heavily matchup-dependent; only same-
matchup deltas count. Baselines re-run after the deck≤3 counter landed
(runs/m30_p0_baseline_{luc,grim}.txt are the final-instrument versions).

⚠ Instrument discovery: identical command + `--seed 1` produced DIFFERENT
games across the two passes (luc 34W–26L then 39W–21L; telepath-vs-grim 56.4%
then 71.9%) — `offline_behavior.py` seeds Python's `random` but the engine
draws from its own unseeded RNG. Consequences, pre-registered for P3: (a) the
FINAL baseline numbers (second pass, final instruments) are the comparison
reference: luc END-item 6.7%·tele 90.7%·deck≤3 7/60·wr 0.650; grim END-item
13.7%·tele 71.9%·deck≤3 6/60·wr 0.600; (b) high-variance axes (telepath share
vs grim, wr) need n=120 (two 60-game runs pooled) before any kill/keep call;
(c) rule-engagement itself (END-item→~0 when `tempo` is on) is mechanical and
readable at n=60.

## 2026-07-23 ~11:45 — P1 DONE: apply_play_overrides landed, 638 tests green

`rl/plan.py` gains `POFFIN_ID/POKE_PAD_ID/SACRED_ASH_ID/DUDUNSPARCE_IDS`,
`_board_pokemon_id` (ABILITY options carry area+index only — P0 probe), and
`apply_play_overrides` (O5 ash > O3 tempo promotions; O4 deckguard demotion).
Wired at `submission/main.py` (after attach overrides) and both matchrunner
closures; new spec kinds `modelt-{tempo,guard,ash,eco}`. Twin byte-synced;
15 override tests; full suite 638 passed.

## 2026-07-23 ~11:50 — P2: trains done; ⭐ rocket clone PASSES the strawman law

- `m30_54773249_winners` (Arm D): val_acc 0.654 on 334 wins / 25,356 rows
  (m28_winners' 0.699 not comparable — different val distribution).
- `m30_bc_rocket_54834745`: val_acc 0.587 — grim-clone fidelity band. Deck
  exported: `data/kaggle/rocket_3394cd30_deck.csv` (md5 a246be4f).
- **Validation (n=60): the live-pin config LOSES to the rocket clone 26W–34L
  (0.433), with 20/34 losses by self-deck-out, avg 24.1 turns, deck≤3 in
  26/60 games, END-with-item 26.6% (Poké Pad 60 declines, Sacred Ash itself
  declined 3×).** First offline opponent EVER to reproduce the live
  stall/grind loss mode (the grim clone never did — M26/M28 strawman law).
  Adopted as the P3 primary test bed; candidate advisory floor for P4 with
  pin 0.433 (this read). Also externally validates the M29 post-mortem's
  loss anatomy: the deck-economy defect is real and this matchup is where
  it kills.

## 2026-07-23 ~11:55 — P3 first pass vs grim (n=60, engagement ambiguous)

vs grim baseline (wr 0.600 / END-item 13.7% / deck≤3 6/60 / avg-deck-end 13.8):
tempo wr 0.583, **END-item 0/204 — rule mechanically engaged, defect
eliminated by construction**; guard wr 0.683, avg-deck-end 16.5; ash wr
0.700 but deck-out losses 4/18. Grim games rarely reach deck≤6/≤10, so
guard/ash engagement is unreadable there — engagement counters (Sacred Ash
plays + deck-at-play, Dudunsparce-at-low-deck used/offered) added to
`offline_behavior.py`; second-pass screens re-running vs the ROCKET clone
(the matchup that actually drains the deck), seed 2, all four arms +
baseline.

## 2026-07-23 ~12:15 — P3 RESOLVED vs rocket: guard wins, ash helps, TEMPO
BACKFIRES in the grind matchup, eco (composed) worst

Rocket baseline (n=120 pooled seeds 1+2): wr **0.458**, deck-out losses 37/65
(57%), deck≤3 51/120, END-item ~26%. Engagement smoking gun at baseline:
**Dudunsparce draw used on 86/88 prompts at deck≤6**; Sacred Ash played
mostly at deck 0–2 (too late). Arm reads (n=60, seed 2, same instrument):

| arm | wr | deck-out L | engagement |
|---|---|---|---|
| guard | **0.533** | **11/28** | dud-at-low-deck **0/101** (was 86/88) |
| ash | 0.517 | 17/29 | 37 ash plays, shifted to deck 3–8 (was 0–2) |
| tempo | **0.383** | 25/37 | END-item 6.1% ✓ but deck≤3 35/60, avg deck end 6.9 |
| eco (all 3) | **0.333** | 18/40 | tempo component dominates the damage |

Verdicts (pre-registered criteria + M21 opportunity-cost law):
- **guard SURVIVES** — target behavior eliminated, deck-outs down, wr up.
- **ash SURVIVES** — engagement real (earlier, more plays), wr slightly up.
- **tempo DROPPED from the composition** — it cuts its counter as designed,
  but forced Poffin/Poké Pad plays CONSUME deck; in the grind matchup that
  converts directly into deck-outs (0.383, deck≤3 35/60). The M29 "defect"
  is partially adaptive vs stall. Tempo-only still runs in P4 as the
  pre-registered fallback arm (short-game matchups may price it positively).
- **eco composition dead** (M26 "one precise intervention" law again) —
  P4 composition = guard+ash (`modelt-guardash` spec kind added).
- Rare Candy (1079) newly visible among END-declines vs rocket — noted, not
  in any rule; a future lever candidate.

## 2026-07-23 ~12:20 — P4 floors phase LAUNCHED

`scripts/m30_p4_battery.sh floors` (background): 4 arms (b1ga, b1tempo, b2ga,
darm) × dragapult 2×200 + grim 2×200 + rocket 2×200 + kyogre 200 — floors
BEFORE the n=800 mirror per post-mortem rec #5; mirror phase only for arms
alive after floors. Decide script `scripts/m30_p4_decide.py` encodes the
SPLIT bars (rule arms non-inferiority; darm resolved-better). Hourly Hermes
heartbeat attached (`scripts/battery_heartbeat.sh`).

## 2026-07-23 ~13:05 — PC crash during floors; recovery clean (M8.1
checkpoints did their job)

Machine crashed at ~12:57 mid-way through the LAST floors run (darm vs
kyogre, 108/200 games checkpointed). All 27 other runs complete on disk
(`runs/m30_*_{drag,grim,rocket,kyo}_s*.jsonl` + `runs/m30_p4_floors.log`).
Resumed the interrupted run via the same `--checkpoint` path (header-key
resume, completed chunks skipped). Floors eyeball from the log, pending the
decide script: dragapult pooled b1ga 0.3125 / b1tempo 0.300 / b2ga 0.365 /
darm 0.465 (all non-inferior vs pin 0.3375, darm strongly ABOVE); kyogre
0.970/0.980/0.975 for the three completed arms. Rocket advisory: b1ga
0.445 pooled ≈ baseline 0.458 (guard+ash gain did NOT carry from the
offline_behavior harness read); b2ga 0.555, darm 0.600 — both above. Grim
advisory: b1ga 0.62 vs pin 0.6875 (slightly soft), darm 0.72.

## 2026-07-23 ~13:15 — P4 floors RESOLVED (decide script): all 4 arms alive
→ mirror launched

Interrupted darm-kyogre run finished 0.960 (≥0.95 floor ✓). Formal decide
read (`m30_p4_decide.py`, pooled 2×200 per axis):

| arm | drag (pin .3375) | kyo | grim adv (pin .6875) | rocket adv (pin .458) |
|---|---|---|---|---|
| b1ga | 0.3125 z=−0.76 ✓ | 0.970 ✓ | 0.6212 z=−1.97 (soft) | 0.4450 z=−0.25 |
| b1tempo | 0.3000 z=−1.14 ✓ | 0.980 ✓ | 0.6625 z=−0.76 | 0.4450 z=−0.25 |
| b2ga | 0.3650 z=+0.82 ✓ | 0.975 ✓ | 0.6488 z=−1.16 | 0.5550 z=+1.87 |
| darm | 0.4650 **z=+3.71** ✓ | 0.960 ✓ | 0.7200 z=+1.01 | 0.6000 **z=+2.75** |

Reads: darm is resolved-better on dragapult AND the rocket advisory — but
dragapult is pre-registered NOT a tie-breaker (M29 trap axis) and darm still
must clear mirror RESOLVED-better (z>+1.96). b1ga's guard+ash rocket gain
from the P3 offline_behavior read did not replicate in the matchrunner
harness (0.445 ≈ 0.458 baseline) and its grim advisory is at the noise edge
(z=−1.97) — regression-watch note for the final call. Mirror phase launched
13:15 for all 4 arms (4 × 4×200 vs rule:lucario), heartbeat attached.

## 2026-07-23 ~13:25 — P4 mirror RESOLVED: 3 rule arms clear, ⭐ ARM D
KILLED by the split bar — the M29 pattern reproduced and caught

Mirror n=800 pooled (decide script): b1ga **0.6900** z=+0.82 · b1tempo
0.6512 z=−0.83 · b2ga 0.6863 z=+0.65 — all three clear mirror
non-inferiority + drag + kyo. **darm 0.6231 z=−2.01 — mirror
resolved-WORSE**; fails its resolved-better bar and is dead as a ship
candidate (evidence-only, per pre-registration). The kill is the milestone's
headline result: darm was resolved-BETTER on dragapult (+0.1275, z=+3.71)
and the rocket advisory (+0.142, z=+2.75) yet resolved-worse on the mirror —
exactly M29's live signature (offline-axis gains, live/mirror loss). The
M29 bar relaxation would have shipped it; the split bar killed it offline
for the cost of a battery. Single-teacher law survives another test: even
54773249's own 334 wins, single-teacher, winners-only, could not clear
resolved-better.

Behavioral-claim status (the remaining bar component for rule arms):
- **b1tempo FAILS on existing P3 data** — vs rocket its deck-out-loss share
  is 25/37 (67.6%) vs baseline 37/65 (56.9%); bar requires strictly below.
  END-item cut (✓) does not rescue it. Out.
- b1ga (composed guard+ash) and b2ga need fresh reads: the composition was
  never run through offline_behavior (P3 measured guard/ash singly), and B2
  has no O1-only baseline at all. Launched 13:25: {b1ga-composed, B2+O1
  baseline, B2+guard+ash} × {rocket, grim} × seeds 1,2 (n=120 pooled per
  the pre-registered variance rule) → `runs/m30_p4b_*.txt`.
- Tie-break note, recorded before those numbers land: the PLAN pre-registers
  "best deck-out-share improvement, then grim" among qualifying rule arms;
  the decide-script docstring paraphrased it as "best rocket-advisory
  delta". The plan wording governs (it is the earlier pre-registration);
  discrepancy flagged to Piotr rather than silently resolved. On the
  rocket-advisory axis the arms differ visibly (b1ga 0.445 ≈ baseline,
  b2ga 0.555 above it); deck-out-share comes from the p4b reads.

## 2026-07-23 ~13:45 — P4 CLOSED: behavioral claims resolved
(`runs/m30_p4b_*.txt`, n=120 pooled per config per matchup) — no arm clears
its FULL bar cleanly; decision package to Piotr

Rocket bed (each arm vs its OWN base's O1-only baseline, pre-registered):

| config | wr | deck-out share | END-item | telepath |
|---|---|---|---|---|
| B1+O1 baseline | 0.458 | 37/65 = 56.9% | 25.8% | 73.0% |
| b1ga (B1+guard+ash) | 0.433 | 57.4% ✗ | 30.0% ✗ | 66.7% ✗ |
| B2+O1 baseline | 0.608 | 57.4% | 30.7% | 70.4% |
| b2ga (B2+guard+ash) | 0.575 | **45.1% ✓** | 29.0% ✓ | 65.4% (−5.0pp, z≈−1.9) |

- **b1ga FAILS the behavioral claim on all three components** — mechanically
  fully engaged (Dudunsparce 0/364 prompts at low deck, 90 ash plays) yet
  economy did not improve: with the draw demoted, B1 ENDs instead (END-item
  UP 25.8→30.0%). Consistent with its flat matchrunner rocket read (0.445)
  and soft grim advisory (z=−1.97). The P3 single-rule guard read (0.533,
  39% deck-out) was an n=60 seed read on exactly the axes the variance rule
  said need n=120 — it did not replicate composed. Per the pre-registered
  bar, b1ga does NOT qualify despite clearing the strength gates.
- **b1tempo FAILED earlier** (deck-out share 67.6% vs 56.9%).
- **b2ga is the only near-qualifier**: strength gates ✓, END-item strictly
  below ✓ (barely, z≈−0.6), deck-out share strictly below ✓ (−12.3pp,
  z≈−1.2 — point-clear, not resolved), telepath POINT-DEGRADED −5.0pp
  (z≈−1.9; within its own 10pp seed spread; grim bed −2.2pp). The
  pre-registration said "not degraded" without fixing point-vs-significance —
  interpretation is Piotr's call, not mine.
- Mechanistic read worth keeping: guard+ash convert ONLY on the B2 base
  (deck-out 57.4→45.1%) — the winners-corpus model has somewhere better to
  spend the turn when the Dudunsparce draw is demoted; B1 does not. Also
  B2+O1 already beats B1+O1 by +15pp on the rocket bed (0.608 vs 0.458).
- Tie-break moot (≤1 qualifier). Structural note from the plan: with b1ga's
  rules dead on B1, arm 1 degenerates to the exact 54903635 config — the
  ROLLBACK re-ship — which needs no new evidence (it is the pin source,
  735@44 live). The M29 rollback decision is therefore now a clean
  three-way call for Piotr: (a) rollback re-ship 54903635 as-is, (b) ship
  b2ga under the relaxed rule-arm bar (honest tension: its base is a
  weights change vs live — never live-validated — and it does NOT meet the
  resolved-better bar that the M29 lesson demands of weights changes;
  mirror z=+0.65 only), (c) no ship, more evidence. Pre-registered rule
  "if no arm clears: evidence to Piotr, no ship, no exceptions" — STOPPING
  here; no P5 QC until Piotr picks the arm.

## 2026-07-23 ~13:50 — Piotr's call: SHIP b2ga → P5 executed, STOPPED at
the human-QC gate

Piotr picked **ship b2ga** (m28_winners + O1 + deckguard + ash) over the
rollback and no-ship options, accepting the telepath point-noise and the
relaxed-bar weights-change tension (both flagged explicitly in the decision
package). P5 ritual, verifier-first per
`verify-before-consequential-actions.md`:

- Ship default changed: `submission/main.py` `PKM_ATTACH_FIXES` default
  `"telepath"` → **`"telepath,deckguard,ash"`** (comment convention updated
  with the sign-off reference). Full suite 638 passed after the change.
- `./build_submission.sh --checkpoint m28_winners.pt --deck clone54618168`
  → parity/isolation/deck-legal/gate-game OK →
  `dist/submission_neural_20260723_134649.tar.gz`.
- Tarball verified: deck.csv md5 **8e8cf124** = clone54618168 ✓; bundled
  npz **21/21 arrays byte-match `m28_winners.pt`** ✓; bundle main.py
  direct-import assert `_ATTACH_FIXES == {telepath, deckguard, ash}` ✓;
  `submission/` dir confirmed the exact ship state (same md5 + weights).
- QC (deliberate, flagged deviation from the plan's literal text): opponent
  = ROCKET clone, not grim — P3 showed grim games rarely reach deck≤6/≤10,
  so grim QC replays would never exercise the new rules; rocket is the
  validated deck-economy bed. 3 games of the actual bundle → **1W–2L**
  (binomially consistent with the 0.575 pooled read),
  `replays/m30_qc_b2ga_000..002.html`.
- **STOPPED. No submit. Awaiting Piotr's manual replay review and explicit
  go**, including sign-off on the default-string change and the QC-opponent
  substitution.

Watch-list for the live A/B if shipped (vs 54903635 and vs the retiring
54914673): deck-out loss share (target: below 30%), Dudunsparce-at-low-deck
(expect ~0), Sacred Ash timing (earlier than deck 0–2), telepath attach
(the −5pp offline point-read — if it reproduces live it is real),
mirror/lucario-family record (b2ga 0.686 offline).

## 2026-07-23 ~14:10 — Piotr QC verdict: NO GO — both reviewed losses were
deck-outs; deck-drain forensics launched

Piotr reviewed 2 of the 3 QC replays: both losses are self-deck-outs; his
call is this won't move the leaderboard while that mode persists. New
instrument `scripts/deck_drain.py` (attributes every A deckCount delta to
the action chosen between prompts; on the offline_behavior loop). First
n=40 read (b2ga vs rocket, seed 3):

- **The rocket clone does NOT mill us** — opponent-phase drain 4.7–7.4% and
  net-POSITIVE refills. ~95% of deck consumption is self-inflicted.
- Drain by source in LOST games: turn draw ~17%, **EVOLVE Kadabra ~10% +
  EVOLVE Alakazam ~9%** (the evolution line itself consumes deck on evolve
  — engine effect, semantics to pin), **ABILITY Fezandipiti ex ~9%**,
  PLAY Rare Candy ~8%, **ATTACH Enriching Energy ~6%**, PLAY Dawn ~5%,
  Poffin ~5%, Dudunsparce only ~4–6% (guard works; the residual is
  above-threshold use). Losses run the draw engine harder than wins
  (Fez 88v42, Enriching 79v44 in the seed-3 n=40 read).
- ⚠ Instrument correction: `deck_end==0` OVERCOUNTS deck-outs — 2 of 4
  "deck-out" losses in the probe had the opponent at 0 prizes left (prize
  losses where our deck coincidentally hit 0). All M30 deck-out counters
  used this heuristic; script now classifies by the engine RESULT log
  reason (2 = true deck-out). Prior deck-out SHARES are still comparable
  between arms (same bias both sides) but absolute levels were inflated.
- n=120 two-seed run with reason-coded losses + per-use drain costs in
  flight (`runs/m30_drain_b2ga_s{1,2}.txt`).

## 2026-07-23 ~14:30 — drain forensics RESOLVED (n=120 + low-deck profile)
→ O6 `conserve` implemented

b2ga vs rocket n=120 pooled (reason-coded): 66W–54L, **true deck-outs 27/54
losses**. Per-use deck costs (engine-measured, stable across seeds):
EVOLVE Kadabra 2.0 / EVOLVE Alakazam 3.0 / Rare Candy 3.0 (the Alakazam
line's own on-evolve draws — the deck's engine, untouchable) / Dawn 2.9 /
Enriching Energy attach 3.8 / **Fezandipiti ex ability 2.8** / Dudunsparce
0.9 / Poffin 1.5 / Poké Pad 1.0. Loss anatomy: only **1/27 deck-outs was
prize-AHEAD** (most are games we're also losing on board — a conservation
rule cannot flip those), but roughly half have the OPPONENT at deck ≤4 at
the end: true mutual races where 2–3 saved cards flip the result to an
opponent deck-out win. The M29 hoarding signature reproduces (11/27 deckouts
end holding 15–23 cards — extra draw adds nothing, the hand is already
full, which is exactly the asymmetry a late-draw demotion exploits).

Low-deck choice profile (deck ≤ 8, n=60): the ONLY optional draws still
firing are **ABILITY Fezandipiti ex (17 uses)** and residual Dudunsparce at
deck 7–8 (13). Enriching Energy and Dawn almost never fire late — demoting
them would be dead code; left alone (mid-game draw is fuel, not a defect).

**O6 `conserve`** (rl/plan.py): at deck ≤ 6, demote the Fezandipiti ex (id
140) draw ability — same demotion pattern as deckguard, block generalized
to the union of active fixes' ability ids. New spec kinds `modelt-con`
(solo) + `modelt-gac` (guard+ash+conserve = revised ship candidate). Twin
synced, 640 tests green (2 new: fire/threshold/off-guard isolation +
O4×O6 composition). Screens in flight: gac drain+behavior ×2 seeds, con
drain ×1, all vs rocket.

## 2026-07-23 ~14:50 — conserve screens POSITIVE: true deck-outs 50%→37.5%,
wr held; strength gates launched

vs rocket (reason-coded, drain instrument): **gac 64W–56L pooled n=120,
true deck-outs 21/56 (37.5%)** vs b2ga's 27/54 (50.0%) — and solo `con`
replicates the direction (10/29 = 34.5%, n=60). Deck-out-share delta
z≈1.35 (point-clear, not resolved at this n; mechanism independently
confirmed: Fezandipiti VANISHED from the ≤8-deck choice list in both
seeds, was 17/60 games; Dudunsparce 0/311). Behavior instrument wr
0.550/0.667 (pooled 0.608) — strength flat-to-up, non-inferior. Telepath
66.5/69.1% ≈ b2ga's 65.4. ⚠ Honest flag: **END-with-item rose to ~36%**
(vs b2ga 29.0, B2+O1 baseline 30.7) — with Fez demoted the model ENDs
holding items; post-P3 (tempo kill: late item plays burn deck vs stall)
this is the intended conservation behavior, but it CONTRADICTS the literal
pre-registered "END-item strictly below baseline" claim axis. Goes to
Piotr as a flagged axis-interpretation, not silently re-scored.

Strength gates for the changed composition launched (~2.5h): `gac` added
to battery/decide scripts (same rule-arm non-inferiority bar), floors
(drag 2×200, grim 2×200, rocket 2×200, kyo 200) then mirror 4×200 →
`runs/m30_gac_*.jsonl`, log `runs/m30_p5_gac_gates.log`, heartbeat on.

## 2026-07-23 ~15:05 — gac CLEARS ITS BAR; rebuilt + QC'd; STOPPED at the
human gate (round 2)

Piotr QC-round-2 review resolved via replay deck-race forensics (QC games
won with 6–8 card margins ≈ exactly the low-deck draw spend the rules
eliminate; opponent-decks-first is the win condition of the matchup, 75–90%
of all wins vs rocket both before and after the rules). His call:
**ship and watch the live A/B.**

Decide script on the full gac battery: **mirror 0.6750 pooled n=800
(z=+0.17 vs 0.671) ✓ · dragapult 0.3750 n=400 (z=+1.11, above pin) ✓ ·
kyogre 0.9750 ✓ — CLEARS.** Advisories: grim 0.6687 (z=−0.57, better than
b2ga's 0.6488); **rocket 0.5675 n=400 z=+2.11 — resolved-better than the
live pin**, the axis this whole revision targeted. Full ritual repeated:
ship default → `"telepath,deckguard,ash,conserve"`, 640 tests green,
rebuild → `dist/submission_neural_20260723_150518.tar.gz`, deck md5
8e8cf124 ✓, 21/21 weight arrays = m28_winners.pt ✓, direct-import assert
of the 4-fix default ✓. QC round 2: 3 games of the actual bundle vs the
rocket clone = **3W–0L**, `replays/m30_qc_gac_000..002.html`. STOPPED —
awaiting Piotr's replay review and explicit go (sign-off covers the
conserve rule + new default string + the END-item axis interpretation).

## 2026-07-23 ~15:25 — M30 SHIPPED: sub 54929991 (Piotr's explicit go)

`kaggle competitions submit` → **54929991**, PENDING, verified in the
submissions list (`submission_neural_20260723_150518.tar.gz`, the exact
tarball of the P5.1 ritual). `model_monitor.ipynb` MODELS updated.

Live A/B watch-list (vs 54903635 @735 and the retiring 54914673 @665,
same deck everywhere; weights + rule set both change vs 54903635, rule set
only vs a hypothetical b2ga — the clean single-variable comparisons are
gone, this is a package bet): score trajectory vs 720–735; deck-out loss
share (M29 was 30%); rocket/stall-family record (we were 0–3); low-deck
behavior in replays (no Fez/Dudunsparce draws at deck≤6, Sacred Ash before
empty); telepath attach (offline point-read −5pp — if live replays confirm
a real drop, the conserve/guard interaction needs a look); mirror family
record (offline says parity).
