# M38 diary — corrected-teacher retrain

Plan: [M38-plan.md](M38-plan.md) (refined v1, 2026-07-31). Incremental
observations only — entries are appended AT THE MOMENT they are produced
(gate results incl. kills, collection stats, train metrics, anomalies).

**Epoch note (R3):** all solver-bed numbers before 2026-07-31 are pre-fix era
and not comparable to anything in this file. Never quote pre-fix numbers next
to post-fix ones here.

## 2026-07-31 — Phase 0 implementation

- Branch `feature/m38` off `main` (0f21d9b, the merged M37 audit).
- Beds inventory (local Windows box): `checkpoints/m25_bc_grim_54861775.pt`,
  `checkpoints/m30_bc_rocket_54834745.pt`, `checkpoints/m28_winners.pt`,
  `data/kaggle/{grimmsnarl_3121746f,rocket_3394cd30}_deck.csv` and all three
  sample agents are PRESENT — the full non-solver E0a bed set can run here
  (the Linux-box-only concern applied to ship tarballs + rocket/grim clone
  *training* artifacts; the eval ckpts are in the repo).
- **Arms landed** (`rl/turn_solver.py`): `M38_OLD_LEAF=1` vendors the
  pre-audit inverted prize terms only (leaf-only, per plan — T4 fix and
  docstrings stay); `M38_BAR=current|const|semantic` selects the G1 bar via
  the new `override_cleared()`, which `plan_iter._cleared` now shares (the
  corpus label gate and the live override can no longer diverge). Semantic
  fact `prize_or_win` threaded through `_dfs`/`solve_turn_line` as a 4th
  return element. A2 constant pre-registered at `W_PRIZE // 2 = 50_000`
  (clears any 1-prize line under worst-case riders; declines net-losing
  1-for-1 trades at −50k and the ~7k no-prize maximum).
- Suite: 662 passed; 4 failures PRE-EXIST on clean main (3× test_meta_eval
  frozen-weights drift, 1× test_network Windows tempfile PermissionError) —
  not from this change.
- **A0 live verification** (prize_semantics_probe, real engine):
  `M38_OLD_LEAF=1` → leaf check FAIL with **2652/2652 prize leaves negative**
  (the historical defect reproduced bit-for-bit in sign); flags off → PASS
  **1775/1775 positive**. The control arm is faithful.
- **Battery infrastructure**: `scripts/m38_battery.py` (E0a cells with
  run_pairs resume checkpoints + E0b walk-in probe + decode) and
  `scripts/m38_label_shift.py` (E0c collect-per-arm + diff). Smokes green.
  Engine throughput surprise: solver-vs-rule games run at ~5/s at workers=8
  (real 52–66-move games, verified via --diag) — the full 4×7×800 E0a
  battery is sub-hour, not overnight.
- Anecdotal pre-battery tell (n=4 diag, NOT evidence): A1 vs tuned swept
  0-4 with prizes-left 6/0 — the arm never took a prize. The prize-phobic
  signature the battery is built to measure.
- **HERMES UNAVAILABLE on this box**: `hermes` CLI is not on PATH (checked
  bash + PowerShell + common locations). Battery/pipeline Telegram
  notifications are being SKIPPED (graceful fallback prints to log).
  Flagged to Piotr — needs install/PATH fix or an alternative gateway.
- **E0a battery LAUNCHED** (background, `runs/m38_battery.log`,
  checkpointed per cell in `runs/m38_{arm}_{bed}_s0.jsonl`): 4 arms × 7
  non-solver beds × n=800, workers=8, then E0b probes n=100/arm vs tuned.
  First cell: **A0 vs tuned wr 0.2550 n=800**.

## 2026-07-31 — E0a decoded (battery completed ~11:5x, ~25 min wall)

Full table in `scripts/m38_battery.py decode`; n=800/cell everywhere.

| bed | A0 | A1 | A2 | A3 | z vs A0 (A1/A2/A3) |
|---|---|---|---|---|---|
| tuned | .2550 | .2775 | .2675 | .2913 | +1.02 / +0.57 / +1.63 |
| iono | .0375 | .0587 | .0500 | .0500 | +1.99 / +1.22 / +1.22 |
| dragapult | .0413 | **.0825** | .0663 | .0563 | **+3.44** / +2.22 / +1.39 |
| grim | .0638 | .0731 | .0762 | .0731 | +0.74 / +0.98 / +0.74 |
| rocket | .1988 | .1775 | **.1475** | .2000 | −1.09 / **−2.72** / +0.06 |
| m28 | .0600 | .0688 | .0775 | .0725 | +0.71 / +1.38 / +1.01 |
| mirror | .0481 | .0581 | .0606 | .0612 | +0.89 / +1.10 / +1.15 |

Pooled (n=5600/arm): A0 .1006 | A1 .1138 (**z≈+2.3**) | A2 .1065 (z≈+1.0)
| A3 .1149 (**z≈+2.4**).

Readings (E0a is the WEAK evidence, per plan):
- **Kill gate, E0a half: NOT killed** — A1 and A3 beat A0 pooled at ~2.3-2.4
  SE, positive direction on 6/7 beds.
- The leaf fix ALONE (A1, bar unchanged) carries most of the pilot-strength
  gain — biggest single cell A1 vs dragapult +4.1pp (z +3.44). The bar arms
  do not clearly beat A1 *as pilots*; the bar's real job is label density
  (E0c pending). Consistent with the audit: overrides are rare enough that
  the bar barely moves live play, but gates the corpus.
- **Rocket anomaly**: A2 (lowered constant) REGRESSES vs rocket by −5.1pp
  (z −2.72) while A3 is flat — first hint of the E0b over-greed concern:
  a pure-constant unlock may fire net-bad prize lines into an aggressive
  deck. Watch A2's E0b prize walk-in rate before picking it.
- Absolute levels are low (pooled ~0.10-0.11) — the solver-as-pilot is weak
  vs these beds; fine, arms are compared to A0, not to the ceiling.

**E0b v1 INVALIDATED + fixed**: v1 printed prize-turns 0/600 for every arm —
impossible next to the wr numbers. Root cause found by tracing: the probe's
`record_turn_end` bailed when the opponent's active was None, and after a KO
the opp active slot is exactly None (awaiting promote) — every prize-taking
turn-end was excluded BY CONSTRUCTION. Fixed (count turn-end + prize fact
first, defer exposure to the first post-promote prompt with both actives);
rerunning n=100/arm. Side finding while tracing: some arm wins vs tuned are
DECK-OUT wins with the arm's prize array untouched at 6 — the M37 dig/deck-out
style, visible offline. (Also: a first debug script briefly implicated the
tuned teacher mutating obs — WRONG, retracted; the anomaly was the debug
script's own prev-init bug counting the setup phase as a 6-prize drain.
Engine arrays verified clean and seat-consistent vs both rule and generic
beds.)

## 2026-07-31 — E0b v2 (fixed probe), n=100/arm vs tuned

| arm | turn ends | walk-in (now/attach) | prize turns | prize-turn walk-in (now/attach, measured) |
|---|---|---|---|---|
| A0 | 800 | .448 / .534 | 44 | .500 / .722 (36) |
| A1 | 746 | .385 / .463 | 52 | .415 / .463 (41) |
| A2 | 715 | .396 / .470 | 50 | .304 / .457 (46) |
| A3 | 719 | .426 / .522 | 42 | .459 / .622 (37) |

- **NO over-greed walk-in excess** in any corrected arm vs A0 — if anything
  lower (prize-turn cells are n≈40, SE ~0.08: treat deltas as noise; the
  conclusion is the absence of an INCREASE). Pre-registered consequence:
  **no W_COUNTER retune this milestone** — principled 2-ply prize-exchange
  term stays in BACKLOG.md, ship diff stays minimal.
- Note A0 also takes prizes (44 prize turns): the inverted leaf gagged the
  SOLVER override, but the greedy pilot still attacks into available KOs —
  A0's damage is concentrated in the labels/planning, not in raw attacking.
  Exactly the H1 shape (teacher-label defect, not gross play defect).
- Tuned bed does not explain the A2 rocket regression (A2 has the LOWEST
  prize walk-in here) — running E0b vs rocket for A0/A2/A3 to check
  bed-specific over-greed before any G1 call.
- **E0b vs rocket (n=100 each)**: walk-in now A0 .077 / A2 .068 / A3 .071;
  prize-turn walk-in .038 / .000 / .020 (n≈52-60 measured). No over-greed
  mechanism on this bed either — and A2's n=100 wr here (0.180) sits within
  ~1 SE of its E0a 0.1475. Verdict: the A2 rocket cell (z −2.72) is
  UNEXPLAINED and plausibly a multiple-comparisons artifact (21 arm-vs-A0
  tests); logged as a watch item, not treated as an over-greed confirmation.
  The E0b conclusion (no W_COUNTER retune) stands unchanged.

## 2026-07-31 — E0c label shift (n=200 games/arm, value_solve OFF) + VERDICT

| metric | A0 | A1 | A2 | A3 |
|---|---|---|---|---|
| labels/game | 64.7 | 59.1 | 60.1 | 63.2 |
| **kill-plan commit rate** | **0.070** | **0.270** | **0.277** | **0.269** |
| plan-null rate | 0.938 | 0.773 | 0.755 | 0.757 |
| derails/game | 0.13 | 0.30 | 0.47 | 0.34 |
| attack-when-available | 0.502 | 0.453 | 0.445 | 0.456 |
| end-when-attack-avail | 0.002 | 0.004 | 0.003 | 0.004 |
| supporter-play rate | 0.203 | 0.257 | 0.253 | 0.241 |

- **THE headline: kill-plan commit rate ×3.9** (0.070 → ~0.27) from the leaf
  fix alone. The corpus the old teacher produced carried committed kill-plans
  on 7% of plan rows; the corrected teacher commits 27%. This is the label
  shift H1 needs — LARGE, not negligible.
- **The bar arms add ~nothing to label density on top of the corrected leaf**
  (A1 0.270 / A2 0.277 / A3 0.269): in the WIDENED corpus solver the
  riders-under-bar gap (P0b) is rare — the audit's 6/7 gagged overrides was
  live-budget solve_turn on n=18 prompts, not the corpus regime. No M8.1
  flood trap either (labels/game did not inflate on permissive bars).
- attack-when-available went DOWN (0.502 → ~0.45), against the naive H1
  arrow. Interpretation (recorded before anyone overfits to it): committed
  multi-action prize lines spend prompts on combo assembly (item → attach →
  attack) while an attack sits on the menu — the solver's entire point.
  Attack-per-PROMPT falls while prize conversion rises. The pre-registered
  H1 behavioral predictions proper (sweep share, race conversion,
  prizes/game, attack-when-LETHAL) are measured on the retrained policy's
  gate replays via pm_extra, not on this proxy.
- supporter-play rate UP 0.203 → ~0.25 (the chronic under-play line moves
  the right way); labels/game slightly down = shorter teacher games,
  consistent with actually closing.
- derails/game up mechanically (more committed plans to derail from);
  derail-per-commit roughly flat.

### Joint kill gate (pre-registered): **PASSED — H1 ALIVE, decisively**

E0a: A1/A3 beat A0 pooled (z≈+2.3/+2.4). E0c: label shift ×3.9. Neither
kill condition holds. Proceed to Phase 1 (G2 re-pins with the G1 winner,
then G3 corpus + retrain arms).

### G1 recommendation (Claude, pending Piotr's call): **A3 semantic gate**

Grounds: (1) E0a-strongest pooled (.1149, z+2.4 vs A0), no negative bed;
(2) label density identical to the alternatives — no flood risk; (3) the
design rationale stands independent of these numbers: the gate's semantics
survive every future W-vector retune, which is the exact mechanism by which
P0b arose; (4) A2's only distinguishing result is the unexplained negative
rocket cell. Counter-candidate A1 (keep the constant bar) is defensible on
minimal-diff grounds — but G1 was pre-registered as this milestone's goal
and the audit called P0 "only half-landed without it".

## 2026-07-31 — G1 LANDED (Piotr's call: A3) + G2 fresh pins

- `M38_BAR` default flipped to `semantic`; pinning tests updated
  deliberately; suite 656 passed. MILESTONES.md epoch marker in (R3).
- **G2 re-pins (2-seed pooled, n=400×2, post-fix semantic-bar solver):**

| pin | seeds | pooled | old-era number (NOT comparable — context only) |
|---|---|---|---|
| champion (m28_winners+gacfr3) vs solver:lucario | .860 / .818 | **.839** | campaign bar "0.55" era |
| plain m28_winners (no rules) vs solver:lucario | .853 / .835 | **.844** | — (G5 no-rules baseline, pinned same day) |
| champion vs grim BC clone | .730 / .681 | **.706** | 0.650 (M26 era) |
| champion vs rocket BC clone | .605 / .585 | **.595** | 0.610 (M30 era) |

- The corrected+semantic `solver:lucario` bed is much softer as an OPPONENT
  than the pre-fix era implied (champion .839) — the "0.55 campaign bar" is
  dead as an absolute number. **Restated campaign context: retrain arms are
  judged against the champion's fresh pins on the SAME beds, primary
  non-solver** (plan Phase 1 rule), solver bed diagnostic only.
- gacfr3 rules add nothing vs solver:lucario (.839 vs plain .844, within
  noise) — first quantified hint for G5's no-rules arm; ship-relevant only
  if G5 shows active harm (decision 3).
- Clone-bed drift since their era pins: grim +5.6pp, rocket −1.5pp — the
  drift law justified itself.

## 2026-07-31 — G3 gen-1 clean collect LAUNCHED

Config committed as `scripts/m38_collect.sh` + `data/m38_population.json`
(teacher seat = alakazam_v2_h4 only; live 700–800 mix as the opponents
rotation: tuned×13, wall-solver×9, self×8, archaludon-solver×8,
grim-clone×8, dragapult×4, rocket-clone×4; value_solve OFF; two fresh
dirs `data/m38_gen1_{a,b}`, 2000 games each, seeds 11/12, workers 8).
Ride-along fix: plan_iter opponent instances get a per-spec suffix (rule
teachers need unique instances; the mixed rotation holds 2 rule specs).

## 2026-07-31 — gen-1 corpus DONE; retrain arms launched

- **Corpus**: 4,000 games → **337,698 labels** (168,460 + 169,238), 34,521
  plan rows, **9,777 kill-plans (28.4% commit rate — the corrected-teacher
  rate, matching E0c)**, plan coverage 0.996, SETUP 0 (value_solve off ✓),
  48 shards. ~26× the champion-corpus floor (~13k rows).
- **Plan-null 0.97 on this corpus vs 0.76 in E0c** — the alakazam-seat
  kill lines mostly derive NULL plans (plan_gust 0.00 too): the derived-plan
  channel is nearly inert for this deck's lines. Noted, not blocking: the
  invariant caveat is pre-existing (committed line + null-derived plan),
  and the champion lineage trained with mostly-null plans as well. Watch
  plan_acc in training; a plan-channel rework goes to BACKLOG if the arms
  underperform.
- Teacher-seat WR in collect: 0.24 vs clones (model), 0.27–0.30 vs rule,
  0.42–0.43 vs solver walls, 0.51–0.52 mirror. The raw teacher is weaker
  than the live-field clones — expected; the student distills labels, not
  the teacher's own wr (E0c ranking rule).
- **Retrain arms launched** (`scripts/m38_train.sh`, background, CPU):
  `m38_ft` = fine-tune from migrate_v3_to_v4(m28_winners) (M21 warm-start
  invariant; champion recipe epochs 10 / lr 1e-4 / SUPPORTER:5 STADIUM:5),
  then `m38_scratch` = fresh v4 net, lr 3e-4, same data+weights.

## 2026-07-31 — retrain arms trained; gate battery launched

- **Latent bug found by the scratch arm** (first-ever fresh train on v4
  expert shards): plan_iter's width-driven fresh path inferred ids/options
  from the data but NOT `extra_dim` → net built 341 columns too narrow,
  mat1/mat2 crash. Fixed + regression test (`test_train_fresh_infers_v4_
  extra_dim`); 26/26 plan_iter tests green. `m38_ft` was unaffected.
- Train (304,783 train / 32,915 val rows, same split → val_acc comparable
  BETWEEN these two arms only):
  - `m38_ft`: init val_acc 0.605 (the untouched champion vs the corrected
    labels — real headroom), best **0.778** (epoch 8).
  - `m38_scratch`: best **0.787** (epoch 9), still rising at cutoff.
  - plan_acc ~0.99 both — near-ceiling because plans are 97% null on this
    corpus (see plan-null note above); not evidential.
- Scratch edging ft on val_acc does NOT overturn the pre-registered
  fine-tune-favored prior — val_acc ≠ pilot strength (campaign lesson,
  many times). The gate battery decides.
- **Gate battery launched** (`scripts/m38_gate.sh`): ft / scratch /
  control (plain m28_winners) × 7 non-solver beds + solver:lucario
  diagnostic, n=400×2 seeds = 800/cell.

## 2026-07-31 — GATE BATTERY: RETRAIN ARMS COLLAPSED (experiment failure)

| bed | ft | scratch | control (plain champion) |
|---|---|---|---|
| tuned | .3237 | .2900 | .6925 |
| iono | .0525 | .0362 | .2988 |
| dragapult | .0675 | .0737 | .2800 |
| grim | .1206 | .1319 | .6887 |
| rocket | .2150 | .1375 | .6162 |
| m28 | .0831 | .0700 | .5250 |
| mirror | .0694 | .0462 | .5181 |
| lucsolver (diag) | .1062 | .1113 | .8309 |

Pooled non-solver: **ft .1437 / scratch .1232 / control .5169** (n=4800
each; every z vs control between −11 and −28). Not a miss — a collapse.

**Reading (before any spin):** the retrained nets pool at almost exactly
the corrected TEACHER-as-pilot's E0a level (~0.10–0.14). They are faithful
clones of the teacher (val_acc 0.78) — and the teacher, corrected or not,
is a far weaker pilot than the champion. The error in the retrain design:
**the champion does not descend from the solver-teacher corpus.**
m28_winners was trained on KAGGLE WINNER REPLAYS (external strong
demonstrators, untouched by our solver inversion), warm-started from
m27_both. Regenerating the *solver-teacher* corpus and BC-ing on it
reproduces the known pre-M28 plateau, with corrected labels. The fine-tune
arm's "localized prior overwrite" assumption also failed as executed: 10
epochs × 305k weak-teacher rows at lr 1e-4 overwrote the champion's
kaggle-winner behavior wholesale (init 0.605 → final 0.778 agreement with
the weak teacher = the champion's stronger play trained OUT).

**What survives intact:** the teacher fix itself (audit + E0a + E0b + E0c
all confirm it), the semantic bar, the fresh pins, the clean corpus as an
artifact, and H1's *mechanism* claim about labels. What failed is the
*delivery vehicle*: pure BC on the corrected teacher cannot beat a
champion built from stronger demonstrations.

**STOPPED per standing rule — prompting Piotr with options.** AWR arm NOT
launched (same weak-demonstrator problem, pointless until the vehicle
question is settled).

## 2026-07-31 — surgical fine-tune (Piotr's pivot): ALSO KILLED

Design (pre-registered): filter the clean corpus to prize-relevant rows
only — chosen-ATTACK + chosen-supporter-PLAY, the two label classes E0c
showed shifting (38,194 rows, 11.3%; commit provenance unrecoverable at
plan-null 0.97, so label-class is the filter) — then fine-tune the migrated
champion at a small dose (2 epochs, lr 2e-5, no kind weights: the filter IS
the weighting). `scripts/m38_surgical.py`, ckpt `m38_ft2`.

- **Confirming detail**: init val_acc on prize-relevant rows is **0.451**
  (vs 0.605 corpus-wide) — the champion-vs-corrected-teacher disagreement
  is concentrated exactly where H1 located it. Dose moved it to 0.667.
- **Kill gate (n=400/bed, control = same-battery plain champion):**
  tuned .260 vs .693 | grim .340 vs .689 | rocket .307 vs .616 |
  m28 .147 vs .525 → pooled **.264 vs .631. KILLED.**

**Conclusion for the vehicle question:** the corrected solver teacher
cannot improve the champion BY IMITATION at any dose tried — its
prize-relevant labels, though sign-corrected, are still weaker than the
champion's kaggle-winner-learned behavior (0.4s-budget solver lines vs
800+-band winners' choices). The localized-prior claim was right about the
LOCATION (0.451) and wrong about the DIRECTION of the overwrite's value.
The teacher fix's payoff is real but lives in the instruments (corrected
beds/labels/probes) and in future corpora from stronger demonstrators —
not in distilling this teacher.

## 2026-07-31 — pivot (Piotr): both lanes in sequence, kaggle auth sorted

Piotr's call: fallback lane (G4 wall clone + rules) AND the kaggle winners
harvest; auth confirmed working (live leaderboard pull, top-15 teams
1115–1166). Stale-parquet recon: wall = `crustle+great_tusk` (top sub
54377429 @ 771, deck_hash d3e4d16c…); top of meta = `alakazam+kadabra` @
**903** — the champion's own family, so the winners corpus is the M28
same-deck-winners recipe at a higher band.

**Harvest round 1 (500 episodes, all clean)**: episodes.parquet 5,945 rows,
opp_decks 1,596 seats. Team-id listing 400s (leaderboard ids ≠ episode
service) — sub-id snowball works. Yield: **101 alakazam winner seats @
800+** (hash 6934a9f4: 66 seats, max 947; hash 9294d9d8 — the M28 champion
corpus hash — 22 seats, max 977; scattered others incl. a 987). Wall: 69
seats, dominant hash d3e4d16c (54 from 54377429; an 830-scoring wall sub
55075280 found). Provisional wall corpus at default min-score: 65 seats /
3,612 decisions (48 low-score wall seats skipped) — rebuilding after
round 2 with --min-score 600. **Round 2 launched**: targets the 900+
alakazam subs + the 830 wall sub, max 500 more episodes.

## 2026-07-31 — round 2 yield, corpora + nets built, gates running

- **Round 2 (500 more episodes)**: 9294d9d8 (the M28 champion-corpus hash)
  now **231 winner seats @800+, max 1058** — the strict M28 recipe with
  more games than the original at a far higher band. Wall 600+: 144 seats.
- Corpora: `bc_m38_w9294` 231 seats / 16,082 decisions (winners-only,
  800+, hand-aware); `bc_m38_wall` 138 seats / 7,321 decisions (600+).
  `decks/greattusk_wall.csv` verified byte-identical to harvested hash
  d3e4d16c — bed spec is `model:checkpoints/m38_bc_wall.pt:greattusk_wall`.
- NOTE: `m27_both.pt` absent on this box → the exact-M28-init arm can't
  run here; arms = `m38_w9294_cont` (init m28_winners, champion recipe:
  10 ep, lr 1e-4, SUPPORTER:5 STADIUM:5) and `m38_w9294_fresh` (no init,
  lr 3e-4).
- Train: w9294_cont init 0.631 → best 0.662 (gentle move TOWARD stronger
  demonstrators — the mirror image of the failed teacher retrain);
  w9294_fresh 0.591; **wall clone val 0.676** (grim clone era was 0.565 —
  strong fit). plan_acc 0.000 everywhere: replay corpora carry no plan
  rows, as with the champion's own corpus.
- Gates launched: 4-bed kill-gate (vs the same-battery control cells) for
  both winner arms + the G4 wall-bed champion pin (n=400×2).

## 2026-07-31 — G4 WALL BED DELIVERED; winner-arm first pass

- **Wall pin (headline): champion vs `m38_bc_wall` = .318/.345, pooled
  .332 (n=800).** The champion LOSES 2:1 to a faithful wall clone — the
  post-mortem's wall loss-mode confirmed on a real bed for the first time
  (solver:wall was strawman-invalid twice; this bed is BC-cloned from 138
  live 600+ wall seats, val 0.676). The rules lane now has its instrument
  and its baseline.
- 4-bed gates vs same-battery control (control pooled .631):
  - `m38_w9294_fresh` pooled **.447 — KILLED** (16k rows can't rebuild
    from scratch, as expected).
  - `m38_w9294_cont` pooled **.603 — mixed**: m28-lineage +3.2pp, grim
    +1.1pp, but tuned −7.0pp and rocket −8.4pp (z≈−2). The overwrite-dose
    pattern again, milder — 10 epochs on 16k high-band rows still trades
    away tuned/rocket strength. NOT shippable as-is; dose iteration next
    (3-epoch variant), since best-val selection optimizes agreement with
    the new corpus, not strength.

## 2026-07-31 — cont3 (low dose): FIRST POOLED-POSITIVE ARM

`m38_w9294_cont3` = champion + 3 epochs on the 800–1058-band same-deck
winners (best-val ckpt = epoch 1, val 0.664). 5-bed gate, n=400, seed 1:

| bed | cont3 | control | Δ |
|---|---|---|---|
| tuned | .667 | .6925 | −2.5pp (ns) |
| grim | .741 | .6887 | **+5.2pp** |
| rocket | .585 | .6162 | −3.1pp (ns) |
| m28 | .610 | .5250 | **+8.5pp (z≈+2.4)** |
| wall | .335 | (champ .332) | flat |

Pooled shared-4-beds: **.651 vs control .631 (+2.0pp)**. Dose law
confirmed in both directions on the same day: 10 epochs regresses, ~1
epoch gains. Wall cell flat — the winners corpus does NOT fix walls; the
wall answer stays with the rules lane (the plan's two-lane claim, now
measured). Full 8-bed × 2-seed confirmation battery launched.

## 2026-07-31 — cont3 CONFIRMED at full scale (n=800/cell, 2-seed)

| bed | cont3 | control | z |
|---|---|---|---|
| tuned | .6875 | .6925 | −0.22 |
| iono | **.4375** | .2988 | **+5.81** |
| dragapult | **.4512** | .2800 | **+7.23** |
| grim | .7312 | .6887 | +1.88 |
| rocket | .5563 | .6162 | **−2.44** ⚠ |
| m28 | .6044 | .5250 | +3.21 |
| mirror | .5787 | .5181 | +2.44 |
| wall | .3200 | (champ .332) | flat |
| lucsolver (diag) | .8538 | .8219 | +1.73 |

**Pooled 6 shared non-solver beds: .5780 vs .5169, +6.1pp, z=+6.03.**
The iono/dragapult gains (+14/+17pp) are the loss-mass beds the milestone
targeted. Rocket −6.0pp is the single regression (live share 4/63) —
logged as the ship watch item. Wall untouched, as measured before —
rules-lane territory. Candidate: `m38_w9294_cont3` (epoch-1 ckpt,
800–1058-band same-deck winners on the champion). Next: G5 mini-matrix
(gacfr3 on/off on the candidate), then export + QC + Piotr review.

## 2026-07-31 — G5 on the candidate: the rule stack hurts the wall, helps nothing measurably

Mini-matrix (n=400/cell, seed 3) + isolation cells:

| config | wall | mirror | tuned | rocket |
|---|---|---|---|---|
| cont3 plain | .330 (pooled .323, n=1200) | .515 | .680 | .522 |
| cont3 + gacfr3 | .235 / .273 (pooled .254, n=800) | .542 | .662 | .557 |
| cont3 + gacf (no racemode3) | .278 (n=400) | — | — | — |

- **Stack harm on the wall bed is real and NOT racemode3 alone**: plain
  .323 > gacf .278 (z≈−1.7) > gacfr3 .254. The base gac(f) economy rules
  (tuned on the old net's behavior) cost ~5pp vs the wall; racemode3 adds
  ~2 more. Meanwhile no rule shows a significant gain anywhere measured
  (mirror +2.7 ns, rocket +3.5 ns, tuned −1.8 ns).
- Decision-3 trigger ("strip only if actively harmful") is MET for the
  stack-as-a-whole on the wall bed; per-rule attribution incomplete
  beyond racemode3. Ship-config decision goes to Piotr.
- Reminder for the ship prep, whichever config: the `rl/plan.py`
  wins/concedes fix (P2) + submission twin rides along with a net-change
  ship per the plan — apply at export time; `prize_semantics_probe` runs
  as the standing pre-ship regression; deck choice confirmed with Piotr;
  monitor row on submit.
