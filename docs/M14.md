# M14 diary — setup plans + mixed opponents (spec: [M14-plan.md](M14-plan.md))

User directives: full pipeline in one pass; multiple opponents; ship
regardless of win-rate gates (replay observation is the evaluation);
technical gates still enforced.

## 2026-07-17 (eve) — full pipeline run + SHIPPED

- Collector extended: `--opponents` rotation (self / buddy `ext:` / rule
  expert; teacher seat only recorded vs externals) + `--value-ckpt`
  setup-plan commits (margin 200, turn<32). Smoke: SETUP commits 23 vs kill
  2 on 42 turn-starts — the value-teacher actively commits development lines.
- **Known gap (logged, deliberate):** most setup lines contain no attack, so
  the attack-shaped plan VOCABULARY maps them to null — the battlefield
  understanding lands in the POLICY labels this round; extending the Plan
  tuple with setup descriptors (develop/attach targets) is the follow-up
  architecture item.
- Collection incident: one worker hung ~2.5h in a never-terminating external
  matchup (solver mirrors always terminate; buddy/rule games don't
  necessarily) — killed by exact PID, 730/800 games kept, **runaway-game cap
  added** (600 prompts → draw).
- Train `osv3_plan2` (plan_m14 + plan_ei0b + bc_v2b, init plan0c, 5ep 1e-4):
  val_acc 0.896, plan_acc 0.932.
- Ship (user-directed, win-rate gate waived; technical gates green: parity,
  isolation, gate game): **Kaggle submission 54790886**. Monitor MODELS
  updated.
- Leaderboard context landed with the ship listing: **osv3_plan0c settled at
  441.0** vs rules bundle 548.6 — the observation-run dip, now quantified.
- **For-the-record measurements** (`runs/m14_record_s1.jsonl`): vs
  `solver:lucario` **0.383** (below plan0c's 0.415 — the setup-plan labels +
  mixed-opponent data cost mirror strength); meta co-gate **0.403**
  (≈ plan0c's 0.406). Consistent with expectations: the mirror was never
  M14's target; the live replays and non-mirror play are.

**Observation round watch-list (next session):** (1) does the agent DEVELOP
differently on non-lethal turns — energy routing, Lunatone-before-Solrock
ordering, bench discipline; (2) non-mirror matchup quality vs plan0c's
0.63–0.77; (3) whether setup-plan-labeled behavior survives distillation
into live play at all (the vocabulary gap predicts partial). Expect the
leaderboard score to settle at-or-below plan0c's 441 — accepted cost of the
observation run.
