# M13 diary — outcome-grounded setup value (spec: [M13-plan.md](M13-plan.md))

Bar: ≥0.55 vs `solver:lucario` pooled n≥1200/2 seeds + meta co-gate; ship at
≥0.53 pooled (standing policy). Entry discipline: every run logged with
command + `runs/*.jsonl` + verdict + **Observations**; kills documented like
promotions.

## 2026-07-17 — SPEC'D, not started

Spec written after the live-observation round on submission 54779834 (see
docs/M11.md) and the user's directive: the model should LEARN this deck's
combos and card usage from playing many games. Design triangulated from the
campaign's measured dead ends — the one live route is outcome-grounded setup
value (pairwise-ranked, M12's proven loss) replacing `score_leaf`'s non-lethal
tail inside the exact turn search, then expert iteration on that broad
operator (M11 infra).

## 2026-07-17 — Rung 0: GO — Gate V0 passes at 0.704

- 0a CONDITIONAL_ATTACKS landed (rl/tcg twins, board_ids threading through
  attach values + plan enumeration; 442 tests green).
- 0b collection: 2,000 games (~20 min, 12 workers), **27,621 setup states**
  (first-own-MAIN per turn) → `data/setupval`.
- 0c matched-pair training (warm trunk from osv3_plan0c, 5,830 train /
  436 val pairs): **held-out matched-pair acc 0.704 ≥ 0.62 GO** (best at
  epoch 0 — saturates fast on this pair volume).

**Observations:** (1) setup-phase buckets t4-15 rank at 0.66-0.77 — the model
reads WHICH boards win with turn+prizes matched away; this is the setup
knowledge score_leaf lacks. (2) Late-midgame buckets t16-23 are weakest
(0.44-0.59, small n) — close races decided by draws; more data may help.
(3) Epoch-0 saturation ⇒ pair volume is the binding constraint — the 10k+
game batch (Piotr sign-off) is the obvious scaling lever before Rung 2.
(4) Live context at start: 54779834 at 26 eps, 11W (0.423 live ≈ the 0.415
mirror measurement).

## 2026-07-17 — Rung 1 v1: Gate S1 KILLED at screen (0.458)

- Implementation: `leaf_value` threaded through score_leaf/_dfs/solve_turn
  (terminal + prize + benchless anchors exact; heuristic tail replaced);
  `vsolver:` pilot (lethal tier untouched; dev-mode solve with the learned
  leaf on every own MAIN; DEV_OVERRIDE_MARGIN unchanged; turn-passed leaves
  perspective-negated — exact in the mirror). 444 tests green.
- Gate S1 screen `runs/m13_s1_screen.jsonl`: **183W-217L 0.458 < 0.53 go /
  ≤0.50 kill — KILLED at v1.** (Still the best non-lethal-override result
  ever: M8.1's heuristic dev-tier scored 0.314 on this protocol.)

**Observations / diagnosis:** the 0.704-pair-acc value is real but NOISY
(trained on 5.8k pairs, saturated at epoch 0), and v1 lets it override greedy
whenever a 0.2s solve beats stand-pat by DEV_OVERRIDE_MARGIN=900 ≈ 0.3 tanh
units — noise clears that constantly, so weak overrides fire everywhere
(the M8.1 over-fire shape, now with a learned signal). Two pre-registered
levers, in order:
1. **Pair volume ×5** (10k-game batch ≈ 1.7h collection — >2h total with
   training: needs Piotr sign-off) — V0's own observation says data is the
   binding constraint; a sharper value may clear the same margin honestly.
2. **Margin recalibration** (one principled value: set the override margin to
   the value-model's observed noise floor — e.g. the 80th percentile of
   |V| gaps between sibling stand-pat leaves on held-out states — instead of
   the heuristic-era 900). One iteration, no sweep.

Status: M13 paused at Rung 1 pending the sign-off/lever decision.

## 2026-07-17/18 — Rung 1 v2: the 10k batch + calibrated margin

- User collected the 10k batch themselves via `m13_collect.sh` (laptop
  profile, chunked with Ctrl-C/rerun; `--target 10000` stopped exactly at
  goal): **10,000 games, 138,247 setup states, 86 shards.**
- Train `osv3_setupval2` (same recipe, 29,760/2,787 pairs): **held-out
  matched-pair acc 0.704 → 0.768** — the data-scaling hypothesis CONFIRMED;
  the pre-registered architecture trigger did NOT fire (MLP is not the
  ceiling yet). Setup-phase buckets t4-15 now 0.79-0.80. Weak zone: deep
  grind t32-47 (0.46-0.66, small n) — long games stay hard to read.
- **Margin calibration** (confidence-accuracy curve on held-out pairs):
  margin 200 = smallest LAMBDA·|ΔV| gap with ordering acc ≥0.80 (0.804 at
  coverage 0.84). vsolver now overrides at 200, not the heuristic-era 900.
  `dev_margin` threaded through solve_turn (default = legacy constant).
- **S1 v2 re-gate: 0.484** (193W-206L-1D, n=400 seed 1,
  `runs/m13_s1v2_screen.jsonl`) — misses the 0.53 go-gate; ≤0.50 ⇒ killed at
  the pre-registered rule. Trajectory: 0.458 (v1) → 0.484 (v2) — both levers
  moved it the predicted direction, but not past break-even.

## 2026-07-18 — S1 v3 (turn-gated): 0.453 — RUNG 1 CLOSED, M13 CLOSED

`runs/m13_s1v3_screen.jsonl`: 181W-219L **0.453**. Three variants — v1 0.458,
v2 0.484, v3 0.453 — all below the 0.500 null. Killed for good.

**The campaign law this completes (5 measurements, 3 milestones):** every
attempt to override the greedy pilot's non-lethal play loses in the mirror —
heuristic dev-search (M8.1: 0.314), distilled ranker override (M12: 0.383),
outcome-value search ×3 (0.458/0.484/0.453). The greedy tier system is
locally optimal against itself: an override must beat it by more than the
sequencing/tempo tax it pays, and no signal we can build — including an
honestly-validated 0.768 outcome value — clears that bar at the margin.
**Add to the dead-end list: override-style consumption of ANY evaluation
signal on non-lethal turns.**

**M13 verdict: ⚖️ closed, no ship — but two durable assets:**
1. `osv3_setupval2` (0.768 matched-pair, 0.80 in the setup phase) — the
   campaign's first outcome-grounded evaluator. Proven signal, unproven
   consumer. Candidate consumers that are NOT overrides: plan-head targets
   (choose plans partly by predicted setup delta), reranking within the
   solver's existing candidate beam only at data-gen (no live tempo tax).
2. The 10k-game self-play corpus + the safari pipeline (m13_collect.sh) —
   mixed-opponent extension already designed.

**Observations / the one un-run mechanism:** the per-bucket table says the
value is excellent early (t4-15: 0.79-0.80) and near-chance in long grinds
(t32-47: 0.46-0.66) — but the v2 pilot trusts it EQUALLY on every turn. A
turn-gated override (value-guided solves only while turn < ~32, greedy after)
follows directly from the pre-registered per-bucket report — not a sweep.
Candidate v3, pending user go (gate overrides are user calls, Rung 0''
precedent). Alternative/complementary lever: mixed-opponent collection
(agreed as the next batch increment) to fix the late-game value itself.

Pre-start checklist:
- [ ] Flag to Piotr: value-learning arc + any >2h self-play batches (sign-off),
      no `rl/ppo.py` edits planned.
- [ ] Rung 0a card-fact patch is parity-sensitive (rl/tcg combat twins) —
      batch with its test updates before any run.
- [ ] Live monitor check before starting: how did 54779834 settle vs the
      rules bundle's 547? (Context for how much runway the neural track has.)
