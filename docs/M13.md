# M13 diary — outcome-grounded setup value (spec: [M13-plan.md](M13-plan.md))

Bar: ≥0.55 vs `solver:lucario` pooled n≥1200/2 seeds + meta co-gate; ship at
≥0.53 pooled (standing policy). Entry discipline: every run logged with
command + `runs/*.jsonl` + verdict + **Observations**; kills documented like
promotions.

## 2026-07-17 — SPEC'D, not started

Spec written after the live-observation round on submission 54779834 (see
docs/M11.md) and the user's directive: the model should LEARN this deck's
combos and card usage from playing many games. Design triangulated from the
campaign's measured dead ends — the one live route is outcome-grounded setup
value (pairwise-ranked, M12's proven loss) replacing `score_leaf`'s non-lethal
tail inside the exact turn search, then expert iteration on that broad
operator (M11 infra).

Pre-start checklist:
- [ ] Flag to Piotr: value-learning arc + any >2h self-play batches (sign-off),
      no `rl/ppo.py` edits planned.
- [ ] Rung 0a card-fact patch is parity-sensitive (rl/tcg combat twins) —
      batch with its test updates before any run.
- [ ] Live monitor check before starting: how did 54779834 settle vs the
      rules bundle's 547? (Context for how much runway the neural track has.)
