# M15 diary — hand-aware encoding (spec: [M15-plan.md](M15-plan.md))

User directives: full pipeline in one pass; standing observation-run policy
(technical gates enforced, win-rate gate informational).

## 2026-07-17 (late) → 07-18 — full pipeline run + SHIPPED

- **Phase E** (commit 3ee26f7): `encode_state_v3` (20 ids = 12 board + 8
  sorted hand-card ids, 0-padded), parameterized `OptionScorerV3`,
  `load_v3_into_v3h` zero-init migration (hand-aware(zero-hand) ≡ old net,
  the warm-start invariant's third use), pad shims for 12-id shards,
  width-sniffing loaders everywhere. 447 tests green.
- **Re-pin** (`runs/m15_repin_plan0c.jsonl`): `osv3_plan0c` **0.357** vs the
  POST-attach-fix `solver:lucario` n=400 — the honest baseline (old 0.415 was
  vs the weaker pre-fix solver; the canonical opponent gained ~6pp).
- **Collection** (`data/plan_m15/`): **800/800 games**, 12 workers, mixed
  opponents (self/buddy/rule), FIXED teacher (post-27cc495 `score_attach` —
  labels no longer teach energy waste), v3 encoder (state_ids width 20 —
  hand ids in every label), runaway cap active. 30,192 decision states. No
  hung workers this round (the M14 cap did its job).
- **Train** `osv3h_plan1.pt`: warm-start `load_v3_into_v3h(osv3_plan0c)`,
  data = plan_m15 + legacy dirs via pad shim, 5ep 1e-4. (Console metrics
  lost in an app crash mid-session; checkpoint carries no metadata — accepted,
  the checkpoint itself is the artifact and passed all downstream gates.)
- **Screen** (`runs/m15_screen_s1.jsonl`): vs `solver:lucario` n=400 seed 1 —
  **0.384** (153W/246L/1D). Read against the re-pin, not history: plan0c is
  0.357 on the same opponent, so hand-awareness + honest labels = **+2.7pp**;
  M14 plan2's headline 0.383 was vs the weaker solver. NB the raw jsonl codes
  0=win — a naive sum of the results arrays reads as 0.620 and is WRONG.
- **Ship** (observation run, win-rate gate informational; technical gates
  green: parity, bundle isolation, deck, gate game — re-verified post-crash,
  `runs/m15_gate_rerun.log`): **Kaggle submission 54793851**, first
  hand-aware bundle (3.57MB weights, 1708-wide state_enc). Monitor MODELS
  updated. NB the listing's initial 600.0 is Kaggle's starting μ, not a
  result — plan2 opened the same way and settled at 427.3.
- **Meta co-gate** (`runs/m15_meta_s1.log`, meta_v2, n=60/deck): weighted
  **0.419** — best neural meta score yet (plan2 0.403, plan0c 0.406). Per
  archetype: 0.392 vs `mega_lucario_ex+solrock` (weight 0.90), 0.683 vs
  mega_kangaskhan, 0.567 vs drakloak, 0.617 vs zacian.

## App-crash note (07-18 ~00:10)

The orchestrator app crashed right after the Kaggle upload; the submission
landed (COMPLETE on the Kaggle side) but the post-ship bookkeeping (this
diary, MILESTONES row, monitor registry, ship commit) was reconstructed from
the working tree + Kaggle listing afterwards. Training console metrics were
the only casualty.

## Observation watch-list (next session)

1. **Energy waste live**: attaches-to-saturated should drop from plan2's
   0.63 (the labels no longer teach it; does the net unlearn it?).
2. **Hand-visible combos**: gust/Gong/energy-in-hand setups the M14 audit
   flagged as invisible — do committed setup lines line up with held cards?
3. Leaderboard settle vs plan2's 427.3 and plan0c's 441 — the first shipped
   net where the score has a fixed teacher AND hand vision behind it.
4. Hand-aware VALUE head still pending: `osv3_setupval2` (12-id) drives
   setup commits; the fresh safari batch with v3 labels is the next
   overnight job (collector already switched).
