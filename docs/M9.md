# M9 diary — pilot-fix + learning ladder (spec: [M9-plan.md](M9-plan.md))

Bar: any agent ≥0.55 vs `solver:lucario`, n≥800 (promotion: pooled n≥1200, each
seed >0.52) + the meta_v2 co-gate. Session policy (2026-07-16, user-confirmed):
**submit pilot v2 to Kaggle once it confirms ≥0.53 pooled** + floors + co-gate —
a strict upgrade over the live solver bundle even below the campaign bar.

## 2026-07-16 — Session 1: Leg 1 ∥ Leg 2 ∥ Leg 0 (external-teacher probe)

### Implementation (Phase E, all edits before any run)

- **Leg 1** (`rl/generic_pilot.py`, `rl/turn_solver.py`, `rl/matchrunner.py`):
  `fixes: frozenset` threaded as explicit keyword-default params (a module
  global would leak across seats — both pilots share one worker process and the
  module-level `score_option`). Fix A: hand-discard with keepers → **-100**
  (below END) when `"handdiscard" in fixes`. Fix B: Boss's Orders (card 1182 —
  name uses **U+2019**, ASCII apostrophe never matches!) scores **2450** (above
  taper 2200/develop 2400, below KO tier 2550) when the opp bench holds a
  target my board KOs strictly faster than their active
  (`_gust_has_better_target`, race math from `rl/combat`). Specs
  `generic2:`/`solver2:` + separable `…2a` (Fix A) / `…2b` (Fix B) via the
  `_FIXED_KINDS` table. Solver DFS threads `fixes` through
  `_candidate_actions`/`_dfs`/`solve_turn`; `score_leaf` untouched (pure
  combat math).
- **Leg 2** (`rl/bc.py`): `collect_dagger` — student (`osv2_bc2.pt`,
  Gumbel-sampled `act(greedy=False)`, NOT argmax) advances the game, solver
  labels every decision; shards byte-compatible with `collect_games_v2` (mixes
  into `BCDatasetV2` directly). CLI: `--teacher dagger --checkpoint --seed`.
- **Leg 0** (`rl/matchrunner.py`): `ext:<dir>:<deck.csv>` spec loads an
  external kaggle-style `main.py` agent (our `cg` pinned in sys.modules first).
- Tests: `tests/test_pilot_fixes.py` (Fix A/B on + off + empty-fixes
  identity), parse_spec/make_pilot cases, DAgger wiring test (stubbed engine:
  labels=teacher, actions=student). One existing stub signature updated
  (`test_dev_pilot_wiring`). **411 passed.**

### R-gate 0 (all green)

- `pytest tests/` → 411 passed (incl. `test_parity.py` byte-identity of the
  default `solver:` path).
- `_GUST_IDS == {1182}` against real card data.
- Smoke `solver2:lucario` vs `solver:lucario` n=20 workers 4 seed 0: zero
  crashes (wr 0.450 — noise-level n, crash check only).
- DAgger smoke: 5 games → 1 shard, 378 decisions, loads in `BCDatasetV2`.

### Leg 0 — external "buddy" agent probe (kagglehub jek1wantaufik/buddy/other/pokemon)

- Bundle read END-TO-END before any execution: clean (only os/pickle/cg.api;
  no network/exec/writes). `deck.pkl` disassembled with pickletools first —
  primitives only — then restricted-unpickled: **60 ids, card-for-card
  identical to `decks/lucario.csv`** (M10's "our deck IS the meta" again).
  Bundled `cg/api.py` byte-identical to ours.
- The agent is a deck-SPECIFIC Lucario rule pilot with a cross-prompt
  `AttackPlan` global (plans attacker/target/attack at MAIN; coordinates
  Switch/Boss's Orders/attach/retreat around the plan). Notable heuristics to
  steal regardless of strength: Boss's Orders 3200 only when the plan targets
  bench (= our Fix B + cross-prompt coherence), Mega Brave discard-scaling
  (+60/fighting energy in discard), prize-trade awareness (-500 for Mega
  Lucario attacks at 2-3 own prizes left), win-detection short-circuit 50000.
- **Probe screen: 0.611** (244W-155L-1D, n=400, seed 1,
  `runs/m9_extprobe.jsonl`); **confirm: 0.617** (493W-306L-1D, n=800, seed 2,
  `runs/m9_extprobe_s2.jsonl`). **Pooled 0.615 at n=1200, both seeds >0.52 —
  formally clears the campaign promotion bar.** Strongest agent measured vs
  the solver all campaign (pinned: solver mirror 0.500, ppo_m83 0.359).
- **Teacher-adoption caveat:** buddy coordinates submenu decisions through a
  module-global `AttackPlan` — the SAME hidden-state pattern that capped the
  original rule-pilot BC teacher at ~55% fidelity (M1–M3 dead end). Milder
  here (the plan is recomputed deterministically from obs at each MAIN
  prompt), but direct BC/DAgger cloning still risks the trap for
  plan-coordinated prompts. Ranked uses: (1) **heuristic PORT** into our
  pilot — we have the code, re-implementation has no fidelity ceiling;
  (2) one bounded win-rate-gated DAgger round (teacher strength 0.617 vs
  solver's 0.500 raises the strength×fidelity product even at capped
  fidelity); (3) NOT plain fidelity-gated BC.
- Shipping buddy's own code (vs. learning from it) stays a user-level
  decision — it is someone else's public Kaggle model.
- Meta co-gate on buddy: RUNNING.

### Leg 1 — KILLED at screen (all variants < 0.52, go-gate was ≥0.53)

All n=400, seed 1, 12 workers, vs `solver:lucario`:

| spec | command checkpoint | result |
|---|---|---|
| `solver2:lucario` (A+B) | `runs/m9_leg1_s1.jsonl` | 202W-197L-1D **0.506** |
| `solver2a:lucario` (Fix A) | `runs/m9_leg1_fixA_s1.jsonl` | 180W-219L-1D **0.451** |
| `solver2b:lucario` (Fix B) | `runs/m9_leg1_fixB_s1.jsonl` | 188W-212L-0D **0.470** |

- **Fix A is actively harmful** (0.451): refusing Carmine whenever the hand
  holds any keeper forfeits more draw tempo than the occasional burned keeper
  costs. The M8.0 taxonomy located a loss mode, but the blanket refusal
  over-corrects.
- **Fix B flat-to-negative** (0.470) with a known mechanism-level residual:
  the PLAY-stage fix fires when a *faster-KO* bench target exists, but the
  downstream EFFECT_TARGET picker still selects the *highest-prize* target —
  Boss's Orders can pull the wrong Pokémon. Candidate **Fix B2**: when
  `"gust" in fixes`, score EFFECT_TARGET by fastest-KO (race math) instead of
  prize count. Cheap probe, queued behind live runs (edit hygiene).
- Code stays in-tree (default-off, byte-identical `solver:` path); the fixes
  simply don't ship.
- **Fix B2 probe also killed:** fastest-KO EFFECT_TARGET (race math, prize
  tie-break) when `"gust"` is on — `solver2b:lucario` rerun 185W-213L-2D
  **0.465** (n=400, seed 1, `runs/m9_leg1_fixB2_s1.jsonl`), no gain over
  Fix B alone (0.470). Likely mechanism: the solver's turn-DFS already finds
  gust lines when they are lethal; the greedy gust play otherwise wastes the
  supporter slot in the mirror. Full Leg 1 verdict: A 0.451 · B 0.470 ·
  A+B 0.506 · B+B2 0.465 — the pilot-fix track vs the SOLVER opponent is a
  measured dead end (the M8.0 taxonomy holes are real but greedy-tier fixes
  don't convert them against a search opponent).

### Leg 2 — round 1 KILLED (0.328 < 0.375 kill line)

- Collection: `bc collect --teacher dagger --checkpoint checkpoints/osv2_bc2.pt
  --games 1000 --out data/bc_dagger --seed 0` — **stopped at 400 games / 2
  shards** (ran ~5 games/min → full 1000 projected ~3.3h, over the 2h
  unattended-run bar; shard auto-resume allows topping up later).
- Train: `bc train --arch v2 --data data/bc_v2b data/bc_dagger --init
  checkpoints/osv2_bc2.pt --lr 1e-4 --epochs 5 --name osv2_dagger1` —
  val_acc 0.885 → 0.889 (fidelity moved little; gate is win rate).
- Gate: `model:checkpoints/osv2_dagger1.pt:lucario` vs `solver:lucario`
  131W-269L-0D **0.328** (n=400, seed 1, `runs/m9_leg2_s1.jsonl`) — below the
  0.345 base and the 0.375 kill line. **Killed.** Caveat: 400-game collection
  is 40% of spec'd volume; but the ≥+3pp bar for a round 2 is far away
  (-1.7pp). A round 2 only makes sense with the STRONGER teacher below.

### Meta co-gate — buddy

`replay_bc meta-eval` (n=60/deck, seed 0): **meta score 0.578** — vs pinned
ship 0.448 / osv2_bc2 0.405. Per-deck: 0.567 vs mega_lucario_ex+solrock
(weight 0.90), 0.650 vs mega_kangaskhan, 0.817 vs drakloak, 0.767 vs zacian.

### Session verdict (2026-07-16)

- **Leg 1 dead** (all 4 fix variants ≤0.506 at screen) · **Leg 2 round 1 dead**
  (0.328) · **Leg 0 buddy: passes every campaign gate** — 0.615 pooled vs
  solver (n=1200, seeds 0.611/0.617), meta 0.578.
- **No submission this session:** the user-approved policy (submit OUR pilot
  v2 at ≥0.53) found no qualifying candidate; shipping buddy's own code is a
  user-level decision (public third-party Kaggle model).
- **Recommended next legs (priority order):**
  1. **Heuristic port** (rules track): re-implement buddy's plan-coherent
     architecture in our pilot — cross-prompt AttackPlan, Boss's-Orders-only-
     on-planned-target, prize-trade awareness, Mega-Brave discard scaling.
     No fidelity ceiling (it's re-implementation, not imitation).
  2. **DAgger round 2 with buddy teacher** (needs a small `rl/bc.py` edit to
     accept an `ext:` teacher spec; gate on win rate as before).
  3. Leg 3 disagreement-weighted distillation with buddy labels.
- Infra kept: `ext:` matchrunner spec, `_FIXED_KINDS` (default-off),
  `collect_dagger`, `tests/test_pilot_fixes.py`.

## 2026-07-16 (late) — Leg 2 round 2: buddy-teacher DAgger — KILLED, law confirmed

User directives: buddy will NOT be shipped; use it to learn (analysis + train).

- Analysis delivered: [buddy-analysis.md](buddy-analysis.md) — the edge is a
  turn-scoped AttackPlan every prompt serves (plan coherence), plus
  prize-trade awareness and supporter rationing. Port plan sketched (closure
  state, deck-agnostic via `rl/combat` race math).
- Infra: `_teacher_pilot` accepts `ext:<dir>` teacher specs;
  `--dagger-teacher` CLI; `data/league/population_lucario.json` (buddy is
  deck-specific — mirror-only collection).
- Collection: 1000 games in ~13 min (no search), 5 shards →
  `data/bc_dagger_buddy` (student `osv2_bc2` sampled, buddy labels).
- Train `osv2_dagger2` (bc_v2b + bc_dagger_buddy, init osv2_bc2, lr 1e-4,
  5 ep): **d0 (lucario, buddy-labeled) fidelity 0.62→0.68 only** — the
  aliasing signature, called in advance.
- Gate: `model:checkpoints/osv2_dagger2.pt:lucario` vs `solver:lucario`
  79W-321L-0D **0.198** (n=400, seed 1, `runs/m9_leg2r2_s1.jsonl`) —
  **-14.7pp vs base, killed.** Buddy's plan-coupled labels are contradictory
  as a function of obs (same state → -1 or 3200 by hidden plan) on MOST
  prompts, unlike the solver (~1% deviation) — training on them actively
  degrades the student.
- **Generalized dead end (extends M1–M3, M10): imitation — BC *or* DAgger —
  from hidden-plan teachers.** "Observable teacher" must mean per-decision
  label consistency w.r.t. the observation, not merely readable code.
- **Buddy's one live lever: the heuristic PORT** (re-implementation carries
  the plan state explicitly, so nothing is hidden). Recommended as next
  session's lead track.
