# M25 — past the BC frontier (diary)

Plan: `docs/M25-plan.md` (refined this session). Antecedents: `docs/M24.md` (ship diary,
sub 54885173), `docs/M23.md` (signal-audit verdict). Branch: `feature/m25`.

## 2026-07-21 ~22:30 — refinement session: git hygiene, first live read, labeler broken

**Git hygiene (Piotr's ask):** M22/M23/M24 work committed on `feature/m22` (09213b2,
607 tests green first), merged to `main` (d2b8743, --no-ff), new branch `feature/m25`.
Not pushed to origin. Stray empty `docs/CLAUDE.md` left uncommitted.

**First live read of 54885173** (refresh: 49 episodes, all decks 2/2, obs ok, 0 warnings;
latest game 22:17Z): **25W–24L, score 600 → 723.0** — ~145 pts above the four our-deck
arms (531.5–579.3). Early/unsettled, μ-prior still decaying; the A/B power gate stays the
only sanctioned comparator (notebook wired but unexecuted — morning action).

**🔴 The archetype labeler is systemically broken** — the M24 Alakazam mislabel was the
tip. Root cause (`rl/kaggle_ingest.py:645`, `_assign_archetypes`): greedy agglomerative
merge at `ARCHETYPE_COS = 0.95` over pooled Pokémon STAT-FEATURE vectors — distinct decks
collapse into the most-played cluster and inherit its name. Evidence, both directions:
- The **1268-score deck `3121746f`** (subs 54861775/54863653/54861685; 689 opp_decks
  rows) is labeled `mega_lucario_ex+solrock` but its cards are **3× Marnie's Grimmsnarl
  ex + Froslass/Snorunt + 4× Munkidori — Darkness control/mill**. The M25 teacher
  candidate is a CONTROL deck, not a lucario variant.
- This session's harvest re-run RESEEDED the clustering and the mega-cluster flipped its
  name to `alakazam+kadabra` (42 of 54885173's 49 games land in that one bucket) —
  labels are not even stable across harvests. Per-archetype forensics is currently
  meaningless; "field is 76% mirror" and "arms bleed vs mega_lucario" need re-reads.

**Matchup profile of 54885173 by TRUE archetype** (card-id grouping per deck_hash,
labeler bypassed; n's tiny → hypotheses, not reads):

| real archetype | record |
|---|---|
| Mega Lucario field (incl. our stock 60) | 8W–6L |
| Alakazam mirror + Dunsparce variants | 5W–5L |
| Dragapult | 4W–1L |
| **Marnie's Grimmsnarl control** | **1W–4L** |
| Team Rocket (Spidops/Murkrow) | 1W–3L |
| Starmie / Crustle-Kangaskhan / other | ~even, 1–2 games each |

Offline battery shape broadly confirmed (strong into the lucario field); the dragapult
line is SOFTER live than `rule:dragapult` implied; the bleeds are Grimmsnarl control and
Team Rocket — neither ever in a pool, and Grimmsnarl is the 1268 cluster's archetype, so
the Phase 1 teacher doubles as counter-exposure to our worst matchup.

**Plan deltas from this session:** Phase 1 gains a blocking step 0 (fix the labeler —
name per deck_hash via its own top-2 Pokémon or card-identity overlap; re-harvest;
verify clone→alakazam, 3121746f→grimmsnarl, our 60→lucario; re-read field composition);
Phase 2's self-play pool may be cross-archetype (alakazam-BC vs grimmsnarl-BC); the
"commit first" open question is resolved (merged).

## 2026-07-21 late — Phase 1.0 DONE: labeler fixed, field re-read, loss anatomy

**Labeler fix shipped** (`rl/kaggle_ingest.py`): `_assign_archetypes` now names every
deck_hash from its OWN top-2 Pokémon (copies, then HP) — the cosine cluster-join and
`ARCHETYPE_COS` are gone (`ARCHETYPE_LABELER = "top2_pokemon_v2"` recorded in meta
manifests). Labels are per-deck, deterministic, stable across harvests by construction.
21/21 `test_kaggle_ingest.py` green (cluster-absorption regression test added).
Refresh: 0 new episodes since 23:21 (54885173 still 49 games, 25W–24L, 723.0).
Re-harvest: 8950 seat-rows, 1097 unique decks, 197 archetypes. Verification: `3121746f`
→ `munkidori+marnie_s_impidimp` (its own cards: 4× Munkidori + Grimmsnarl line — the
control deck, correctly named), 54885173 → `alakazam+kadabra`, all lucario arms →
`mega_lucario_ex+solrock`. Caveat: top-2-by-copies surfaces 4-ofs, so the Grimmsnarl
family splits across `munkidori+marnie_s_impidimp` / `marnie_s_grimmsnarl_ex+*` labels —
group by family for matchup reads.

**Field re-read (opponent seat-rows, post-fix):** alakazam+kadabra 1614/115 decks,
**munkidori+marnie_s_impidimp 1192/28** (our worst matchup is the #2 archetype and
heavily concentrated — few lists, many games = high scorers), team_rocket 661/39,
mega_lucario+solrock 539/20, drakloak+dreepy 468/122. "76% lucario mirror" is dead —
the ladder is alakazam + darkness-control heavy.

**54885173 forensics (fixed labels, n=49 — directional):** lucario field 8W–6L
(riolu 7–3, stock solrock 1–3), alakazam mirror ~even, dragapult 4–0 soft live,
**grimmsnarl family 1W–4L, kangaskhan+crustle 0–3, cinderace+starmie 1–3** (the
starmie/kangaskhan bleeds were hidden inside "other" pre-fix), rocket 0–1. All three
munkidori losses are to the SAME deck `be5107ce` (opp 708–867, 171–193 steps).

**Loss anatomy (rl/postmortem on the 8 bleed losses):**
- **Deck-out vs walls**: 87348411 decked at t22 with SIX prizes left (zero taken);
  87329948 decked at t24 with 5 left; 87330412 down to deck=8, hand = 3 hoarded
  Hildas. Kangaskhan/Crustle stalls and we draw our deck away without converting.
- **Mill race lost vs Grimmsnarl** (`be5107ce`): deck 6–17 by turn 11–16 with 3–4
  prizes still to take — Petrel/Froslass shreds the deck while conversion stalls.
- Chronic flags in EVERY loss: `evolve-left` (turns ended with Alakazam evolutions
  offered, unplayed — up to 8 consecutive turns), `fetch-dead-evolution` (searching
  Alakazam/Kadabra with no basis anywhere), `trainer-hoarded` (Boss's Orders playable
  4–8 turns, never played — the exact tool that breaks stall), `attach-off-racer`.

**Flag audit across all 49 games: the flags are CHRONIC, not loss-specific** —
per-game rates nearly identical in wins vs losses (attach-off-racer 2.8/3.0,
evolve-left 2.6/1.4, trainer-hoarded 1.7/1.1, fetch-dead-evolution 0.7/1.2). The
pilot carries the same behavioral defects into every game; fast/linear opponents
don't punish them, stall/control does. Interpretation: this is teacher-fidelity
ceiling behavior (BC never saw anti-stall play), which is exactly the Phase-1 bet —
the Grimmsnarl-mill teacher is both archetype diversity AND counter-exposure to the
punisher. A cheaper orthogonal lever: fidelity push (weighting, epochs) may not fix
`evolve-left`-class defects if the teacher itself has them — check the teacher's own
replays for the same flags before blaming BC fidelity.

**Teacher-vs-clone flag comparison (teacher 54618168, n=373 vs clone n=50):**
`attach-off-racer` identical (2.84 vs 2.92/g) and `evolve-left` HIGHER in the teacher
(3.51 vs 2.02/g) — these are teacher behaviors faithfully cloned, and a 1268-score
teacher exhibiting them while winning means the mechanical flags are not pure error
signals. But **`fetch-dead-evolution` (0.44→0.94/g) and `trainer-hoarded`
(0.77→1.38/g) DOUBLE in the clone — genuine fidelity losses**, now concrete target
metrics for the Phase-1.2 fidelity push (flag-rate parity with teacher as a new gate).

**Teacher matchup profile (n=373, real n's at last):** munkidori/grimmsnarl control
**0.59 over n=79 — the teacher BEATS our worst matchup**; our clone's 1–4 there is
either fidelity loss where it matters or n=5 noise → fidelity push targets this
directly. But **kangaskhan+crustle 0.31 (n=39), crustle+dwebble 0.11 (n=9) — the
wall/stall bleed is TEACHER-LEVEL**: no fidelity gain reaches it; needs the second
teacher or a policy-level fix. Field context: grimmsnarl-family runs 0.533 vs the
alakazam field (n=454) and 0.453 vs walls (n=95) — the Phase-1 grimmsnarl arm patches
the wall hole partially and holds its own vs the #1 archetype.

**A/B monitor re-run (23:48):** Decidable NO — gap +1.0pp WR (54885173 vs 54864190),
MDE 28.9pp at n=49/45; the 723-vs-564 score gap reflects opponent strength, not a
sanctioned WR read. `docs/M22-live-state.md` regenerated with the M24 arm.
(Note: notebook executed via `uv run --with nbconvert --with ipykernel python -m
nbconvert --execute` — nbconvert is not a project dep.)

## 2026-07-22 ~00:00 — Phase 1.1 launch: Grimmsnarl teacher corpora + trains

Corpora built (`scripts/m25_build_grimmsnarl_corpora.sh`, all three subs play the
identical deck 3121746f): `bc_m25_grimmsnarl_54861775` 171 eps, 3 shards;
`bc_m25_grimmsnarl_54863653` 255 eps / 23,628 decisions, 5 shards; pooled 552 seats /
49,947 decisions (backup only — M24 single-teacher law). Deck extracted:
`data/kaggle/grimmsnarl_3121746f_deck.csv` (validates legal, round-trips to hash
3121746f, **md5 dc8c23cd** for the ship ritual). Trains launched ~00:05
(`scripts/m25_p1_train.sh`: fresh V3-as-BC, epochs 10, lr 1e-4 — the E1/M24 recipe)
for both single-teacher corpora; screens staged (`scripts/m25_p1_screens.sh`, each
clone on ITS OWN deck vs rule:lucario n=200 seed 1, workers 8).

**Train results (~00:03, 30s/train):** `m25_bc_grim_54861775` train 13,271 / val 1,525,
val_acc **0.565**; `m25_bc_grim_54863653` train 21,473 / val 2,155, val_acc **0.580**.
Both well below the alakazam clone's 0.655–0.664 — the mill/control policy is harder to
imitate (longer games, more symmetric option menus?) or the pilot is more stochastic.
Fidelity ≠ strength (M23 law: V2 0.664 ≈ V3 0.657 yet different strength) — screens
decide. Screens launched ~00:05.

**Screen results (n=200 vs `rule:lucario`, seed 1, decoded 0=side-a-win):**
`m25_bc_grim_54861775` **66W–134L, wr 0.330**; `m25_bc_grim_54863653` **65W–135L, wr
0.325**. Both land at the `rule:lucario` FLOOR band (historical series 0.319–0.3475,
bar 0.362 — no current agent clears it) and far below the alakazam clone's 0.605 pooled
n=400 on the same opponent.

**Type-mechanics check (Piotr's hypothesis: Grimmsnarl's live edge over our shipped
Alakazam is a Darkness>Psychic weakness artifact, and it might also explain this screen).**
Engine: `tcg/combat.py` — weakness is a flat `WEAKNESS_MULTIPLIER=2` (`tcg/constants.py`)
when attacker energy_type == defender `weakness_id`; resistance is a flat `-30`.
Confirmed from `data/cards_features.csv`:
- Our shipped clone's core (Abra/Kadabra/Alakazam, card ids 741/742/743) — Psychic,
  `weakness_id=7` (Darkness). Grimmsnarl's attackers are Darkness (energy_type 7) →
  **2× damage, mechanism for the live 1W–4L bleed CONFIRMED and quantified.**
- The Phase-1.1 screen is a DIFFERENT pairing: Grimmsnarl (Darkness) piloting its own
  deck vs `rule:lucario` (Fighting; weak_id=5 Psychic on Makuhita/Hariyama/Riolu/Mega
  Lucario ex, weak_id=1 Grass on Lunatone/Solrock). No card in either deck has a
  weakness the opponent's attack type exploits — Munkidori (Grimmsnarl's one non-Darkness
  card) even `resist_id=6` (Fighting), a mild advantage FOR Grimmsnarl. **Type mechanics
  are neutral-to-favorable for Grimmsnarl here — they do NOT explain the 0.33/0.325
  screen result.** The weakness story is confirmed for the live matchup and explicitly
  ruled out as the cause of the offline screen kill.

**VERDICT: KILL both Grimmsnarl single-teacher arms** (54861775, 54863653) — screen wr
0.33/0.325 vs the 0.605 pooled comparator, at floor-band level, and not a type artifact.
Read as genuine fidelity/gameplan-fit failure: val_acc 0.565–0.580 (vs 0.657–0.664 for
the alakazam clone) trying to execute a mill/control gameplan, which punishes the
pilot's chronic BC defects (`evolve-left`, `trainer-hoarded`, `fetch-dead-evolution` —
see Phase 1.0 flag audit above) far harder than `rule:lucario`'s straightforward
beatdown punishes them — every wasted turn is a wasted stall/mill turn. Pooled 3-sub
corpus (backup, M24 single-teacher law) not screened — no reason to expect it clears a
bar the stronger single-teacher legs both missed by ~27–28pp; not pursued further.
**New standing testing note**: read any screen for a `weakness_id`/`energy_type` overlap
between the two decks before attributing the result to pilot quality — this one happened
to be clean, but it won't always be.

Next: Phase 1.2 (fidelity push on the shipped teacher, `m24_bc_54618168` / alakazam
corpus) — the plan lists three untried levers (more epochs, winner/score weighting via
`rl.bc.py --weighting {score,winner}`, hand-aware ids via `plan_iter --init-v3h`) behind
the 0.660/0.290 win-rate pins (M24's seed-1/pooled-dragapult numbers). One variable at a
time per campaign discipline — lever choice pending.

## 2026-07-22 ~08:30 — Phase 1.2: fidelity-push levers 1 & 2, both NULL/KILL

**Lever 1 — more epochs** (`rl.plan_iter train --data data/bc_m24_54618168 --epochs 25`,
fresh V3, identical corpus/recipe to the shipped `m24_bc_54618168` besides epoch count):
`m25_bc_alakazam_ep25` val_acc climbs monotonically to **0.655 at epoch 24** — matches,
does not beat, the original 0.655 pin (which the 10-epoch baseline already reached).
Screen n=200 seed 1 vs `rule:lucario` (clone deck): **120W–80L, wr 0.600** vs the
0.660 pin — a −6pp move, inside n=200 screen noise (~±7pp) but no detected gain.
**NULL RESULT: more epochs alone does not move fidelity or strength past the existing
ceiling** — the corpus/recipe combination plateaus at ~0.65–0.66 by epoch 10 already.
(Caveat: no fixed train/val-split seed, so epoch-9 reads ~4pp apart across runs are
partly run-to-run noise, not a clean ablation — a second seed would be needed to firm
this up if revisited.)

**Lever 2 — winner/score weighting** (`rl.bc.py train --arch v2 --weighting score`,
same corpus, 10 epochs — shards carry `teacher_score`/`seat_won` so the flag applies
directly): val_acc 0.625 at epoch 9, already **4pp below the unweighted V2 clone's 0.664**
fidelity. Screen n=200 seed 1: **80W–119L–1D, wr 0.403** — a **−19pp** move off the V2
clone's 0.59 comparator (`checkpoints/bc_clone_54618168.pt`), far outside screen noise.
**KILL: score-weighting on this corpus actively hurts strength.** Consistent with the
M19 law that label-reweighting trades accuracy for behavior shaping and costs strength
in proportion to the reweighted-row fraction — here the reweighting bought nothing
because the base clone is already faithfully imitating a proven-strong, uniformly-good
teacher (373 eps, wr .620–.626), so upweighting its highest-score subset just narrows
the effective training distribution.

**Phase 1.2 status: 2 of the 3 planned fidelity levers now null/killed.** Third lever
(hand-aware ids, `plan_iter --init-v3h`, "untried on replay corpora" per the plan) not
yet run — checking in with Piotr before spending it, since the first two both landed at
or below the existing pin and Phase 1's cheap-BC-headroom premise is looking thin.
Piotr: proceed with hand-aware ids.

## 2026-07-22 ~09:00 — Phase 1.2 lever 3: hand-aware ids — NEW CANDIDATE, full battery clears

**Infra, two real bugs fixed to unblock this lever** (both landed, 610→ tests green):
1. `rl/replay_bc.py` never wired `encode_state_v3` (M15's board+own-hand encoder) to the
   replay-corpus builder — only `encode_state_v2` (board-only) was ever called. Added
   `--hand-aware` to `replay_bc build` (and a `hand_aware` param through `encode_decisions`)
   to opt into it; recorded in `deck_registry.json` params for provenance. Own-hand ids
   are legitimately available in replay data — a replayed seat's observation always
   includes ITS OWN hand. Test: `tests/test_replay_bc.py` (dispatch width, 2 cases).
2. `plan_iter.load_v3_into_v3h` (the actual M25 blocker) hardcoded the LEGACY
   `OPTION_V2_DIM` (90) option-encoder width when constructing the warm-start target —
   silently wrong for every option-identity (`OPTION_V3_DIM`=94) checkpoint, which has
   been the default since M16. Every shipped V3-as-BC checkpoint since M24 (and this
   session's own `m25_bc_alakazam_ep25`/`_wscore`) is option-identity, so this warm-start
   path was DEAD CODE for the entire recipe this campaign now uses — first real use this
   session hit the size-mismatch crash immediately. Fixed to infer `option_dim` from the
   source checkpoint's own `option_enc.0.weight` width (same pattern `load_v3h_into_v3o`
   already uses) — backward compatible, existing pre-M16 warm-start tests unchanged.
   Regression test added (`test_v3_into_v3h_preserves_option_identity_width`).

**Corpus**: rebuilt `data/bc_m25_alakazam_v3h` from the same sub (54618168, min-score
600) with `--hand-aware` — 370 eps, 28,651 decisions, 6 shards (matches the v2 corpus
exactly, only the ids widen 12→20).

**Train**: `plan_iter train --init-v3h checkpoints/m24_bc_54618168.pt` (the shipped
checkpoint itself as the warm-start source — hand columns zero-init, so epoch 0
val_acc 0.659 lands right at the legacy pin per the warm-start invariant), 10 epochs.
**Best val_acc 0.668 (epoch 8)** — +1.3pp over the 0.655 pin, first lever to beat it.

**Full battery, `checkpoints/m25_bc_alakazam_v3h.pt` (clone deck), n=200×2 seeds:**

| opponent | seed 1 | seed 2 | pooled n=400 | pin |
|---|---|---|---|---|
| `rule:lucario` | 0.670 | 0.650 | **0.660** | 0.605 |
| `rule:dragapult` | 0.260 | 0.355 | **0.3075** | 0.290 |
| `random:kyogre` | 0.970 | 0.960 | 0.965 | 0.950–0.955 |

**Beats both strength pins** (+5.5pp lucario, +1.75pp dragapult — a new campaign
out-of-loop record) **and clears floors.** Also qualitatively different from the
original: `m24_bc_54618168`'s two seeds were 0.660/0.550 (11pp spread, "borderline" per
the M24 diary); this checkpoint's are 0.670/0.650 (2pp spread) — hand-aware ids may have
fixed a genuine instability, not just nudged the mean. **First candidate this session to
clear NEW CANDIDATE status.**

## 2026-07-22 ~09:17 — 🚀 SHIPPED: sub 54897966 (m25_bc_alakazam_v3h + clone deck)

Piotr's explicit ship call. `./build_submission.sh --checkpoint m25_bc_alakazam_v3h.pt
--deck clone54618168 --message "..."`. Ritual run before treating it as done:
- tarball `dist/submission_neural_20260722_091711.tar.gz` (5258 KB) deck.csv md5
  **8e8cf124** = `decks/clone54618168.csv` ✓ (matches the pinned value exactly, same deck
  as 54885173 — deliberately unchanged, only the checkpoint differs).
- gate game OK (177 decisions), isolation + deck-legal OK.
- **Kaggle sub 54897966**, upload COMPLETE, μ=600 (starting prior, not a result).
- `notebooks/model_monitor.ipynb` MODELS dict updated + re-executed: sub resolves to
  `clone54618168.csv — ace Alakazam (clone)` from a live replay, confirming shipped deck
  identity independently of the build-time md5 check.

Live arms now: **54897966 (M25, hand-aware alakazam clone)** + 54885173 (M24, same deck,
prior checkpoint) + 54864190/54849475/54846434/54836093 (our-deck lineage). Watch: does
the hand-aware net's live matchup profile echo the offline gains, particularly against
the Grimmsnarl-control bleed (54885173's live 1W–4L) — Munkidori/Grimmsnarl weakness
math is unchanged (still 2× Darkness→Psychic on our Alakazam core), so no live
improvement there is expected from this lever specifically. Next: let it settle, re-run
the A/B monitor once both alakazam-deck ships have enough games, then decide Phase 2
(S2-flip test) vs further Phase 1 headroom (only 2 of the corpus's leaderboard subs have
been probed as teachers).

## 2026-07-22 ~post-ship — 54897966 first-9-games post-mortem: ALL losses are deck-outs; Telepath Psychic Energy near-unplayed

Refresh: 9 episodes (5W–4L). Full-decision forensics (scratchpad `m25_postmortem.py`
/ `sub_behavior.py` / `attach_probe.py`, replayed via `iter_replay_decisions` +
`to_observation_class`):

**Every one of the 4 losses ends with OUR deck at 0** (LogType.RESULT reason 2 =
"start turn with 0 deck cards"): deck_end us/opp = 0/13, 0/0 (t47), 0/4, and 0/25 —
that last one we milled ourselves out while the opponent had 25 cards left. 6/9 games
end with our deck ≤3. This is the M24-era "deck-out vs walls" bleed, now 100% of the
live loss mass on n=9.

**Causal chain is energy under-attachment → slow conversion → self-mill.** Same-probe
teacher (54618168, last 60 eps) vs clone (54897966, 9 eps):
- Energy attached on **41% of our turns vs teacher 57%**.
- When Telepath AND another energy are both attachable in the same menu, teacher picks
  Telepath 46 : 56 Basic-P (45% Telepath); the clone picks it **3 : 19 (14%)**. Overall
  Telepath goes down 5.8% of attach-opportunity decisions vs teacher 15.2%; ~1.6
  attaches/game vs teacher 2.3.
- Telepath's text is the deck's SETUP ENGINE: attach to a {P} Pokémon → search up to
  2 Basic {P} Pokémon onto the Bench (and it thins the deck without drawing). Not
  attaching it = no free Abra basis → the chronic `evolve-left` /
  `fetch-dead-evolution` flags → games run 21.8 turns avg vs teacher 16.6 while the
  faithfully-cloned Dudunsparce draw engine (5.9 ability uses/game vs teacher 5.6)
  keeps milling ~4 cards/turn → deck-out.
- Draw discipline at deck ≤6 is teacher-level recklessness in both (clone takes the
  Dudunsparce draw 21/40 offered, teacher 148/242) — the teacher survives it only
  because it converts faster.
- Supporter hoarding (known M24 flag) also worse than teacher per hand-sighting:
  Boss's Orders 4/382 vs 54/1545, Hilda 2/352 vs 57/2087. Shaymin ~never played by
  either (0/190 vs 8/1212 — teacher behavior, not a fidelity loss).

**Encoder is NOT blind here**: `encode_option` resolves the attached card's identity
(FEAT row + id embedding for hand area/index), and hand-aware v3h ids see the 4
Telepath copies sitting in hand (they're in-hand in ~70% of MAIN decisions). The
Telepath deficit is a learned-policy/fidelity gap on the ATTACH class specifically —
plausibly class imbalance (ATTACH is 8% of MAIN decisions in the corpus) letting a
2.6:1 teacher preference collapse to 6:1 the other way. Candidate M26 levers, in
order of cheapness: (a) per-class fidelity report for `m25_bc_alakazam_v3h` on the
val split (does ATTACH-class val_acc lag the 0.668 headline?); (b) attach-class or
Telepath-decision upsampling in the corpus (NOT score-weighting — that lever is
killed); (c) a solver/rule override: if Telepath in hand + energyAttached false +
{P} Pokémon on board and bench space, attach it — teacher-mimicking heuristic
patch at pilot level, zero training cost.
