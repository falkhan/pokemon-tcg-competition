# M32 — deck analysis + a PPO-vs-exogenous-meta quick check (diary)

Branch `feature/m31` (continuing). This milestone opened as "deck surgery" but the
meta analysis redirected it. Kept incremental per CLAUDE.md.

## 2026-07-23 — P0 deck analysis: our deck IS the meta-optimal Alakazam list

Context: M31 (54935640) regressed live (6W–8L at n=14, verified NOT a crash — normal
actions in the raw replay; the offline battery mispredicted live again). Piotr chose to
try a different deck / retune. Data-driven scan of `opp_decks.parquet` (9,419 opponent
rows, 208 archetypes):

**Meta + our pooled WR (opp score ≥900):** cynthia's gabite 1151 (0.454) · team_rocket
1130 (**0.420**) · munkidori/grimmsnarl-control 1127 (0.447) · dragapult 1117 (0.464) ·
**alakazam (ours) 1083 (0.499)** · kangaskhan/crustle walls 1055–67 (0.58–0.66, we beat).
We are 0.42–0.46 vs the entire top tier.

**Decisive finding:** the top Alakazam variant `9294d9d836` (1,584 games, avg 1084,
**max 1251**) is **byte-identical to our `clone54618168`** — it is the dominant AND
highest-scoring Alakazam build; every other variant is rarer and weaker. So **the deck is
already meta-optimal (M4 redux); the entire 791→1251 gap is PILOT quality.** And on
Alakazam we've exhausted both pilot levers: BC hit the ~0.66 fidelity ceiling
(M24/25/26), and PPO-vs-exogenous+mirror was run to convergence without out-of-loop
transfer (M23). Anti-Darkness deck-tech is marginal + risky (the 1140 players run our
exact list and accept the Munkidori loss).

Piotr's call: **try (B) as a quick check** — the one Alakazam PPO variant not specifically
done: fine-tune targeted at the decks we LOSE to, as an exogenous pool, and test transfer.

## 2026-07-23 — M32-B quick check: PPO vs the decks we lose to (PRE-REGISTERED)

Design (warm-start, deck fixed at the optimal list, exogenous pool, one sealed instrument):
- **Base:** `m28_winners.pt` (V3 plan-head net + value head; PPO-compatible, M20 path).
- **Learn deck:** `clone54618168` (the optimal Alakazam list — no fidelity reset).
- **Training pool (exogenous, the decks we lose to):** rocket clone 0.35 + grim/munkidori
  clone 0.35 + mirror 0.30 (mirror for stability). **Dragapult deliberately EXCLUDED** so
  `rule:dragapult` stays the clean out-of-loop transfer gauge (M22 discipline).
- **KL anchor 0.1** to the frozen start (prevent forgetting the BC base / mirror).
- 8 iters × 400 games, `--workers 8`, `--eval-every 99` (skip the misleading in-loop
  eval — M23 law; final model = `checkpoints/ppo_current_m32b.pt`, saved every iter).

**[Superseded gate — re-spec'd 2026-07-25, see below.]** Pre-registered kill-gate (fixed BEFORE the numbers). After 8 iters, battery BOTH the
base (m28_winners plain) and `ppo_current_m32b` (plain) on the same beds, n=400 (2×200):
- **Transfer gate (primary):** `rule:dragapult` (SEALED) — ppo must beat base resolved
  (z > +1.96). If flat/down (z ≤ 0) → **KILL: the M23 no-transfer pattern reproduced;
  targeted-exogenous PPO overfit the trained pool** → move to the dragapult archetype (A).
- **Mirror guard:** `rule:lucario` ppo ≥ base − MDE (non-inferior; no catastrophic
  forgetting).
- **Trained-matchup sanity:** ppo vs rocket clone AND grim clone ≥ base (training took).
- **Floor:** `random:kyogre` ≥ 0.90.
- Promising ⇒ transfer gate passes AND mirror holds. Then consider more iters / QC / ship.
  Otherwise KILL and pivot to A (dragapult).

Wiring smoke (1 iter × 16) passed: pool parsed (rocket+grim+mirror), KL active (kl 0.039
@ coef 0.1). Launching the 8-iter run.

## 2026-07-23 — M32-B run CANCELLED at iter 0 (machine shutdown); clean resume state

Piotr stopped the run to power off the box. It was killed during iter 0's first
collection — **no iteration completed, no trained checkpoint produced.** Stopped via
TaskStop (not pkill; engine jobs share the box); orphaned mp-Pool workers cleared by
exact PID; original main PID confirmed gone. The untrained/smoke `*_m32b*` checkpoints
(iter-0 copies of m28_winners) were deleted so a resume isn't misled. `runs/m32b_ppo.log`
(15 KB) preserved. **Nothing to salvage — resume = re-run the same idempotent command.**

**Resume checklist (pick up here next session):**
1. Re-launch the exact quick-check run (idempotent, warm-starts fresh from m28_winners):
   ```
   uv run python -m rl.ppo \
     --start m28_winners.pt --learn-deck clone54618168 \
     --iterations 8 --games-per-iter 400 --workers 8 \
     --opponents "model:checkpoints/m30_bc_rocket_54834745.pt:data/kaggle/rocket_3394cd30_deck.csv=0.35" \
                 "model:checkpoints/m25_bc_grim_54861775.pt:data/kaggle/grimmsnarl_3121746f_deck.csv=0.35" \
                 "mirror=0.30" \
     --kl-coef 0.1 --eval-every 99 --tag m32b > runs/m32b_ppo.log 2>&1
   ```
   Final model → `checkpoints/ppo_current_m32b.pt` (saved every iter). ETA ~2h @ workers 8.
2. Battery BOTH `model:checkpoints/m28_winners.pt:clone54618168` (base) and
   `model:checkpoints/ppo_current_m32b.pt:clone54618168` (ppo) — n=400 (2×200) — vs
   `rule:dragapult` (SEALED transfer gate), `rule:lucario` (mirror), the rocket + grim
   clones (trained-matchup sanity), `random:kyogre` (floor). Reuse the matchrunner harness.
3. Decode against the **pre-registered kill-gate above**: sealed dragapult must improve
   resolved (z>+1.96) with mirror non-inferior → promising; else KILL → pivot to the
   **dragapult archetype (option A)**, the only unexplored ground (fresh clone + PPO).

## 2026-07-24 — M31 post-mortem at n=54: deck-out is a CLOSING-SPEED failure, and the battery measures the wrong beds

Refreshed 54935640 to **54 games** (+40 since the n=14 read below). Full write-up
in `docs/m31-post-mortem.md`; new reusable probe `scripts/live_deck_race.py`.

- **M31 converged flat-to-down, the n=14 alarm did not revert:** 28W–26L (0.519),
  settled ~707–719 vs M30's 812; opponent-adjusted implied ELO **710 vs 811**, and
  M31 faced *weaker* opposition (697 vs 764). WR gap alone is z≈0.49 — directional,
  not resolved.
- **O7 poffinfloor engaged live but bought nothing:** Poffin-declined-at-bench≤1
  51→7, bench=1 prompts 7.3%→4.2%, bench=5 16.9%→24.9%. Poffin plays/game flat
  (1.55→1.54) ⇒ O7 *relocated* plays, did not add deck cost — the "O7 worsens
  deck-out" hypothesis is **not supported**. New small regression: missed-bench at
  bench==0 went 0→12 prompts (O7 displacing a playable basic at bench 0).
- **deckguard/conserve verified firing at ~100%** (dud 0/98, fez 0/26 at deck≤6 is
  the *designed* demote — `rl/plan.py:335,:344,:440`, not a wiring bug). Deck-out
  still rose: M29 30% → M30 31.6% → **M31 34.6% of losses**.
- **The diagnosis behind the whole O-rule family is backwards.** Deck-out games are
  long and burn *slowest* (M31: 25.6 turns @ 2.00/t vs wins 14.9 @ 2.63). Confound
  controlled with a fixed-turn deck curve (pooled n=142): at T8 we hold **22** cards
  in games we deck out vs **16** in games we win; curves only cross ~T13. WR by
  length: **≤15 turns 0.65 (n=82) vs ≥16 turns 0.40 (n=60), z≈2.9, p≈0.003.** 8/9
  M31 deck-out losses were level-or-behind on prizes, 3/9 hard stalls at 6 prizes.
  ⇒ We under-dig early and fail to close; conservation at deck≤6 fires ~10 turns
  after the divergence and cannot help.
- **Instrument problem (the big one).** Pooled archetype WR (n=142): lucario 22.5%
  share @ **0.72**, mirror 21.1% @ **0.47**, archaludon 12.0% @ **0.47**, crustle
  9.2% @ 0.54, **team_rocket 7.0% @ 0.10**, grim 6.3% @ 0.33, dragapult 6.3% @
  **0.78**. `decks/lucario.csv` (the battery's primary bed) is a *Mega Lucario ex*
  list — our 2nd-best matchup; `rule:dragapult` (M32-B's SEALED gate) is our **best**
  matchup. The two instruments deciding our ships cover 28.8% of the meta and we win
  both; the 46.4% we lose is largely unmeasured, and **archaludon (12%, 0.47) is in
  no bed and no pool**. Mechanical explanation for `no-validated-live-predictor`.

**M32-B re-spec (legitimate: the run was cancelled at iter 0, nothing observed).**
Keep the training pool (rocket/grim/mirror — it targets 0.10 and 0.33 matchups).
**Swap the sealed transfer gate from `rule:dragapult` (0.78, ~0.22 headroom, cannot
resolve) to an archaludon clone** built from `opp_decks.parquet` — 12% of the meta,
0.47 live, genuinely out-of-loop. Add mirror + archaludon to the reporting battery
either way. Awaiting Piotr's go before re-launching.

**Open threads carried out of this session (all in the campaign memory):**
- **M31 (54935640) is regressing live** — 6W–8L at n=14 (verified NOT a crash), ELO <700.
  Latest sub represents us; the M31 offline battery mispredicted live again. Rollback
  insurance (re-submit 54929991 ~791) is a 2-min job if wanted; deferred (Piotr: "we have
  time").
- **Strategic prior for M32:** the Alakazam deck is meta-optimal, the gap is 100% pilot,
  and both Alakazam pilot levers (BC ceiling, PPO-vs-mirror) are spent. If B's transfer
  gate fails, the honest next move is the dragapult archetype, not more Alakazam.

## 2026-07-25 — M32-B RE-LAUNCHED with the archaludon sealed gate (Piotr's go)

Piotr confirmed this session: **run M32-B now**; build the sealed transfer gate as
**`generic:archaludon` (deck-only)**, not a BC clone.

**Sealed gate built.** Extracted the dominant archaludon list from
`opp_decks.parquet` — deck_hash `b65e607ca55cf0f87adf1123e81bc40d2ff9f002`,
archetype `archaludon_ex+cinderace` (86 opp seat-rows, avg 824, max 1025; the
top of 12 archaludon variants). Legal (60 cards, validate_deck clean), written to
`decks/archaludon.csv`; `resolve_deck('archaludon')` round-trips to the same hash.
- **Spec choice:** `rule:archaludon` is impossible — `load_teacher` only knows the
  three company sample agents (lucario/iono/dragapult), and there is no archaludon
  sample agent. The deck-agnostic **generic rule pilot** on the archaludon deck is
  the analog Piotr approved: spec `generic:archaludon`
  (`rl/matchrunner.py:335` → `make_generic_pilot(resolve_deck('archaludon'))`).
  Genuinely out-of-loop: archaludon is in no training pool and no prior bed.
- **Resolving-power caveat to check at battery time:** the generic pilot on an
  unfamiliar deck plays on fallback scores, so it may be a *weak* archaludon pilot.
  Smoke n=4 had base 3W–1L — if the base sits near ~0.7+ at n=400 we inherit the
  same ceiling problem that retired `rule:dragapult` (0.78, ~0.22 headroom). The
  gate decodes the base-vs-ppo *delta*, but a ceiling still caps detectable gain.
  Flag if base WR is high; do NOT read absolute WR as archaludon strength.

**Re-launched (idempotent, warm-start m28_winners).** Exact resume command from
the checklist; startup confirmed clean: `baseline vs_teacher 5.5%`, `iter 0:
opponent mix [rocket=0.35, grim=0.35, mirror=0.30]` — pool parsed, archaludon
correctly ABSENT. Log `runs/m32b_ppo.log` (the cancelled iter-0 log preserved as
`runs/m32b_ppo_cancelled_iter0.log`). Hourly Hermes heartbeat attached
(`scripts/hermes_heartbeat.sh m32b …`). ETA ~2h.

**Re-spec'd kill-gate (fixed BEFORE the numbers; supersedes the dragapult gate
above).** Battery BOTH `model:m28_winners:clone54618168` (base) and
`model:ppo_current_m32b:clone54618168` (ppo), n=400 (2×200), two-proportion z on
(ppo − base):
- **PRIMARY (transfer):** `generic:archaludon` (SEALED). ppo beats base resolved,
  z > +1.96. z ≤ 0 → KILL (M23 no-transfer reproduced) → pivot to dragapult (A).
- **GUARD (non-inferior):** `rule:lucario` (out-of-loop). z > −1.96 — no
  catastrophic forgetting of a matchup we win. (This is the pre-registered mirror
  guard; `rule:lucario` is Mega Lucario, not the alakazam mirror.)
- **SANITY (trained took):** rocket clone AND grim clone ppo ≥ base; the true
  alakazam mirror (`model:m28_winners:clone54618168` as opponent) reported
  alongside.
- **FLOOR:** `random:kyogre` ≥ 0.90.
- Promising iff PRIMARY passes AND GUARD holds → QC + Piotr go. Else KILL.

New reusable tools: `scripts/m32b_battery.sh` (phases `primary` = arch+luc,
`sanity` = rocket+grim+mirror+kyo — run only after the collect frees the 8
workers), `scripts/m32b_decide.py` (two-proportion decode + the gate above),
`scripts/hermes_heartbeat.sh` (generic hourly heartbeat → Telegram).

## 2026-07-25 — M32-B KILLED: PPO-vs-exogenous DEGRADED both out-of-loop beds (M23 redux, 3rd time)

Run completed clean: 8/8 iters, exit 0, no errors; `defect/g 0.00` throughout;
KL anchor held (kl 0.041 → peaked 0.147 @ iter4 → settled ~0.093, coef 0.1).
Final `checkpoints/ppo_current_m32b.pt` (+ `ppo_best_m32b.pt`, unused — eval was
skipped by `--eval-every 99` per the M23 in-loop-eval law).

**Primary battery (n=400 = 2×200, two-proportion z, ppo − base):**

| bed | role | base | ppo | Δ | z | verdict |
|---|---|---|---|---|---|---|
| `generic:archaludon` | SEALED transfer | 0.860 | 0.8125 | −0.048 | **−1.82** | KILL (z ≤ 0) |
| `rule:lucario` | non-inf GUARD | 0.735 | 0.6475 | −0.088 | **−2.69** | BREACH (z ≤ −1.96) |

**Verdict: KILL.** The pre-registered primary rule (archaludon ppo > base, z >
+1.96) fails — ppo is *down*, not up. And the guard breaches: ppo lost ~9pp on
Lucario, a matchup we win. The fine-tune did not merely fail to transfer; it made
a **worse pilot on the out-of-loop meta.**

**The archaludon-ceiling caveat does NOT carry the conclusion — the Lucario
breach does.** As flagged pre-run, `generic:archaludon` is a weak-pilot bed: base
sits at 0.860 (the generic pilot can't pilot the archaludon deck, so we crush it;
this is NOT the live 0.47 archaludon matchup, which comes from skilled pilots).
Same defect that retired `rule:dragapult` (0.78). So the archaludon Δ alone would
be a soft read. But `rule:lucario` is our long-standing bed, identical
construction every milestone, out-of-loop, and unambiguous: z = −2.69 degradation.
The two agree on direction. Kill stands independent of the archaludon instrument.

**This is the M23 no-transfer law reproduced a THIRD time** (M22 same-deck pilots
0.17–0.22 vs rule:dragapult; M23 30-iter convergence flat/down out-of-loop; now
M32-B targeted-exogenous PPO down out-of-loop). Warm-start + KL-anchor + a pool of
the exact decks we lose to still does not buy out-of-loop strength on the Alakazam
pilot — it costs it. Sanity beds (rocket/grim/mirror/kyo) running to confirm the
signature (up on the trained pool, down elsewhere = clean overfit vs down
everywhere = broken run); decision is already KILL either way.

**Sanity beds (n=400) confirm the overfit signature — NOT a broken run:**

| bed | role | base | ppo | Δ | z |
|---|---|---|---|---|---|
| rocket clone | trained | 0.5625 | 0.615 | +0.053 | +1.51 |
| grim clone | trained | 0.6775 | 0.645 | −0.033 | −0.97 |
| m28 mirror | trained | 0.499 | 0.516 | +0.018 | +0.50 |
| random:kyogre | floor | 0.9725 | 0.9725 | 0.000 | 0.00 |

The fine-tune latched onto the **rocket** matchup alone (+5.3pp, and not even
resolved at n=400) while leaving grim/mirror flat and **degrading both
out-of-loop beds** (lucario −8.8pp z=−2.69, archaludon −4.8pp). Floor held. So
the pool training partially "took" (rocket), but bought a single trained matchup
at the cost of the meta we don't train on — the definition of overfit. `ppo_best_m32b`
is unused (eval skipped). **Net: `ppo_current_m32b` is a strictly worse ship
candidate than the frozen `m28_winners`.**

**M32-B CLOSED — NO SHIP.** Third reproduction of the M22/M23 no-transfer law:
warm-start + KL-anchor + a pool of the exact decks we lose to still does not buy
out-of-loop strength on the Alakazam pilot. The clean out-of-loop lucario breach
(instrument-independent) carries the verdict; the archaludon-ceiling caveat is
moot. Pre-registered pivot: the **dragapult archetype (A)** — but see the M32
endpoint note below: nearly every Alakazam-pilot lever is now measured-spent, so
the direction is a genuine strategic fork for Piotr (dragapult clone vs the
setup-speed lever vs rollback insurance to M30 54929991).

## 2026-07-25 — M32 endpoint: strategy review (docs/M32-strategy-review.md)

Piotr's call after the KILL: step back, no rollback (keep M31 live). Full review
in `docs/M32-strategy-review.md`. Headline finding (new, from an 851-game
card-type analysis): **the Darkness type wall is only 7.2% of games / 9.5% of
losses — the bottleneck is PILOT skill in even matchups (mirror 0.47, non-weakness
0.466), where every skill-adding tool we have (BC ~0.66 ceiling, PPO no-transfer
×3, O-rules ×5) is measured-capped.** Archetype-switching (A) escapes only the
~10% type wall and inherits the BC ceiling. Recommendation: highest-EV *cheap*
next step is a deck-tech early-dig probe (targets the best-powered under-dig
finding, does NOT require beating the BC/PPO walls); dragapult (A) and a
long-game value function remain on the table. Awaiting Piotr's pick.
