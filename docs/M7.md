# M7 — The Deck Factory Loop: implementation diary

**Status:** 🚧 Active (started 2026-07-09). Spec: [M7-plan.md](M7-plan.md).

## Why

M0–M6's measured conclusion (DECISIONS.md): the #1 bottleneck is opponent-field
diversity — every offline search overfits to 2 rule pilots + bc_v1 while the real field
is ~4,500 teams. M7.0 attacks it first because it's cheap, independent, and changes the
category of every downstream problem: Kaggle serializes both agents' actions per episode,
and the env contract makes each agent's *first* action its 60-card decklist — so our own
submissions' replays carry real opponent decks.

## Assumptions (tracked)

All three are schema bets that only the [NET] spike ([M7-manual-tests.md](M7-manual-tests.md) §1) can settle — this
sandbox has no Kaggle network access, so M7.0 landed as offline infrastructure + tests.

| # | Assumption | Test | Status |
|---|---|---|---|
| 1 | Decklists extractable: each seat's first recorded action is a 60-int list at `steps[0..2]` | `python -m rl.kaggle_ingest verify --episode <id>` hard-asserts 60 ints passing `validate_deck` | ⏳ [NET] |
| 2 | Endpoint reachable: `EpisodeService/{ListEpisodes,GetEpisodeReplay}` works for this competition (fallback: kaggle CLI + `import-file`) | `python -m rl.kaggle_ingest list --sub 54474043` | ⏳ [NET] |
| 3 | Opponent observations present mid-game (unlocks BC-on-winners; if blank, harvested decks are still the bigger win) | `verify` prints `opponent obs`; `bc-shards` audits | ⏳ [NET] |

Go/no-go (M7-plan §7): decklists extractable → full plan; only outcomes → keep
forensics, drop the meta-field/BC edges, recalibrate gates to the old anchor field.

## Design (M7.0 — `rl/kaggle_ingest.py`)

- **Fetch/parse seam.** `fetch_episode`/`list_episodes` are the only network
  touchpoints (lazy `requests`, injectable fetchers); everything downstream is pure and
  replays the immutable gzip cache `data/kaggle/raw/episode_<id>.json.gz` (atomic
  writes, ~1 req/s throttle, resumable). Parser fixes reprocess history for free.
- **Two payload shapes** accepted: `env.toJSON()` dicts and the `GetEpisodeReplay`
  `{"replay": "<json-string>"}` wrapper. `parse_episode` is schema-*tolerant*
  (missing decks/rewards → warnings, never crashes); the HARD asserts live in the
  `verify` command, because discovering the schema is the milestone.
- **`deck_hash`** = sha1 of sorted ids — the deck identity the M7.2 league imports.
- **Archetypes**: exact hash, then greedy agglomerative cosine (≥0.95) over the
  L2-normalized pooled FEAT of the Pokémon core (the representation the 98% probe
  validated). Deterministic ordering → stable labels across reruns. The FEAT matrix is
  re-derived from `cards_features.parquet` rather than imported from `rl/encoders.py`
  (encoders → `rl.combat` → `cg`; ingestion must run engine-free). Tech debt: extract a
  shared cg-free `rl/features.py` during the M7.3 encoders-v2 work.
- **Outputs**: `data/kaggle/episodes.parquet` (outcomes + schema evidence per episode),
  `data/kaggle/opp_decks.parquet` (per-seat decks + archetypes), frozen
  `data/kaggle/meta_v<N>/` snapshots (validated deck csvs + manifest), and
  `build_meta_field` → `("generic", deck_csv_path)` specs matching the collector's
  picklable-tuple convention (the M7.2 matchrunner resolves name-or-path).
- **Honest scoping**: `bc_shards_from_opponents` is an *audit* only (encoding needs a
  confirmed obs schema + the engine); `forensics` defers first-KO for the same reason.

## Milestones

- **M7.0 — Ingestion spike.** Infrastructure + 21 offline tests ✅ (2026-07-09);
  [NET] verification pending (manual tests §1).
- **M7.1 — Deck factory.** `rl/deck_build.py` + shells v1 + `field_fitness` ✅
  (2026-07-09); the [ENGINE] gate (≥1 generated deck ≥50% vs the Lucario deck
  same-pilot, n=400) NOT passed on the first league run (manual tests §2).
- **M7.2 — League + gates.** ✅ COMPLETE (2026-07-10): self-test gate PASSED on the
  real engine — anchors reproduce the M6 ordering at n=1600/anchor (results log).
- **M7.2b — Race-math lookahead L1.** Combat race primitives + pilot rewiring ✅
  (2026-07-09); measured: floor 0.695 ❌ / vs-expert 0.305 ✅ (manual tests §4).
- **M7.3 — Deck-conditioned pilot v1.** Infrastructure ✅ (2026-07-10): encoders
  v2 + `OptionScorerV2` + generic-teacher BC pipeline + population export; the
  [ENGINE] collect/train/gate loop is manual tests §5.
- **M7.4b — PPO retry (offline half).** ✅ landed (2026-07-10): the historic
  loss bug FIXED in both twins (was `policy_loss * VALUE_COEF * value_loss`),
  critic warm-start, collector on matchrunner with multi-deck self-play +
  generic pool + optional race shaping, PPO v2-aware; the [ENGINE] smoke/run
  is manual tests §6.
- M7.4a combo solver · M7.5 counter-meta ship loop — see M7-plan.md §7.

## Design (M7.1 — `rl/deck_build.py` + `field_fitness`)

Two data facts unlock an engine-free factory (both pinned in tests):

1. **Evolution is by NAME, not card id.** `evolves_from_id` points at one specific
   printing, but decks legally play any same-named card — the tuned Lucario deck runs
   Riolu #677 (80 HP) while Mega Lucario ex's `evolves_from_id` is Riolu #974 (70 HP).
   Lines are walked by name, taking the highest-HP printing of each pre-evolution.
2. **`attackId` ↔ card via cumulative `n_attacks`** in card_id order (the parquets were
   written in one pass; verified: all 1,556 attacks assign with zero `max_damage`
   mismatches). This gives per-attack damage AND cost without the engine —
   `min_attack_cost` alone is the *cheapest* attack, the wrong denominator for
   damage-per-energy (Mega Lucario: min cost 1 = Aura Jab 130, best = Mega Brave 270/2).

Pipeline: `enumerate_lines()` (complete basic→final chains, 1–3 stages) →
`score_line` = damage-per-energy × weakness-coverage − prize-liability − setup-cost
(setup penalty strong on purpose: M6's floor-test weakness is slow setups; variable
attacks discounted 0.5 — printed damage is best-case) → shells v1 mined from the known
decks (`data/shells.json`; `harvested_shell()` is the §2.3 source-3 hook, live after
the [NET] spike) → `build_deck` (4-3 / 4-3-2 line counts, 10–14 matched basics by
best-attack cost, dual-type split gives a coverage tech 4 basics, shell fills, energy
pads — legal by construction, asserted) → `generate` round-robins the top-K lines ×
shells × {no-tech, coverage-tech}, deduped by `deck_hash`, into `decks/gen/` + manifest.

`rl/deck_search.field_fitness(deck, field, games_per_opp, pilot)` rates candidates
vs the diverse FIXED anchor field, never mirror-only (the documented mirror-overfit
failure). `OpponentSpec` tuples now cover rule/model/generic/random with name-, path-,
or ids-valued decks; the battle loops graduate to `rl/matchrunner.py` in M7.2.

First batch (pool-histogram meta, since ingestion hasn't run): 40 legal candidates
across 10 archetypes — top lines Ceruledge, Palafin ex, Decidueye, Greninja ex,
Medicham; Mega Lucario ex ranks lower under the strong setup/prize penalties (270 dmg
but cost 2 + 3-prize liability).

## Design (M7.2 — `rl/league.py` + `rl/matchrunner.py`)

**Match runner** (`rl/matchrunner.py`) is now the canonical home of the
`OpponentSpec` vocabulary and the slot-fair direct-engine battle loop —
`deck_search.matchup`/`_play_vs_spec`/`_spec_pilot`/`_resolve_deck` are thin
delegating wrappers (signatures unchanged), the collector migrates in M7.3
together with per-game deck sampling (it layers tensor recording on top of
match running), and `eval.play_games` stays on kaggle_environments because it
exercises the SHIPPED file agents. Parallelism follows the collector pattern:
spawn Pool over even-sized game chunks (~2 jobs/worker, only str/int tuples
pickled); `workers<=1` runs in-process, which is also the injected-`game_fn`
test seam. `_engine_game` optionally accumulates per-seat moves/time/errors —
the G1/G6 gate inputs come from the same games as G2–G4, no second pass.

**League** (`rl/league.py`, persisted at `data/league/league.json`): a
PlackettLuce table over (deck, pilot) entries. Decisions worth recording:

- **Frozen ≠ fixed rating.** Anchors are permanent members excluded from
  candidate scheduling/promotion, but their *ratings* update — the self-test
  needs anchors rated among themselves; the scale is pinned by the population
  being permanent, not by freezing mu.
- **Draws** skip the rating update (the `rate_population` convention) but count
  toward `games` and as half a win in win rates (the `field_fitness` convention).
- **Specs are normalized on add**: every entry's deck slot is rewritten to its
  registered `data/league/decks/<hash>.csv`, so league.json round-trips cleanly
  (JSON lists → tuples on load) and specs stay hashable.
- **Gates are data**: `gate --entry <id>` writes
  `data/league/gates/<entry>.json` — per-gate `{threshold, n, value, pass,
  date}` plus the field version and `n_scale` (smoke runs are honestly
  labeled). G5's fixed reference opponent (`generic+lucario`) is recorded in
  the file. Ship-eligibility (`ship_eligible`) is pure arithmetic; the `rl.gate`
  bundle suite stays a separate manual step.
- **Scheduling**: candidates play every anchor (40 games each) until their
  anchor schedule is done, then their 3 nearest-ordinal peers. Interrupted runs
  may replay some anchor games — extra n only sharpens ratings.

## Design (M7.2b — race-math lookahead L1)

The deck-agnostic port of the experts' planning edge, targeting M6's measured
floor-test failure (the pilot self-decks won games: ~1 KO/turn while milling).
Key discovery: the Lucario expert's `AttackPlan` is a *single-turn* attack
commitment — its multi-turn behavior emerges from per-turn re-commitment plus
energy banking. So L1 ports **race math**, not a plan object:

- **Primitives** (`rl/combat.py` ↔ `tcg/combat.py`, pure Python — both files
  ship verbatim in the rule bundle): `charged_best` (best attack at FULL charge;
  `best_damage`'s affordable-only view is why the pilot stopped charging once
  the *cheapest* attack was paid), `turns_to_ready` (total-cost energy gap,
  attach-1/turn), `hits_to_ko`, and `turns_to_first_ko = max(gap,1) + hits − 1`
  (an attacker one energy short fires THIS turn — the same semantics as the
  pinned 4000 unblock tier). `UNREACHABLE(_TURNS) = 99` sentinel.
- **Scorer rewiring** (both pilots + `tcg/constants.py`, lockstep for parity):
  attach now bonuses my fastest closer (2750 active / 2680 bench — the bench
  closer deliberately outranks the KO tier: the attach doesn't end the turn, the
  KO fires on re-prompt); "already loaded" means the BEST attack is charged;
  promote subtracts 50 per attach still needed (capped ×4); and **close mode**:
  when the opponent board is harmless (every Pokémon known + zero printed
  damage), chip attacks jump above trainers (2300+) so the pilot attacks every
  turn instead of milling. Close mode v1 is deliberately conservative — it's
  exactly the floor case and (almost) never fires vs a real opponent, so the
  >30% expert gate can't regress through it; the full prize-race comparison is
  deferred.
- **Floor deck committed**: `decks/floor_zero_damage.csv` (4× the highest-HP
  zero-damage basics + energy; `python -m rl.deck_build floor` regenerates it) —
  M6's ad-hoc floor measurement is now reproducible.
- **Deferred**: k-turn `encode_state` features land in encoders-v2 (M7.3) — v1
  must stay byte-identical (bc_v1/value artifacts + shipped-npz parity, risk 4).

## Design (M7.3 — deck-conditioned pilot v1)

The core bet (M7-plan §3.1b): the generic pilot is a *function of observable
state* — unlike the Lucario expert's hidden AttackPlan there is no invisible
reasoning to alias, so BC from it across MANY decks should reach high fidelity
and produce a pilot ≈ teacher strength but *differentiable* (improvable by
PPO/AlphaZero where the frozen rule pilot is not).

- **Encoders v2** (`encode_state_v2`/`encode_option_v2`, both twins, v1
  byte-identical): return (numeric, ids). Numeric = v1 + the k-turn race block
  (8 features on the M7.2b primitives — turns-to-ready/turns-to-first-KO both
  ways + race delta, the M7.2b deferral) + two deck-context pools (full 60-list
  and remaining-deck FEAT sums — pooled, NOT per-id counts, the
  reverted-feature lesson; needs the pilot's own deck list, which observations
  don't carry — pilots close over it). Ids = 12 board-slot card ids + the
  option's acted/target ids, feeding a learnable `nn.Embedding(1268, 16,
  padding_idx=0)` — row 0 aligns with FEAT's "no card" padding row. The 36
  features are nearly blind for trainers; embeddings learn per-card behavior.
- **`OptionScorerV2`** (both twins): the v1 pointer architecture with embedded
  ids concatenated onto both branches. `save_npz` unchanged (the embedding is
  one more matrix; inference is a row lookup). `matchrunner.make_pilot`
  auto-detects v2 checkpoints (`embedding.weight` in the state dict).
- **BC pipeline**: `bc collect --teacher generic --decks
  data/league/population.json` — per game each seat samples its OWN deck from
  the population, BOTH seats are recorded (both are the teacher); shards gain
  `state_ids`/`option_ids`/`deck_idx` columns (deck_idx = the deciding seat's
  population index, for held-out-deck splits). `bc train --arch v2` trains
  `OptionScorerV2` and reports overall + per-deck val accuracy (the G5
  deck-conditioning signal). `league population` exports the deck list (top
  league candidates; decks/gen manifest fallback).
- **Deferred**: collector/PPO per-game deck sampling + matchrunner migration
  move to M7.4b with the PPO retry (the M7.3 gate is BC-only); the shipped-npz
  v2 export/submission update happens at ship time via the `rl.gate` suite.

## Results log

- **2026-07-10 — M7.4b offline half landed + THE PPO LOSS BUG FIXED.** The
  long-suspected quirk was real: `rl/ppo.py`/`tcg/ppo.py` computed
  `loss = policy_loss * VALUE_COEF * value_loss − entropy·c` — a multiplicative
  objective whose policy gradient scales with (and flips sign with) the value
  error. Every stalled PPO run (M2–M4) trained on it, so "PPO failed" was never
  an honest measurement. Fixed to the documented additive form in both twins; a
  new test pins the corrected form literally. Also: critic warm-start
  (`--value-ckpt`, loads the proven supervised value head), collector migrated
  onto `matchrunner.make_pilot` (generic + v2 + pre-M3 opponents for free),
  per-game learner deck sampling (`--decks population.json`, required for v2
  checkpoints; shards gain state_ids/option_ids/deck_idx), optional
  potential-based race shaping (`--race-shaping`), and PPO training is
  v2-aware end to end (`--start osv2_bc.pt` works). Suite 320 green. All manual
  runbooks consolidated into [M7-manual-tests.md](M7-manual-tests.md).
- **2026-07-10 — M7.3 infrastructure landed.** Encoders v2 (side-by-side, v1
  byte-identical incl. the COMBAT_SLICE compat contract), `OptionScorerV2`
  (embeddings; overfit-tiny-batch sanity green), generic-teacher BC collection +
  `BCDatasetV2`/`collate_v2`/`train_v2` with per-deck fidelity, `league
  population` export (committed: 3 known + top-10 factory decks),
  v2-checkpoint-aware `make_pilot`. ~20 new tests. [ENGINE] loop via runbook.
- **2026-07-10 — M7.2 SELF-TEST PASS + first full league run (real engine, n=1600/anchor).**
  Anchor ordering reproduces M6 exactly under the shared runner:
  lucario_expert 48.61 ≈ iono_expert 48.42 > tuned 45.82 > generic+lucario 35.23 >
  bc_v1+kyogre 26.97 > generic+iono 23.20 > random+kyogre 22.55 (ordinals).
  The expert≈tuned tie and bc_v1-between-generic-decks both replicate; generic+iono
  sitting barely above random re-confirms the M6 legibility finding from the rating
  side. **M7.1 verdict from the same run (40 template decks, 280 games each):**
  best = Diggersby+iono_engine (`deck_cc1cce15c5f4`, ordinal 26.25, mu 31.72) — above
  the generic+iono and random anchors and at bc_v1's level, but ~9 mu below
  generic+lucario: template-only decks don't reach human-tuned parity, so the M7.1
  gate is NOT passed yet (0 decks above the anchor-field median 35.23; the ≥50%-vs-
  Lucario promote check at n=400 still worth running on the top deck). Patterns in
  the batch: the iono_engine shell dominates the top ranks, no-tech beats the
  dual-type tech variants of the same line (the 4-energy split costs consistency),
  and Diggersby (cost-1 140-dmg colorless) is the factory's best find. Next lever
  per plan §2.4: hill-climb refinement of the top templates + harvested-meta
  targeting after M7.0.
- **2026-07-10 — Post-fix measurements + two portability bugs fixed.** Re-run after the
  starvation fix: floor **0.695** (still FAIL — awaiting `--diag` loss signatures),
  vs expert **0.305** (PASS), vs random **0.855** (recovered from 0.795). Two crashes
  found by the Windows run, both fixed: (a) **bc_v1 is a pre-M3 checkpoint** — its state
  input is exactly `N_COMBAT` narrower than the current encoder (1270 vs 1281); the
  model loader is now dimension-aware and slices the combat block (`COMBAT_SLICE`,
  pinned against `encode_state` in tests) out of the encoding to reconstruct what
  bc_v1 was trained on. (b) **league.json carried absolute sandbox paths** — deck and
  checkpoint slots are now stored ROOT-relative (POSIX), `resolve_deck`/`make_pilot`
  resolve them against the repo root from any cwd/OS; the committed league.json is
  regenerated. NOTE for existing checkouts: discard the local league state once
  (`git checkout origin/<branch> -- data/league/league.json`, then re-run `add-batch`).
- **2026-07-09 — M7.2b gate measurements (real engine, user's machine) + starvation fix.**
  Gate 2 **PASS**: vs Lucario expert **0.329** (n=400; > 0.30, up from ~0.25–0.30).
  Gate 1 **FAIL**: floor **0.715** (n=200, 0 draws; need ≥ 0.90) — the floor deck can't
  KO, so all 57 losses are self-deck-outs: the mill came back. Sanity vs random:kyogre
  0.795 (depressed). **Diagnosis (confirmed against card data):** the bench race-closer
  attach tier (2680) outranked attach-active (2600), so with the closer benched (Mega
  Lucario behind Riolu — the standard opening) every energy went to the bench; Riolu
  needs 1 energy to attack and 2 to retreat, so no ATTACK option ever appeared (close
  mode had nothing to boost), trainers (2200) fired every turn, mill. **Fix:** the bench
  tier now requires an attack-ready active (`turns_to_ready(active) == 0`) — feed the
  active first, then bank the closer. Also: `matchrunner play --diag` (per-game
  end-state loss forensics) and `league add-batch --dir` (PowerShell-safe enrollment).
  Awaiting re-run.
- **2026-07-09 — M7.2b race-math L1 landed.** Race primitives in both combat
  cores, scorers rewired in both pilots (attach race winner, best-attack
  loading, promote race term, close mode), `decks/floor_zero_damage.csv`
  committed with its builder. 17 new tests incl. an extended >400-scenario
  parity sweep; 3 pinned tests updated deliberately (close mode, best-attack
  loading, promote race term). Suite 294 green. [ENGINE] gates via runbook.
- **2026-07-09 — M7.2 league + match runner landed.** `rl/matchrunner.py` (canonical
  OpponentSpec home + slot-fair loop + spawn-Pool `run_pairs`), `rl/league.py`
  (persistent openskill table, 7 anchors bootstrapped, gates-as-data G1–G6,
  deck promotion, submissions log, anchor self-test). deck_search battle loops now
  delegate. 35 new tests (277 total green). `data/league/league.json` initialized
  with the anchor field. [ENGINE] self-test pending via runbook.
- **2026-07-09 — M7.1 deck factory landed.** `rl/deck_build.py` (lines / shells /
  generate CLI) + `field_fitness`/`OpponentSpec` in `rl/deck_search.py`; 15 new tests
  (238 total green). `python -m rl.deck_build generate --n 40` emits 40 legal deduped
  candidates across 10 archetypes to `decks/gen/`. The M7.1 gate is [ENGINE] — runbook
  below.
- **2026-07-09 — M7.0 infrastructure landed.** `rl/kaggle_ingest.py` (fetch/cache/
  throttle, tolerant parser, harvest + archetype clustering, meta snapshots, forensics,
  bc-audit, 9-command CLI) + `tests/test_kaggle_ingest.py` (21 tests, canned
  env.toJSON()-shaped fixtures using the real repo decks). Full suite 223 passed.
  Offline degradation verified: every network path raises a `[NET]` error pointing here.

## Runbooks

All manual [ENGINE]/[NET] tests — commands, gates, tunable hyperparams, and the
math behind each — are consolidated in **[M7-manual-tests.md](M7-manual-tests.md)**
(single source of truth as of 2026-07-10; the per-milestone runbooks that used to
live here moved there).


## Notes

- `data/kaggle/raw/` is gitignored (re-fetchable, potentially large); the derived
  parquets and `meta_v<N>/` snapshots are committed — they're the frozen inputs other
  milestones cite.
- `data/bc_v2/` shards are local artifacts (gitignored with the other data shards).
