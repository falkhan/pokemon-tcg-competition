# M24 — replay-BC as the primary signal (the pipeline inversion)

Plan: `docs/M24-plan.md`. Antecedents: `docs/M23.md` (signal audit, 18:27 verdict),
`docs/m23-signal-audit-plan.md` (✅ complete). Champion pins at kickoff: three live arms —
B2 54846434, B3 54849475, M22c-RL 54864190 (all lucario) + M20 champion 54836093.
Reference pins: E1 `bc_clone_v3_54618168` 0.5625 pooled n=400 vs `rule:lucario`; V2 clone
0.59 n=100; out-of-loop clone lineage 0.20–0.25 vs `rule:dragapult`.

Piotr's scoping calls (2026-07-21 evening): both-decks A/B (clone 60 `9294d9d8…` vs our
lucario 60 `20dcd313…`); corpus expand now; no early clone ship — one ship per milestone,
after gates.

## 2026-07-21 19:02 — Phase 0.1 snowball COMPLETE (parquet 2943 → 3943)

`kaggle_ingest refresh` over our 4 live subs + top-10 opponent subs (≥1140 score),
max-new 1000, all 1000 fetched clean (decks 2/2, obs present, 0 warnings), harvest re-run
(`runs/m24_snowball.log`). Per-target episode coverage after:

| sub | score | episodes |
|---|---|---|
| 54618168 | 1251.4 | **373** (was 351) |
| 54773249 | 1171.0 | **237** |
| 54834745 | 1190.0 | **221** |
| 54840044 | 1152.9 | **211** |
| 54827443 | 1191.3 | **188** |
| 54826859 | 1165.2 | **176** |
| 54837973 | 1174.1 | 46 |
| 54829486 | 1180.2 | 5 |
| 54703548 | 1141.4 | 1 |
| 54833827 | 1140.0 | 2 |

Six subs clear the ≥150-episode bar for per-teacher corpora. The max-new budget starves the
tail as in M23 — recorded, not hidden; a second refresh round is cheap if Phase 1 wants the
missing four.

## 2026-07-21 ~19:05 — Phase 0.2 corpora builds launched

`scripts/m24_build_corpora.sh` (`runs/m24_corpora.log`): per-teacher corpora
`data/bc_m24_<sub>` for the six ≥150-episode subs (min-score 600) + the our-deck corpus
`data/bc_m24_ourdeck` (`--deck-hash 20dcd3130bc0`, min-score 900 — the M24 filter landed
this session with suite 607 green; its live validation is THIS run, same ritual as the M23
`--only-subs` filter). `teacher_score` is stored per row, so train-time re-filtering needs
no rebuild. Deck hashes pinned: ours `20dcd3130bc0b81d865aaf1475caf4438ab2d194`, clone
`9294d9d8360aa2a637665cbd20b5a76b71da5e04`.

## 2026-07-21 ~19:25 — Phase 0.2 corpora DONE + 🔴 a deck finding that reshapes the A/B

Six per-teacher corpora built (~108k decisions total, 4× the E1 corpus):

| corpus | teacher (score) | decisions | deck |
|---|---|---|---|
| bc_m24_54618168 | 1251.4 | 28,651 | `9294d9d8` (the clone deck) |
| bc_m24_54773249 | 1171.0 | 18,443 | **`9294d9d8` — same deck, independent team** |
| bc_m24_54827443 | 1191.3 | 16,858 | `f2b4039a` |
| bc_m24_54834745 | 1190.0 | 14,897 | `3394cd30` |
| bc_m24_54840044 | 1152.9 | 14,927 | **`3394cd30` — same deck as 54834745** |
| bc_m24_54826859 | 1165.2 | 14,038 | `475928aa` |

**The our-deck corpus is EMPTY — and that is the finding, not a bug.** The `--deck-hash`
filter validated live (3,295 candidate seats ≥900 score checked, all other decks): **no
pilot above score 820 plays our exact 60.** The seats playing `20dcd313` at volume are our
own ships plus 1-game strays. Every 1100+ pilot plays a tuned variant. Max observed live
scores per list: our 60 → **820.1**; clone 60 → 1251.4; `3394cd30` → 1200.0; `f2b4039a` →
1194.5; and a deck we had NOT harvested, `3121746f`, reaches **1268.4** (a sub cluster
54861775/54863653/54861685 — one team iterating; targeted snowball round 2 launched,
`runs/m24_snowball2.log`).

Consequences: (a) the C-ours arm cannot be sourced from strong-pilots-on-our-deck — it
falls back to zero-shot gating of a mirror-archetype pilot on our 60, per plan; (b) the
evidence that our stock list is itself a limiter just got stronger — the A/B (Piotr's call)
is now also a deck experiment with real stakes; (c) two teachers on the SAME clone deck
(54618168 + 54773249, 47k pooled decisions) allow a same-deck two-teacher pool — trains
launched as `m24_bc_54618168` (single-teacher refresh) and `m24_bc_pool9294` (pooled;
M10's mixed-teacher caveat applies to unobservable MIXED policies — two consistent strong
pilots on one deck is the benign case, and the A/B between the two variants measures it).

## 2026-07-21 ~19:45 — Phase 1 screens: 0.660 record; zero-shot deck transfer FAILS

Fidelity: single-teacher 0.655 / pooled 0.654 val acc (≈ E1's 0.657 — stable).
Screen grid n=200 seed 1 vs `rule:lucario` (`runs/m24_scr_*.jsonl`):

| candidate | clone deck | our deck (zero-shot) |
|---|---|---|
| `m24_bc_54618168` (single-teacher, 373 eps) | **0.660** | 0.228 |
| `m24_bc_pool9294` (two-teacher, 47k decisions) | 0.570 | 0.310 |

- **0.660 is the strongest number in campaign history** (E1 pin 0.5625, V2 clone 0.59;
  n=200 ±~7pp so the delta vs E1 is borderline — seed 2 in the battery decides).
- **Pooling cost on-deck strength (−9pp) and bought cross-deck generality (+8pp)** — M10's
  caveat reproduces mildly even in the benign same-deck case. Single-teacher is the arm.
- 🔴 **Zero-shot transfer to OUR deck collapses to 0.228/0.310 — WORSE than our live PPO
  lineage (0.32–0.37).** Combined with the 820-score ceiling on our stock 60, the C-ours
  arm as scoped (replay-BC pilot on our deck) is dead: deck+pilot are a unit. The
  our-deck comparison already exists on the ladder — the three live arms ARE our-deck
  agents. Ship-shape decision goes to Piotr after the full battery.

Full battery launched on the winner (`runs/m24_p1_battery.log`): `rule:lucario` seed 2,
`rule:dragapult` n=200×2, `random:kyogre` n=200×2, latency (workers=1, n=20).

## 2026-07-21 ~19:46 — 🟢🟢 full battery: every gate green, two campaign records

`m24_bc_54618168` piloting the clone deck (`runs/m24_bat_*.jsonl`, 0=WIN decode):

| gate | s1 / s2 | pooled | best prior reference |
|---|---|---|---|
| `rule:lucario` | 0.660 / 0.550 | **0.605** n=400 | E1 0.5625; V2 clone 0.59; our PPO lineage 0.32–0.37 |
| `rule:dragapult` | 0.305 / 0.275 | **0.290** n=400 | M22c-RL 0.1888 n=800 (lineage 0.17–0.22); clone line 0.20–0.25 |
| `random:kyogre` | 0.955 / 0.945 | 0.950 | best pins 0.92–0.935 |
| latency | mean 0.7ms · p99 1.4ms | — | ~52ms ship reference |

- **vs the sample agent: 0.605 pooled — ABOVE company-agent parity** for the first time.
  Seed spread 11pp (n=200 seeds — noted, pooled is the read).
- **Out-of-loop: 0.290 pooled n=400 — the best dragapult number the campaign has ever
  measured**, +10pp over the 0.1888 champion pin (comfortably outside the ~8pp MDE at
  these n's, and this gate is doubly clean — never trained against, different deck).
  Still below the sample agent's ~0.50; the replay corpus carries on-distribution strength
  plus a real but partial generalization gain (373 episodes > 348 helped, or the fresh
  rebuild — not decomposable from here).
- Floors best-ever, latency trivial (pure BC forward, no search).
- Zero training on our own self-play — the entire gain is the exogenous signal. The M23
  audit's prescription, validated end-to-end in one evening.

Ship decision → Piotr (standing policy: explicit call + deck md5 ritual +
verify-before-consequential-actions review). Snowball round 2 (the 1268-score deck
cluster) still fetching — a possible second/stronger teacher for a follow-up arm.

## 2026-07-21 ~20:03 — 🚀 SHIPPED: sub 54885173 (m24_bc_54618168 + clone deck) — after the ritual caught a third fossil

Piotr's explicit ship call. **The ritual earned its keep AGAIN, pre-burn this time:** the
first build used a typo'd deck name (`clone54168`) and `deck_source`'s silent root-deck
fallback produced a BUILD-OK tarball carrying the **kyogre fossil** (tarball deck.csv md5
`910ece3f` ≠ `8e8cf124`) — the exact M18.1/M22c failure shape, caught by the mandatory
tarball-md5 check BEFORE submission. Durable fix landed: `deck_source`/`_deck_src` (both
parity twins) now **hard-error on an unresolvable deck name** — the M18.1 guard checked
`--deck` presence, not resolution; that hole is closed, parity test updated, suite 607.

Verified ship (`dist/submission_neural_20260721_200216.tar.gz`, 5138 KB):
- export line names the pairing: `m24_bc_54618168.pt (OptionScorerV3) paired with deck
  'clone54618168'`; parity 7.45e-08; isolation + deck-legal + gate game OK
- tarball deck.csv md5 **8e8cf124** = clone deck ✓; `policy_weights.npz` byte-equal to the
  gated checkpoint (state_enc 256×1580, 12-id V3) ✓
- **Kaggle sub 54885173**, PENDING at μ=600 (not a result; settles ~a day)

Live arms now: 54885173 (M24, clone deck) + 54864190 / 54849475 / 54846434 / 54836093
(all our-deck lineage — the de-facto our-deck side of Piotr's A/B). Next: wire 54885173
into the monitor notebooks; 3121746f-cluster corpus (snowball2 landed 400 eps) is the
follow-up teacher candidate.

## 2026-07-21 ~20:30 — 🔴 CORRECTION (Piotr): the shipped deck is an ALAKAZAM deck

Piotr flagged it from the live replays; verified against the decklist card ids: the clone
deck (`9294d9d8…`, now `decks/clone54618168.csv`) runs **4× Abra/Kadabra/Alakazam +
3-2 Dunsparce/Dudunsparce + Fezandipiti ex + Shaymin** — a psychic Alakazam deck, mostly
single-prize. **The harvest archetype label for sub 54618168 ("mega_lucario_ex+solrock")
was WRONG**, and this session propagated it ("mirror archetype", "tuned lucario 60") from
M23 onward. Labels lie; card ids don't — the M18 lesson, caught again by Piotr, not by me.

What this changes (and doesn't):
- Every gate NUMBER stands — the batteries measured this exact deck+pilot bundle.
- The INTERPRETATION improves: 0.605 vs `rule:lucario` was won **off-archetype into the
  lucario field**, not in a mirror; and the mostly-single-prize board means opponents'
  Mega KOs cash 1 prize while ours cash up to 2+ — a structural prize-trade edge that may
  be part of WHY the top teams play this line. 54773249 (1171) plays the same 60 → two
  independent top teams on Alakazam; the strong end of the ladder is less mirror-shaped
  than the field-wide 76% figure suggested.
- M23's "mirror-meta evaluator" framing for the clone was archetype-wrong (its VERDICT —
  strong exogenous evaluator, not a dragapult substitute — is unchanged).
- Monitors corrected: `model_monitor` DECK_META now says Alakazam (🔮, psychic; sprite
  slot ready for `notebooks/sprites/alakazam.png`), MODELS + A/B-monitor arm labels
  updated. Deck resolution itself was never wrong (it matches the csv byte-for-byte).
- Follow-up: audit the harvest archetype labeler against decklist card ids for the other
  corpus teachers (M25 stub — the 3394cd30 and f2b4039a lists need the same check before
  any archetype claim is made about them).
