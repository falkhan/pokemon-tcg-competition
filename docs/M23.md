# M23 — converge against strong opponents, then decide the architecture

Plan: `docs/M23-plan.md`. Antecedents: `docs/m22-post-mortem.md`, `docs/M22.md`.
Champion pins at kickoff: three live arms — B2 54846434, B3 54849475, M22c-RL 54864190
(all lucario). Out-of-loop baselines vs `rule:dragapult`: M22c-RL 0.1888, B3 0.172 (n=800 2-seed).

## 2026-07-21 ~09:30 — Phase 0 landed (seat fix, dragapult guard, supporter counter)

Suite **605 green** (was 574). Three instrument changes:

1. **`play_series` seat defect fixed** (`rl/matchrunner.py`): a `game_fn` that declares
   `a_seat` now receives side a's seat each game (inspected once; 5-arg callbacks untouched).
   `rl/behavior.py::from_series` instruments `seat == a_seat` instead of the hardcoded seat 0
   that recorded the OPPONENT half the time. Regression test pins the alternation.
2. **Dragapult hard guard** (`rl/collector.py::_assert_dragapult_held_out`): `parse_pool` now
   raises on any spec that is the dragapult rule agent or resolves to `decks/dragapult.csv`
   under any pilot; `ALLOW_DRAGAPULT_TRAINING=1` is the deliberate override. The reference deck
   is read straight from the CSV (not via `resolve_deck`, which is a monkeypatched test seam).
   Convention → hard error, per the M18.1/M22c lesson.
3. **Supporter-play availability counter** (`rl/behavior.py::judge_supporter`): per-our-turn
   buckets t1–t6 + late; deliberately an AVAILABILITY rate (the module's one sanctioned
   exception to the strictly-worse-denominator rule) whose only sanctioned use is a same-deck
   contrast vs a reference pilot; the caveat prints unconditionally and a test pins it.

## 2026-07-21 ~09:45 — 🔴 the draw-engine gap is real: 2× supporter usage gap from turn 3

First seat-correct diagnostic, n=200 games each (`runs/m23_supporter_{model,sample}.log`):
`model:ppo_current_m22cRL:lucario` vs `rule:lucario`, and `rule:lucario` vs itself (the
reference is the pricing).

Raw per-STATE availability rates are similar between the pilots — but per-state rates conflate
dithering with playing (each MAIN prompt before the supporter is played is another candidate
state). The clean readout is **taken-count per game**, because `supporterPlayed` flips after one
play, so `taken` fires at most once per our-turn: taken/games ≈ "played a supporter that turn".

| our turn | model (m22cRL) | sample agent | gap |
|---|---|---|---|
| t1 | 107/200 = 0.54 | 133/200 = 0.67 | −0.13 |
| t2 | 109/200 = 0.55 | 150/200 = 0.75 | −0.20 |
| t3 | 69/200 = 0.35 | 145/200 = 0.73 | **−0.38** |
| t4 | 68/200 = 0.34 | 147/200 = 0.74 | **−0.40** |
| t5 | 68/200 = 0.34 | 160/200 = 0.80 | **−0.46** |
| t6 | 32/200 = 0.16 | 118/200 = 0.59 | **−0.43** |

**From turn 3 onward the sample agent plays a draw supporter roughly twice as often as our
model.** This matches the m22 shutout anatomy (hand empty from turn 4, Carmine ×3 unplayed)
and gives Phase 1 a concrete behavioral needle to watch: if training vs the strong teacher
teaches the draw engine, this table converges toward the reference.

Caveats, recorded now: (a) not every game reaches t5/t6 and game lengths differ between the
two series, so late-turn denominators of 200 overstate the base slightly — the t3–t4 gap is
the load-bearing one; (b) the model's opponent was `rule:lucario` while the reference played
its own mirror — same decks both sides in both series, but not an identical opponent
distribution; (c) availability (supporter actually in hand) is not conditioned out — if
anything that biases the model's rate UP (unplayed supporters accumulate in hand).

Also of note: seat-correct gust/retreat rates on series play — model gust strict 10/191
(0.052), sample 20/541 (0.037); retreat strict model 14/274 (0.051), sample 37/1141 (0.032).
The sample agent is not out-gusting us; its edge is not there.

## 2026-07-21 ~10:00 — rl/ppo.py promotion regated on vs_teacher (Piotr-approved)

`rl/ppo.py:460-467, 508-521`: the promotion baseline and gate now use the TEACHER
(`rule:lucario` sample agent) at `--eval-games`; vs_solver is still measured and logged every
eval but no longer gates. The teacher eval n rises from the hardcoded 100 to `--eval-games`.
Per the M20 law this stays a trigger, never evidence. Suite green after the edit.

## 2026-07-21 ~10:10 — smoke leg caught a config bug; then verified the whole path

`scripts/m23_phase1.sh` (the saved pipeline) smoke run (1 iter × 40 games, tag m23smoke):

- **First smoke FAILED usefully**: `rule:tuned` defaults its deck slot to "tuned" and
  `decks/tuned.csv` does not exist — the tuned sample agent plays the lucario deck. Fixed to
  `rule:tuned:lucario` in the script. Exactly what the smoke exists to catch.
- **Second smoke green end-to-end**: baseline printed `vs_teacher 35.0%` (promotion edit
  live), mixture parsed with no dedupe warning, `kl_coef 0.000` with no anchor built, no
  gust-boost line in collect, plan head training (plan_loss present), and iter 0 promoted on
  `vs_teacher 40.0% > 35.0%`. Smoke checkpoints deleted BEFORE the real launch — `past=`
  globs `ppo_*it*.pt`, so a leftover `ppo_m23smoke_it0000.pt` would have leaked into the real
  run's self-play pool.

## 2026-07-21 10:15 — Phase 1 leg LAUNCHED (tag m23p1)

20 iters × 400 games from `ppo_current_m22cRL`, pool `rule:lucario=0.45 rule:iono=0.15
rule:tuned:lucario=0.05 mirror=0.20 past=0.10 random:kyogre=0.05`, KL=0, no gust-boost,
`--eval-every 1 --eval-games 200`, workers 8. Log `runs/m23p1_pipeline.log`; heartbeat
reporter attached. ETA ~2h.

## 2026-07-21 ~10:15 — Phase 3 groundwork (parallel, no training-box conflict)

- `bc-shards` audit: **3468 opponent seats ≥600 score, all 3468 with (observation, action)
  streams** — volume exists in principle, but per-sub coverage is the binding constraint.
- `replay_bc build --only-subs` filter landed (the clone-one-opponent path; registry params
  record it). Suite 605 green. `build` had zero prior test coverage; the filter's validation
  is the Phase 3.3 live run against the real cache, recorded here rather than hidden.
- Top clone candidates by score (`targets`): 54618168 (1251.4), 54703548 (1141.4), 54639964
  (1118.4) — all mega_lucario_ex+solrock — and 54524125 (1101.0, kangaskhan). **Cache holds
  only 1–2 episodes each** → snowball refresh of the top 7 launched (`runs/m23_snowball.log`),
  harvest chained after.

## 2026-07-21 ~10:40 — clone corpus landed; target = 54618168 (1251.4, lucario mirror)

Snowball complete (500 fetched, parquet 2443→2943; harvest re-run). Post-snowball coverage:
**54618168: 351 episodes with obs** · 54639964: 53 · the rest 1–2 (max-new hit before them).
54618168 is the clear clone target: top score in our cache AND the mirror archetype, so the
clone directly measures pilot quality on our own deck matchup.

`replay_bc build --only-subs 54618168 --min-score 600 --out data/bc_clone_54618168`:
**348 teacher seats → 26,957 decisions, 6 shards** (filter verified live: 5,528 non-target
seats skipped). BC clone training launched: `rl.bc train --arch v2` (the M10 path — these
shards are v2-schema), name `bc_clone_54618168`, 10 epochs, thread-capped under the RL run.
M10 context for expectations: fidelity saturated ~0.53 cloning MIXED unobservable teachers;
a single consistent policy should clone better — the fidelity gate is Phase 3.4, BEFORE any
measurement role.

### Found on the way: v2 BC trainer/loader pinned to pre-M16 option widths

First clone train crashed: replay shards carry the MODERN `encode_option_v2` width
(OPTION_V3_DIM=94, states 1361 — identical to the live inference path) while `rl/bc.py`'s
collate/`OptionScorerV2` and the `model:` loader in `rl/matchrunner.py` assumed the pre-M16
legacy 90. Nothing had trained on `replay_bc build` output since the encoders grew. Fixed
width-driven end to end: `OptionScorerV2(option_dim=…)`, dataset/collate take the width from
the data, and the loader sniffs `option_enc.0.weight` to pick modern vs legacy encoding
(pinned pre-M16 checkpoints keep the legacy path). Suite 605 green.
(`tcg/network.py` twin deliberately untouched — the clone is measurement-only and never ships.)

## 2026-07-21 ~10:55 — 🔴🔴 the clone BEATS the sample agent on the ladder distribution

`bc_clone_54618168` (10 epochs, 24,419 train / 2,538 val decisions, by-game split):
**held-out action accuracy 0.664** (best 0.664 at epoch 7; M10's mixed-teacher ceiling was
~0.53 — single-policy cloning clones better, as hoped).

Deck discovery: the target does NOT play our list — same archetype, different 60 (deck_hash
`9294d9d8…` vs our `20dcd313…`). Exported to `data/kaggle/clone_54618168_deck.csv`; the clone
pilots its own deck.

Strength probes (n=100 each, workers 2, seed 1 — deliberately light, main leg running):

| clone vs | wr | reference points |
|---|---|---|
| `rule:lucario` (sample agent) | **0.590** | our best agents 0.32–0.37; solver 0.33 |
| `rule:dragapult` (out-of-loop floor) | **0.250** | our agents 0.17–0.22; sample agent ~0.505 |

**First pilot this campaign has ever produced that beats a company agent** — and it is
(partially) exogenous: behaviour cloned from an outside team's 1251-score policy on its own
deck. But the dragapult read shows it is NOT a generalization miracle: 0.250 is barely above
our ceiling. Reading: the clone carries its target's on-distribution (ladder ≈ 76% mirror)
strength; whether the 0.25 reflects the target's own spread-matchup weakness or clone
off-distribution decay is NOT distinguishable from here — do not over-read.

⚠ Caveats pinned: n=100 probes (±~10pp); deck and pilot are BUNDLED in both probes (their
tuned 60 may carry part of the edge); fidelity 0.664 is action-level, and the remaining
Phase 3.4 step — behavioral contrast of clone-piloted series vs the target's own replays —
has not run yet.

**Evaluator verdict (per the pre-registered role):** validated as a strong, partially-exogenous
MIRROR-META evaluator (exactly the pilot-quality-aware opponent the solver-piloted cells could
never be); NOT a substitute for `rule:dragapult` as the out-of-loop floor. Open question for
Piotr (scope extension, not acted on): a 0.59-vs-sample-agent observable policy is now the
strongest teacher candidate we own — Phase 1's teacher (`rule:lucario`, 0.50) may be obsolete
the moment Phase 1's verdict lands.

## 2026-07-21 ~11:15 — Phase 3.4 behavioral contrast: clone tracks the target early, decays late

Target's own replays (351 games, THEIR seat via scratch script — `from_replays` assumes
`our_seat`, unusable here) vs clone-piloted series (n=100, seed 2). Supporter taken/games by
our-turn (≈ per-turn play rate, taken fires ≤1/turn):

| pilot | t1 | t2 | t3 | t4 | t5 | t6 |
|---|---|---|---|---|---|---|
| target 54618168 (replays) | 0.40 | **0.92** | **0.80** | **0.74** | 0.62 | 0.50 |
| clone (series) | 0.43 | 0.80 | 0.54 | 0.57 | 0.45 | 0.17 |
| sample agent (reference) | 0.67 | 0.75 | 0.73 | 0.74 | 0.80 | 0.59 |
| our model m22cRL | 0.54 | 0.55 | 0.35 | 0.34 | 0.34 | 0.16 |

The clone inherits the target's early-game draw-engine profile (t1–t2 close) and
under-imitates mid/late game — consistent with 0.664 action fidelity concentrated on common
early states. Every strong pilot we can observe (1251-target, sample agent, clone) plays
supporters at 0.5–0.9/turn through the midgame; our model alone drops to ~0.34 from t3.
Correlational, not causal — but it is now three independent strong pilots against one weak
one, all same-archetype. Also: the target had only 13 strict-gust candidate states in 351
games (its 60 may not run Boss at volume) — gust is NOT how the leader wins, further burying
the M21 gust story.

Phase 3 deliverable state: corpus ✅ · filter ✅ · clone trained ✅ · fidelity: action 0.664 +
strength probes + behavioral contrast recorded ✅. Role: informational mirror-meta evaluator
next to (never replacing) dragapult.

## 2026-07-21 16:13 — Phase 1 training COMPLETE: the plateau held

20/20 iters (10:15→16:13, ~18 min/iter with per-iter evals). Full vs_teacher curve (n=200):
30.0 33.0 34.0* 35.5* 32.0 40.5* 35.0 30.0 33.0 **43.0*** 36.5 35.0 36.5 35.5 39.0 37.5
27.5 35.0 30.5 34.5 (* = promoted). Baseline 33.0.

- **Real early climb** (33 → high-30s band, promotions to 43.0 at iter 9), then **10 iters of
  sideways** — no promotion after iter 9, final iters 27.5–35. The agent trained ~30 total
  iters on this recipe (10 in M22c + 20 here) and has not approached teacher parity (0.50).
- vs_solver drifted 49–51 → 44–47 over the leg (solver fully out of the pool) — mild,
  monitored via the mirror gate below.
- In-loop numbers are triggers, not evidence (M20 law) — the verdict rests on the gate
  battery, launched 16:15: `rule:dragapult` n=400×2 seeds on BOTH `ppo_best_m23p1` (iter 9)
  and `ppo_current_m23p1` (final), + `rule:lucario` n=200×2, mirror n=400×2, random floor
  n=200×2 on best. Logs `runs/m23p1_gate_*.{log,jsonl}`.
- Pre-registered read pending: plateau-well-below-teacher is CONFIRMED on the in-loop curve;
  the fork's second condition (dragapult flat vs 0.1888/0.172) decides ARCHITECTURE vs mixed.

## 2026-07-21 ~17:45 — gate battery, secondary gates in: the "climb" did not survive offline

(Dragapult primary delayed by a tooling slip, recorded honestly: the gate loop wrote
`model:checkpoints/$ckpt:lucario` and **zsh's `:l` history modifier lowercased the expansion**,
producing `…ptucario` — all four dragapult runs died on spec parse. The literal-typed gates
were unaffected. Re-launched with `${ckpt}` braces + quotes. Lesson for scripts: always brace
and quote specs built from variables in zsh.)

`ppo_best_m23p1` (iter 9 — the 43.0% in-loop promotion), all 2-seed:

| gate | s1 / s2 | pooled | reference |
|---|---|---|---|
| `rule:lucario` n=200×2 | 0.350 / 0.365 | **0.3575** | m22cRL 0.365, B3 0.357 |
| `solver:lucario` n=400×2 | 0.406 / 0.464 | **0.435** | m22cRL 0.480, B3 0.5094 |
| `random:kyogre` n=200×2 | 0.915 / **0.835** | 0.875 | m22cRL 0.920, B3 0.935 |

- 🔴 **The offline teacher read is FLAT: 0.3575 vs m22cRL's 0.365.** Twenty more iterations
  against a 50%-of-pool teacher bought zero measurable offline gain vs that same teacher. The
  in-loop 43.0% promotion read was n=200 promotion-trigger noise — the M20 lesson reproducing
  on schedule (in-loop evals are triggers, NEVER evidence; this diary said so this morning).
- Mirror 0.435 pooled is nominally −4.5pp vs m22cRL (inside the 7pp MDE, but the direction is
  consistent with the solver leaving the pool entirely).
- Floor seed 2 at **0.835** is below every recent pin (m22cRL 0.920, B3 0.935) with an 8pp
  seed spread; single-seed floors are known-noisy ([[floor-gate-sample-size]]) but this is a
  watch item — a candidate this shape does not ship regardless.
- Verdict still pends the dragapult primary, but the plateau branch of the pre-registered
  fork is now supported by BOTH the in-loop curve AND the offline teacher gate.

## 2026-07-21 ~18:40 — 🔴 PHASE 1 VERDICT: the strong teacher, converged, moved NOTHING

Primary gate, `rule:dragapult` n=400×2 seeds per checkpoint:

| checkpoint | s1 / s2 | pooled n=800 | vs baselines (m22cRL 0.1888, B3 0.172; MDE 5.5pp) |
|---|---|---|---|
| `ppo_best_m23p1` (iter 9) | 0.144 / 0.158 | **0.151** | −3.8pp — flat, nominally DOWN |
| `ppo_current_m23p1` (final) | 0.175 / 0.177 | **0.176** | −1.3pp — flat |

**Both pre-registered plateau conditions are now met on offline evidence:** vs-teacher
plateaued well below parity (offline 0.3575 vs 0.365 baseline — zero gain from 20 iters
against a 50%-weight teacher) AND dragapult is flat. Per the fork written before the run:
**the bottleneck is NOT the teacher/training-side recipe → architecture/signal side → the
signal audit (`docs/m23-signal-audit-plan.md`) is the next work, exactly as queued.**

What this leg settles that M22c could not: the teacher-weakness thesis is now **tested at
convergence and answered NO** — 30 total iterations on the strong-teacher recipe (10 M22c +
20 here), vs-teacher offline flat, out-of-loop flat. "One short leg" is no longer the excuse.
The M23-plan strategic caveat (a 0.50 teacher is a mediocre ceiling) never even got tested —
the agent could not reach the ceiling that was supposedly too low.

Bonus finding, promotion-selection edition: the FINAL checkpoint (in-loop 34.5%) nominally
beats the iter-9 "best" (in-loop 43.0%) on dragapult, 0.176 vs 0.151 (z=1.37, inside noise) —
in-loop promotion picked, at best, a coin-flip. Third strike for n=200 promotion evals as
selection signal.

**No ship, recommended and assumed** (standing policy — Piotr's call if he disagrees):
every gate is flat-to-down vs the live m22cRL arm (mirror 0.435 < 0.480, teacher 0.3575 ≈
0.365, dragapult 0.151/0.176 ≤ 0.1888, floor seed-2 0.835 watch item). There is nothing here
a live slot would measure. Live arms B2/B3/M22c-RL continue accruing untouched.

### M23 exit state

- Phase 0 ✅ (seat fix, dragapult guard, supporter counter — all landed, suite 605)
- Phase 1 ✅ RESOLVED: training-side lever exhausted at convergence; fork → signal audit
- Phase 2: NOT triggered as "plan-head rewrite by default" — superseded by the audit
- Phase 3 ✅ over-delivered: `bc_clone_54618168` (0.59 vs sample agent) + the corpus +
  width-driven v2 path — and it is the audit's primary instrument (S1/E1)
- Next session entry point: `docs/m23-signal-audit-plan.md` (memory updated)

## 2026-07-21 ~15:45 — signal-audit plan written (Piotr: next session's kickoff)

Prompted by the Phase 1 plateau + the clone result, Piotr called for an actionable diagnostic
plan instead of defaulting to the Phase 2 plan-head rewrite. Written to
`docs/m23-signal-audit-plan.md`: S1 shadow-eval agreement by option type (over the clone
corpus), S2 supporter↔win correlation in our own self-play, S3 behavior-policy action-type
frequencies, E1 the V3-distillation discriminator (architecture exonerated iff V3-as-BC
matches the V2 clone), S4–S7 logging additions for the next leg (advantage-by-type, value
calibration by turn, plan-consistency, entropy-by-turn). All thresholds pre-registered in the
doc. The clone's existence reframes Phase 2: an OptionScorerV2 without a plan head plays
0.59 vs the sample agent, so capacity is not the bottleneck — the audit decides what is.

## 2026-07-21 evening — SIGNAL AUDIT KICKOFF (Piotr decisions + pipeline review)

Fresh session reviewed the audit plan against the actual pipeline before running anything.
Findings folded into `docs/m23-signal-audit-plan.md` header:

1. **E1 is cheaper than planned** — `BCDatasetV3` already shims plan-less shards
   (plans=zeros, plan_labels=-1), so no bc.py adapter is needed; the only real gap is
   `plan_iter train`'s fresh-init hardcoding `n_state_ids=20` vs the clone shards' 12
   (width-driven fix this session, same pattern as the M23 bc.py fix).
2. **S1 encoding wrinkle** — PPO checkpoints are v4 (1702/25 ids) vs the clone corpus's
   v2 encoding (1361/12): zero-pad the v4 block + ids (legitimate "no history" defaults);
   read the within-checkpoint option-type CONTRAST, not absolute agreement; V2 clone runs
   as harness control.
3. **S2/S3/S5/S7 need no new collection** — m23p1 final-iter shards survive in `data/ppo/`
   (400 games, v4, stored `plans` ⇒ collection-time logits exactly reproducible with
   `ppo_current_m23p1.pt`).

**Piotr decisions (this session):** audit-first sequencing confirmed; S4 rl/ppo.py logging
edit approved; clone-as-teacher deferred to the S2/S3 verdicts; **replay-BC on other teams'
public replays approved as a ship path** — E1's exoneration branch would make the replay
corpus the primary training signal for M24, with self-play PPO demoted to fine-tuning.

Architecture suspects registered before the probes run (exploration session, for the record):
(A) plan vocabulary is attack-only — `enumerate_plans` cannot express a setup/draw line, so
the plan head structurally cannot help the measured t3+ supporter deficit; (B) nothing prices
card economy — reward is terminal ±1 + 0.1 prize shaping only; (C) exploration prior —
ENTROPY_COEF=0.001, plain multinomial behavior policy. S1/S6 probe A, S2/S4/S5 probe B,
S3/S7 probe C, E1 arbitrates capacity vs signal.

## 2026-07-21 evening — 🔴 AUDIT SHARD PROBES IN: the signal is ABSENT, everything else is fine

Tool: `scripts/m23_signal_audit.py` (new, saved; logs `runs/m23_audit_s{1,2,3,5,7}*.log`).
Probes S1/S2/S3/S5/S7 all ran on existing data (clone corpus + surviving m23p1 shards, 400
games). Verdicts against the pre-registered thresholds:

- **S1 (shadow eval, clone corpus): the "localized card-economy deficit" branch does NOT
  fire** — the gap runs the OTHER way (attack-class 0.13–0.17 vs card-economy 0.31, both
  checkpoints). Harness control: the V2 clone reproduces 0.695 full-corpus (0.664 was
  held-out — validates the probe). Both PPO checkpoints sit at 0.48 overall vs the clone's
  0.695. ⚠ The abysmal ATTACK/ATTACH agreement (0.22/0.04) is CONFOUNDED by plans=zeros —
  the plan vector is precisely what names attacker/target at inference; do not over-read
  that cell. The robust cells: PLAY_supporter agreement 0.12–0.13 (clone control 0.26),
  decaying with turn (0.30→0.09) exactly like the live supporter table; EVOLVE 0.39 vs
  control 0.80. Read: broad off-distribution weakness, worst on card-play targeting.
- **S2 (supporter↔win in OUR self-play): 🔴 THE FINDING — the signal is absent, in fact
  slightly NEGATIVE.** Winners play FEWER t2–t4 supporters than losers (0.72 vs 0.84/game;
  availability-conditioned take-rate 0.274 vs 0.341, r=−0.108, p≈0.05, n=323 games). Per
  the pre-registered branch: **the signal PPO needs does not exist in our self-play
  distribution — no optimizer or architecture fix will teach the draw engine from this
  data. The fix is opponents/data.**
- **S3 (exploration): NOT the ceiling** — supporter-play sampled in 16.3% of candidate
  states (threshold <5%), 13–24% across turn buckets. PPO tries the lines; the data never
  rewards them.
- **S5 (critic calibration): moderate, not the catastrophic branch** — corr(V, outcome)
  0.23 early rising to 0.47 late. Weak early baseline adds variance but is not the
  bottleneck given S2.
- **S7 (entropy): healthy** — H/logN 0.46–0.60 across all buckets, no premature
  determinism.

**Combined verdict, per the decision tree: opponent/data-distribution problem.** Training
harder, tweaking the optimizer, adding exploration bonuses, or rewriting heads cannot work
while the collected games contain no positive draw-engine signal. This converges with the
clone result from the other side. Remaining discriminator: E1 (is our V3 arch able to
ABSORB strong exogenous play via BC?) — if yes, architecture exonerated and M24 = replay-BC
as primary signal (Piotr pre-approved the ship path) + strong exogenous opponents in the
loop.

## 2026-07-21 ~18:20 — E1 fidelity: V3-as-BC matches the V2 clone at the action level

`rl.plan_iter train` fresh on `data/bc_clone_54618168` (width-driven fix landed +
regression test; suite 606 green): `bc_clone_v3_54618168`, 10 epochs, **best val_acc
0.657** vs the V2 clone's 0.664 — parity within 0.7pp on the identical corpus and by-game
split ritual. The plan head trained on nothing (no plan rows — plan_acc 0.000 as expected);
this IS the V3(plan=0) path our live agent falls back to. Architecture absorbs the strong
exogenous policy exactly as well as the plan-less V2 at the action level. The pre-registered
E1 verdict rests on the strength probes (vs `rule:lucario` ≥~0.54 pooled = exonerated,
≤~0.45 = implicated; vs `rule:dragapult` informational), n=200×2 seeds, clone deck — next.
S4 advantage-by-type logging also landed in `rl/ppo.py` (Piotr-approved) with the audit
script `scripts/m23_signal_audit.py` covering S1/S2/S3/S5/S7 for reuse.

## 2026-07-21 ~18:30 — S6 (from stored plan-PPO shards): the plan head is NOT decorative

Computed without series play (the shards carry the committed plan per row + board ids +
chosen option). First cut had a probe bug worth recording: ATTACK options encode NO card
identity (`option_ids` = [0,0] — the pre-M16 aliasing note, still true for ids), so
attacker-consistency must be read as "the card named at commit is the ACTIVE at attack
time" (`state_ids[:,0]`). Fixed read, n=327 plan-committed attack rows:

- **ATTACK: 0.902** attacked with the named attacker as active (59/327 plans named a bench
  attacker — the plan is not just 'attack with whoever is up').
- **ATTACH (needs_attach plans): 0.675** attach to the named attacker (n=80).

Both far above chance → the pre-registered "decorative plan head" branch does NOT fire —
**deleting the plan head is not indicated**. The plan machinery executes what it commits;
its structural limit stands elsewhere (the vocabulary is attack-only, suspect A), and per
S2 even a richer vocabulary could not be learned from data that carries no setup signal.
Caveats: sampled (plan-tau 1.5) behavior, not greedy; slot drift between commit and attack
is the approximation error; single-recipe shards (m23p1 final iter).

## 2026-07-21 ~18:27 — 🟢 E1 VERDICT: ARCHITECTURE EXONERATED

`bc_clone_v3_54618168` (our exact OptionScorerV3, fresh, null plans) piloting the clone
deck, n=200×2 seeds per opponent (`runs/m23_e1_*.jsonl`, decode 0=WIN):

| gate | s1 / s2 | pooled n=400 | V2-clone reference (n=100) |
|---|---|---|---|
| `rule:lucario` | 0.540 / 0.585 | **0.5625** | 0.590 |
| `rule:dragapult` | 0.185 / 0.220 | **0.2025** | 0.250 |

**Pre-registered bar: ≥~0.54 pooled = exonerated — met on both seeds individually.** Our
architecture, trained on 348 games of exogenous strong play, beats the sample agent — the
thing 30 iterations of PPO against that same sample agent could not approach. Combined with
S2 (signal absent from self-play) the audit's conclusion is unambiguous:

**THE AUDIT'S ANSWER: nothing is wrong with the architecture, optimizer, exploration,
critic, or plan head. The RL loop starves because its DATA carries no signal for the
behaviors that win. The fix is the signal source: leaderboard-replay BC as primary,
self-play PPO demoted to fine-tuning, strong exogenous opponents (the clones) in any
future loop.** Dragapult stays the out-of-loop floor (clone-line 0.20–0.25 confirms
imitation carries on-distribution strength, not generalization).

### Audit exit state (all probes resolved same-day)

| probe | branch fired |
|---|---|
| S1 | no localized deficit; broad off-distribution gap (plan=0 confound noted) |
| S2 | 🔴 signal ABSENT (slightly negative) → opponents/data, full stop |
| S3 | exploration fine (16.3% ≫ 5%) |
| S5 | critic moderate (0.23→0.47), not the bottleneck |
| S6 | plan head NOT decorative (0.90/0.68 consistency) — do not delete |
| S7 | entropy healthy (0.46–0.60) |
| E1 | fidelity parity (0.657 vs 0.664) + strength bar MET (0.5625 ≥ 0.54) |

Instruments saved: `scripts/m23_signal_audit.py` (s1–s7), `scripts/m23_e1_gates.sh`,
S4 logging live in `rl/ppo.py`. Suite 607 green. M24 scoping next (Piotr decision).
