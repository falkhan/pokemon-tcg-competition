# M39 diary — build the instrument for 1000, recover the regression

Plan: [M39-plan.md](M39-plan.md) (APPROVED 2026-08-01, all 13 decisions
settled). Validation protocol: [VALIDATION.md](VALIDATION.md).

---

## 2026-08-01 — P0.3 live mix: the planned gate roster FAILS G-3's coverage floor

`scripts/m39_live_mix.py` over the pooled M37 (55065484) + M38 (55146658)
samples — 147 ladder games after dropping 2 self-validation rows. Written to
`data/m39_live_mix.json`.

| family | n | share | bed |
|---|---|---|---|
| lucario | 29 | .197 | tuned |
| mirror | 23 | .157 | mirror, m28 |
| wall | 20 | .136 | wall |
| **archaludon** | **18** | **.122** | **NONE** |
| grim | 18 | .122 | grim |
| dragapult | 7 | .048 | dragapult |
| garchomp | 7 | .048 | garchomp |
| starmie | 6 | .041 | NONE |
| other | 5 | .034 | NONE |
| rocket | 5 | .034 | rocket |
| **stall** | **4** | **.027** | stall (planned) |
| kanga | 2 | .014 | NONE |
| froslass / iono / ogerpon | 1 each | .007 | NONE / iono / NONE |

**Roster coverage: 77.5% — BELOW the 80% floor. As specced, the M39 gate
could not have emitted a verdict at all** (G-3: a roster covering <80% of
live-mix mass is invalid). The plan was drafted against the M37 post-mortem's
remembered shares (archaludon ~.09); measured, it is .122 and it is the
largest family with no bed.

Two corrections to the plan's assumed weights: **wall .136** (assumed .11)
and **stall .027** (assumed .04). The wall/grim/stall "28% loss block" is
real in aggregate (.136+.122+.027 = .285) but **stall contributes only 2.7pp
of it** — the block is wall+grim, and stall is a rounding error carrying a
whole planned bed.

## 2026-08-01 — P0 census: stall is unbuildable, archaludon is, and the falsification test passes

`scripts/m39_census.py` (NEW) over 2594 cached seats / 1297 episodes.
Family classification imports `m39_live_mix.classify`, so the census and the
gate weights cannot drift apart.

**Bed buildability — seats per family per band** (working floor 100 at band;
`m38_bc_wall` shipped on 138):

| family | <700 | **700-850 LIVE** | 850-1000 | 1000+ | total |
|---|---|---|---|---|---|
| mirror | 145 | **249** | **609** | 26 | 1034 |
| grim | 33 | **105** | 141 | 30 | 309 |
| wall | 73 | **120** | 65 | 3 | 262 |
| garchomp | 198 | 34 | 23 | 2 | 257 |
| lucario | 67 | **125** | 14 | 0 | 210 |
| **archaludon** | 8 | **167** | 21 | 0 | 197 |
| dragapult | 23 | 81 | 7 | 0 | 111 |
| **stall** | 10 | **9** | 1 | 1 | **22** |

Three results, in order of consequence:

1. **`m39_bc_stall` is NOT BUILDABLE. 9 seats at band, 22 across all bands.**
   The plan's fallback ("if seat volume is thin, a combined stall bed is
   acceptable v1") does not save it — pooling every band still gives 22, and
   pooling across bands is precisely the band-infidelity that made the M26
   grim clone lie. **Killed, not deferred.**
2. **`m39_bc_archaludon` IS buildable — 167 seats at band**, more than the
   wall bed we already trust. Adding it takes roster coverage **77.5% →
   89.7%**, back over the G-3 floor.
3. **The instrument is comfortably buildable: 609 mirror seats at 850-1000**
   (plus 26 at 1000+, thin for a standalone bed). P0.8 is not at risk.

`m39_bc_grim` also confirmed buildable at 105 seats at band — the M26 relic
can be retired as planned (G-6).

**VS-LOSS CENSUS — the deck-decision falsification test, and it PASSES.**
Our-deck (9294d9d8) winner seats at ≥800, by opponent family:

| opponent | winner seats @800+ |
|---|---|
| mirror | 82 |
| **grim** | **47** |
| **wall** | **22** |
| archaludon | 20 |
| lucario | 18 |
| garchomp | 14 |
| dragapult | 7 |
| rocket / starmie / other | 6 / 5 / 6 |
| iono / ogerpon | 3 / 1 |
| **stall** | **0** |
| TOTAL | 231 |

Pre-registered reading (plan, deck-decision section): "Plenty → the matchups
are winnable with better lines, stay-decision confirmed." **47 grim and 22
wall winner seats at 800+ is plenty.** Strong pilots on our exact list beat
the decks that beat us, so the loss mass is a piloting gap, not a deck
property. **Stay-on-alakazam is now confirmed with data rather than
argument.**

The one exception is stall: **zero** 800+ winner seats against it, on top of
2.7% live share and an unbuildable bed. Every instrument we have says the
same thing — we cannot measure stall, we have no demonstrations of beating
it, and it is too rare to matter this milestone.

### Consequences (proposed — flagged to Piotr before P2 gating)


- **Bed roster: swap `m39_bc_stall` → `m39_bc_archaludon`.** Forced by G-3;
  archaludon is the only family that restores the floor, and it is 4.5× the
  live share of stall.
- **P2a keeps its stall trigger-set change** (Fan Rotom / Hop's Trevenant /
  Kangaskhan ids) — it is a cheap, provably-inert-elsewhere id addition
  validated by the coverage regression test — **but it can no longer be
  gated on a stall bed.** It ships on the fixture test alone or not at all.
- **P2's kill gate needs rewording**: "wall+stall improves ≥5pp" becomes
  **wall+grim+archaludon**, which is where the deck-race mass actually is.
- **P3 corpus A** filtered to the loss matchups is 47+22+20+7 = **96 winner
  seats** (grim/wall/archaludon/dragapult), just under the ≥100 target and
  before the loser seats that decision 8 keeps. Corpus B (best-response)
  covers the shortfall as designed.

---

## 2026-08-01 — P0.8 THE INSTRUMENT: built, and the gap read is the BEST pre-registered reading

`m39_bc_top` — BC clone of **329 seats** on our own list (9294d9d8) at
leaderboard score ≥900, piloting `decks/clone54618168.csv` (that same list).
**val_acc 0.682**; the wall clone that proved live-faithful was 0.676, and
the M26 grim clone that lied was 0.565. Battery:
`scripts/m39_topgap.sh`, n=400 × 2 seeds = 800/cell.

| candidate | pooled WR vs 900+ bed | 95% CI | gap vs parity |
|---|---|---|---|
| champion (m28_winners, M37 net) | **0.404** | ±0.034 | **−9.6pp** |
| **cont3 plain (M38 net, no rules)** | **0.516** | ±0.035 | **+1.6pp** |
| shipcfg (cont3 + gacfr3 — LIVE NOW) | **0.466** | ±0.035 | −3.4pp |

| contrast | delta | z |
|---|---|---|
| cont3 − champion | **+11.2pp** | **+4.49** |
| cont3 − shipcfg | **+5.0pp** | **+2.00** |
| shipcfg − champion | +6.2pp | +2.50 |

**Pre-registered reading: "≤10pp → the 800+ band is reachable by closing
known loss mass; slot 3 is a genuine 1000 attempt." The gap is ≤10pp for
every candidate, and for cont3 plain it is statistically indistinguishable
from PARITY** (0.516 ± 0.035 spans 0.50). We are not 40pp adrift of the
band we are chasing. This is the best of the three readings the plan
pre-registered.

Three consequences, in order of size:

1. **The M38 fine-tune was a large real gain the live sample could not
   see.** cont3 beats the champion by **+11.2pp (z=+4.49)** against
   top-band opposition. The M38 post-mortem concluded the winners fine-tune
   "fixed its own family but the ceiling is set by the wall/stall/grim loss
   mass" — on this instrument it is worth eleven points against exactly the
   band we could not previously measure. A live read at n=54 (implied ELO
   659, CI [566, 753]) could never have detected it. This is
   [VALIDATION.md](VALIDATION.md) §0's argument arriving as a concrete
   result rather than an abstraction: **we nearly abandoned a lane that was
   working, on a sample too small to see it.**
2. **The rule stack costs 5.0pp at the top band (z=+2.00) — independent
   confirmation of G5 on a completely different bed.** G5 measured −7pp on
   the wall bed; this is −5pp on a mirror-family 900+ bed. Two independent
   beds, same direction, same rough magnitude. **Decision 1 (full strip) is
   now backed by two instruments instead of one.**
3. **Ship A's expected value rose.** What is live right now is the 0.466
   config; plain cont3 is 0.516 on this bed and .323 vs .254 on the wall
   bed. The strip is looking like the cheapest real gain available.

### Honest limitation of this instrument (stated before anyone over-reads it)

`m39_bc_top` is cloned from **mirror-family** seats — strong pilots of our
OWN list. It measures "can we play top-band pilots of our own deck to
parity", not "can we compete at 900+ across the meta". The live 800+ band
also contains grim, wall and archaludon pilots at that altitude, and this
bed says nothing about them (G-8).

The census says the extension is buildable — grim has **141 seats at
850-1000** (+30 at 1000+) and wall has **65** — so a high-band non-mirror
instrument is a real option. It is beyond P0 as written; flagged to Piotr
rather than silently added.

---

## 2026-08-01 — P0.6 ride-along `live_ci.py`: the M38 "regression" is not a detectable difference

`scripts/live_ci.py` (NEW, the G-9 instrument) over the two ship subs.
Wilson intervals (correct near 0/1, where our matchup cells live) and Fisher
exact for the A/B.

| sub | n | W-L | WR | avg_opp | implied ELO | 95% CI |
|---|---|---|---|---|---|---|
| M37 55065484 | 93 | 46-47 | 0.495 | 718.0 | 714 | **[645, 784]** |
| M38 55146658 | 56 | 27-29 | 0.482 | 672.5 | 660 | **[572, 749]** |

**Fisher two-sided p = 1.000.** The two ships have *statistically identical
win rates* — 0.495 vs 0.482. The entire implied-ELO gap comes from a
**45-point difference in average opponent strength** (718.0 vs 672.5), not
from playing worse.

Read alongside the P0.8 gap result (cont3 beats the champion by +11.2pp,
z=+4.49, against 900+ opposition), the picture inverts the M38 post-mortem's
headline: **the M38 net is measurably stronger than its parent, and the
"regression to 659" was a sample the campaign was never powered to read.**
What the ship carried that plainly does hurt is the rule stack — −5.0pp on
the top bed, −7pp on the wall bed, two independent instruments.

Every per-matchup live cell in the table prints its Wilson interval, and
they are all enormous — M37's wall cell is 4-10, CI [0.12, 0.55]. That is
the cell the M38 post-mortem ranked the milestone from. G-9 now bars it from
gating anything.

Caveat on this run: the M38 sub's opponent families all decode as `unknown`
because its episodes are newer than the current `opp_decks.parquet` build —
the P0.1 harvest now running will backfill them. The pooled numbers above
are unaffected (they come from `episodes.parquet`).

---

## 2026-08-01 — P0.7 strip ledger, AND A CORRECTION to the P0.8 entry above

### The methodological finding that forced a re-measurement

The first ablation pass (n=800/cell) produced `plain` = **0.466** on the top
bed. The P0.8 gap battery, **same specs, same bed, same seeds**, had
produced **0.516**. Two independent n=800 measurements of one quantity, 5pp
apart.

Cause, confirmed by running the identical command five times at seed 1:
**0.471 / 0.494 / 0.526 / 0.506 / 0.537**. `matchrunner --workers 8` is not
run-reproducible — M37 established this (engine RNG is per worker process;
`mp.Pool` assigns chunks by timing) and it is written in
`scripts/m37_decide.py` B4. The spread is ordinary binomial, not excess
variance, but the consequence is sharp: **the seed does not pin the result,
so every cell is an independent sample and n=800 buys ±3.5pp.**

Cross-referenced against [VALIDATION.md](VALIDATION.md)'s own power table,
detecting 5pp needs **n≈1560 per arm**. The ablation was therefore
underpowered for the effects it was built to find — its "no signal"
verdicts were preordained, not informative. Re-ran the decision-relevant
arms to **n=2400/cell**.

### CORRECTION

The P0.8 entry above claims *"the rule stack costs 5.0pp at the top band
(z=+2.00) — independent confirmation of G5"* and calls it a second
instrument backing the strip. **That was an underpowered reading and it did
not replicate.** At n=2400 the stack is neutral at the top band. Corrected
numbers below; the entry above stands only for the parts re-confirmed here.

### Powered results (n=2400/cell)

| bed | plain | cont3+gacfr3 (shipped) | strip delta | z |
|---|---|---|---|---|
| **wall** | 0.3237 ±0.019 | 0.2833 ±0.018 | **+4.04pp** | **+3.05** |
| **top (900+)** | 0.4729 ±0.020 | 0.4750 ±0.020 | −0.21pp | −0.14 |

| top bed, all data pooled | WR | gap vs parity |
|---|---|---|
| champion (M37 net) | 0.4127 ±0.020 | **−8.7pp** |
| cont3 plain | 0.4729 ±0.020 | **−2.7pp** |
| cont3 + gacfr3 | 0.4750 ±0.020 | −2.5pp |

| contrast | delta | z |
|---|---|---|
| cont3 − champion (top bed) | **+6.02pp** | **+4.20** |

**What survives, what does not:**

- ✅ **The P0.8 pre-registered reading holds at proper power.** Champion is
  −8.7pp and cont3 −2.7pp against the 900+ bed — inside the "≤10pp → the
  band is reachable, slot 3 is a genuine 1000 attempt" branch.
- ✅ **cont3 really is stronger than the champion: +6.02pp, z=+4.20.** The
  direction and significance survive; the *magnitude* does not — I reported
  +11.2pp off n=800 cells, and the powered value is +6.0pp. The conclusion
  that the M38 net was better than its live sample suggested is unchanged.
- ❌ **The strip is NOT confirmed at the top band.** It is **wall-specific**:
  +4.04pp (z=+3.05) on wall, nil at 900+. G5's direction is confirmed; its
  magnitude (+6.9pp) is not — the better-powered estimate is +4.0pp.

### The per-rule ledger (decision 1)

Leave-one-out from the shipped stack. Positive delta = removing the rule
helps. n=800/cell except `conserve`, powered to n=2400 because it was the
only arm whose first-pass signal could have changed the strip scope.

| rule removed | wall delta (z) | top delta (z) | verdict |
|---|---|---|---|
| telepath | +3.9pp (+1.69) | −1.3pp (−0.50) | strip (underpowered) |
| deckguard | −1.5pp (−0.67) | +2.3pp (+0.93) | strip (no signal) |
| ash | +0.4pp (+0.17) | +0.1pp (+0.05) | strip (no signal) |
| **conserve** | **−0.29pp (−0.22)** | **−2.75pp (−1.91)** | **strip — closest call** |
| benchfloor | +0.3pp (+0.11) | +0.8pp (+0.30) | strip (no signal) |
| racemode3 | −0.1pp (−0.06) | −1.5pp (−0.60) | strip (no signal) |

**Verdict: FULL STRIP, per the pre-registered rule** ("default strip-all;
keep a rule only if removing it significantly HURTS"). No rule cleared the
bar.

`conserve` is the one judgment call and is recorded as such: at n=800 it
looked like a keeper (−5.2pp, z=−2.11 on top); powered to n=2400 it weakens
to −2.75pp, **z=−1.91, p≈0.056 — just under the bar**. It has a real
mechanical rationale (M30 low-deck draw protection). Three things decide it
for strip anyway: (i) the pre-registered trigger did not fire and G-1 makes
triggers binding; (ii) **12 contrasts were tested, so one result at p≈0.056
is exactly what chance produces**; (iii) the effect is absent on the wall
bed, where the deck-out mass actually is. Flagged for Piotr as the single
reversible call in the ledger — it is one token in a config string.

**Caveat carried forward:** the other five rules are decided by an
underpowered battery falling to the strip default, not by evidence that they
are inert. That is the correct call under the decision rule, but it is not
the same as having measured them.

---

## 2026-08-01 — P0.1 harvest round 3: corpus A is now over target, and a high-band grim bed became buildable

Snowball over 15 top-band opponent subs (mirror ≥900; grim/wall/archaludon
≥780), 600 new episodes. `episodes.parquet` 6499 → 7101 rows; parsed seats
**2594 → 3908** over **1954 episodes**.

| family | 700-850 | 850-1000 | **1000+** | total |
|---|---|---|---|---|
| mirror | 273 | 674 | **133** | 1291 |
| **grim** | 116 | **257** | **562** | **973** |
| wall | 124 | 137 | 38 | 382 |
| archaludon | 221 | 51 | 0 | 287 |
| lucario | 133 | 16 | 0 | 234 |
| stall | 9 | 3 | 9 | 34 |

**Stall remains unbuildable at band even after the snowball** (9 seats at
700-850) — the kill stands on more data than it was made on.

**vs-loss census, refreshed:** grim **76** (was 47), wall **27** (was 22),
archaludon **24** (was 20), mirror 91. Corpus A filtered to the loss
matchups is now **≈127+ winner seats before the loser rows decision 8
keeps — over the ≥100 target**, so P3 corpus A is no longer the thin-corpus
risk the plan was hedging against. Corpus B (best-response) stays scheduled
as the volume lane and the A/B diagnostic.

**New, and it answers the P0.8 limitation I flagged:** grim now has **562
seats at 1000+ and 257 at 850-1000**, wall has 137 at 850-1000. The
mirror-only ceiling instrument can be extended to a **non-mirror high-band
bed** cheaply. That is beyond P0 as written — flagged for Piotr, not
silently added (see the P0 close-out).

`live_ci.py` re-run after the reparse now decodes the M38 families and
reproduces the post-mortem's cells exactly (grim 1-6, wall 1-5, mirror 7-2)
— with the intervals that were always missing: **grim 1-6 is [0.03, 0.51]**
and **wall 1-5 is [0.03, 0.56]**. Those two cells ranked the entire M38
post-mortem, and each spans from "hopeless" to "roughly even".

---

## 2026-08-01 — P0.3b/P0.4 weighted gate + baseline pins: SHIP A'S PREMISE IS NOT SUPPORTED

`scripts/m39_gate.sh` roster (10 beds, 3 arms, 2 seeds) + `m39_decide.py`.
Roster coverage **82.3%** — clears the G-3 floor. (No garchomp cell:
`m37_bc_garchomp.pt` is a pre-v4 net, 1580-wide, and will not load against
today's encoders. 4.8% of the mix sits in the uncovered remainder.)

While decoding I found that my own `m39_decide.py` printed the weighted
delta **with no interval** — the exact error the gate exists to stop. Fixed:
the weighted delta now carries a CI, a z, and the roster's MDE.

### Gate 1 — cont3 vs champion (is the M38 net better?)

| pool | arm | control | delta |
|---|---|---|---|
| unweighted | 0.5720 | 0.5397 | **+3.2pp** |
| **WEIGHTED** | 0.6063 | 0.6050 | **+0.14pp** |

**M38's exact failure mode, reproduced by the new instrument on a new
net.** The unweighted gain is carried by dragapult (+19.9pp, z=+8.17, 4.8%
share) and iono (+11.4pp, z=+4.76, **0.7% share**), while the big-share
beds go the other way: lucario **−7.3pp** (z=−3.16) at 19.7% share, wall
−6.0pp (z=−2.55) at 13.6%. Weighted, it is a wash.

**This qualifies my earlier entry.** Above I wrote that "the M38 net is
measurably stronger than its parent". Precisely: it is stronger against
900+ mirror pilots (+6.0pp, z=+4.20) and **indistinguishable from the
champion across the live opponent mix**, while being worse against the
single largest family. Both facts are real; only quoting the first was
over-claiming from one instrument.

### Gate 2 — cont3 plain vs shipcfg: THE SHIP A NUMBER

| pool | arm (strip) | control (live) | delta |
|---|---|---|---|
| unweighted | 0.5720 | 0.5787 | −0.67pp |
| **WEIGHTED** | **0.6063** | **0.6173** | **−1.10pp ±1.73, z=−1.24** |

Roster MDE ±2.5pp. **=> NO DETECTABLE DIFFERENCE.** Not a gain, not a
regression — the honest statement is that the strip does not move the
weighted pool at measurable power.

Every big-share cell leans mildly negative for the strip: lucario −3.2pp,
mirror −2.1/−2.9pp, wall −2.0pp; grim is the only positive (+3.5pp,
z=+1.51). None individually significant.

Note the wall cell contradicts the powered ablation (+4.04pp for the strip,
n=2400). Pooling both measurements — same nets, same bed, n=3200 each —
gives **+2.5pp for the strip on wall, z≈+2.2**. Real but half the ablation's
estimate, and G-12's warning applies to my own n=800 gate cells.

### What this does to Ship A

The plan justified Ship A as "the cheapest real gain available" and P1's
pre-registered bar was *"no weighted-pool regression vs the M38 ship config
pin; wall cells ≥ the plain pins."* Strictly: there is no detectable
regression, so the bar is not violated — but the *premise* (a real gain) is
not supported either. The measured case for the strip is now:

- **For:** wall +2.5pp pooled (z≈+2.2), the worst matchup we have (0.30);
  removes six rule paths from the shipped config.
- **Against:** weighted pool −1.1pp (n.s.); mildly negative on lucario
  (19.7% of games) and mirror (15.7%).

**This is a decision point for Piotr, not a call I should make** — Ship A
would spend 1 of 4 remaining slots on a change with no measured net benefit.
Options: (a) ship it anyway as the clean single-variable attribution probe;
(b) skip it and take the P2/P3 work straight to Ship B, banking a slot;
(c) power the big-share cells to n=2400 first — though resolving a ~1pp
delta is not realistically affordable.

---

## 2026-08-01 — P1-inv: the conserve MECHANISM probe predicts exactly where the WR effect was

`scripts/conserve_probe.py` (NEW, on the `racemode_fire_probe` pattern —
wraps `apply_play_overrides`, isolates real FIRES by diffing the same call
with and without the fix, and runs **single-process so it is reproducible**,
unlike `matchrunner --workers 8`).

| bed | MAIN prompts | trigger-true (own deck ≤ 6) | **conserve FIRES** |
|---|---|---|---|
| wall | 348 | 64 | **0** |
| grim | 608 | 33 | **0** |
| mirror | 559 | 114 | **8** |
| top (900+ mirror) | 597 | 117 | **13** |

**Conserve is active only in mirror-family games and provably inert vs wall
and grim.** Mechanically that is coherent: those are the long deck-race
games where our own deck runs low *while a targeted draw ability is the
model's top pick*, which is the only state the demote can act on.

**This substantially changes my read, and it argues for KEEPING conserve.**
In the P0.7 ledger I dismissed the conserve signal as one-of-12 contrasts
landing at p≈0.056 — the multiplicity argument. But the mechanism probe was
run afterwards and *independently predicts which beds should show an
effect*, and the prediction matches the win-rate data it never saw:

| bed | conserve fires | WR cost of REMOVING conserve |
|---|---|---|
| top | 13 / 12 games | **−2.75pp (z=−1.91)** |
| wall | **0** | −0.29pp (z=−0.22) |

An inert rule cannot cost 2.75pp, and conserve is inert exactly where the
cost vanishes and active exactly where it appears. That is not what a
chance p≈0.056 looks like — a fluke would not have known which bed to land
on. The multiplicity argument still applies to the *other* five rules,
where no such mechanism evidence exists.

Not calling it yet: the running Ship A battery measures `conserve-only`
directly against `plain` and `shipcfg` across the full weighted roster at
n=2400/cell. That is the clean test, and the mechanism probe is a prior
going into it, not a substitute for it. If the weighted gate says
conserve-only ≥ plain, the strip ledger's "strip conserve" line gets
overturned on evidence — which is the point of having run the probe.

---

## 2026-08-01 — P1-inv RESULT: Ship A is `conserve-only`, and the strip ledger is OVERTURNED on evidence

Three candidate ship configs, full 9-bed weighted roster, 6 seeds =
**n=2400/cell**, same battery (so every control is a same-battery control).
Weighted-pool MDE **1.4pp**, down from 2.5pp at P0's power.

| comparison | weighted delta | z | verdict |
|---|---|---|---|
| **conserve-only vs shipcfg (live)** | **+1.74pp ±1.00** | **+3.42** | **SIGNIFICANT** |
| **conserve-only vs plain (full strip)** | **+1.03pp ±1.00** | **+2.03** | **SIGNIFICANT** |
| plain vs shipcfg | +0.71pp ±1.00 | +1.39 | no detectable difference |

**Ordering: conserve-only > plain ≈ shipcfg.**

The per-cell pattern is what makes this trustworthy rather than a pooled
fluke — `conserve-only` vs the live config is positive on **six of eight**
beds at a consistent ~+2 to +3pp:

| bed | share | delta | z |
|---|---|---|---|
| grim | .122 | +2.9pp | +2.15 |
| wall | .136 | +2.8pp | +2.04 |
| mirror | .157 | +2.8pp | +1.94 |
| m28 (mirror) | .157 | +2.7pp | +1.89 |
| archaludon | .122 | +2.2pp | +2.38 |
| iono | .007 | +2.5pp | +1.72 |
| dragapult | .048 | −0.0pp | −0.03 |
| rocket | .034 | −2.3pp | −1.58 |

This is the opposite shape to M38's gate, which is the point of the
instrument: there the gain lived entirely in two ~5%-share beds and vanished
under weighting. Here it is broad, sits on the **big-share** beds, and
survives weighting.

### The strip ledger's "strip conserve" line is overturned

P0.7 concluded FULL STRIP because conserve's removal cost only −2.75pp at
z=−1.91, just under the bar, and one of twelve contrasts at p≈0.056 is what
chance produces. That reasoning was sound on the evidence available. It was
also wrong, and the thing that caught it was the **mechanism probe**, which
predicted *before this battery ran* that conserve is active only in
mirror-family deck races (13 fires/12 games on the 900+ bed, 8 on mirror)
and provably inert vs wall and grim.

Chain, in order: probe says the rule really acts → predicts a real effect →
powered gate finds conserve-only beating both alternatives at z=+3.42. **A
mechanism probe rescued a rule that a significance threshold had discarded**
— the concrete argument for G-11 being mandatory rather than nice-to-have.

Honest caveats: `conserve-only vs plain` is z=+2.03, barely over the bar,
and three pairwise comparisons were made here. The load-bearing number is
`conserve-only vs shipcfg` at **z=+3.42** with consistent per-cell support.
Note also that `plain vs shipcfg` moved from −1.10pp (n=800) to +0.71pp
(n=2400) — a **sign flip inside the MDE**, and a third demonstration of
G-12. Both readings mean the same thing: no detectable difference.

### What this does to Ship A

**Ship A's premise is restored, with a different config than planned.**
P0 left the strip at "no detectable difference", which is why Piotr called
investigate-further. The investigation found a config that IS supported:
`conserve-only` is **+1.74pp weighted over what is live (z=+3.42)**,
≈ **+12 implied ELO** — modest against the 1000 target, but measured,
broad-based, and it costs one token in a config string with no code change.

Proposed Ship A fix string: **`conserve`** (from
`telepath,deckguard,ash,conserve,benchfloor,racemode3`).
The action-masking layer is untouched — it was never part of this string.

---

## 2026-08-01 — P0.9 EXTENDED INSTRUMENT: the extension FAILED to produce a harder bed, and that is the finding

`m39_bc_topgrim` = grim list 3121746f cloned from seats at score **≥1000**
(200 seats, val 0.674). Same list as `m39_bc_grim` (700-850 seats, val
0.662), so the pair is a controlled pilot-skill ladder. n=2400/cell.

| candidate | vs grim 1000+ bed | 95% CI | gap vs parity |
|---|---|---|---|
| champion | 0.6577 | ±0.019 | **+15.8pp** |
| cont3 | 0.6992 | ±0.018 | **+19.9pp** |
| shipcfg | 0.6925 | ±0.019 | +19.2pp |

**We beat the 1000+ grim bed comfortably.** And the ladder:

| candidate | vs grim **700-850** | vs grim **1000+** | price of opponent skill |
|---|---|---|---|
| champion | 0.653 | 0.658 | **+0.5pp** |
| cont3 | 0.708 | 0.699 | **−0.9pp** |

**Cloning the same deck from 1000+ pilots instead of 750 pilots produces an
opponent that is not measurably stronger.** Both deltas are inside noise at
n=2400. The clones fit their corpora equally well (val 0.662 vs 0.674) — the
demonstrator's band simply does not transfer into bed playing strength.

### Why this matters more than the number

1. **The extension did not deliver what it was built for.** Piotr asked for
   a non-mirror high-band instrument; what came back is a bed no harder than
   the one we had. Reporting it as "built ✅" would be false.
2. **Rebuilding the grim bed did NOT fix the M38 fidelity failure.** M38's
   post-mortem blamed the M26-era clone's *age* (G-6 exists because of it).
   But the fresh 700-850 clone says we win **0.708**, the fresh 1000+ clone
   says **0.699**, and live grim went **1-6** (CI [0.03, 0.51]). The bed and
   the ladder disagree by ~30-50pp, and re-cloning at any band does not
   close it. **G-6 is necessary but not sufficient — the problem is not bed
   era, it is that BC cloning saturates below live pilot strength for this
   matchup.**
3. **It puts a caution on P0.8's headline.** The "≤10pp gap → the band is
   reachable" reading assumed `m39_bc_top` plays like a 900+ pilot. If
   cloning saturates, that bed may measure a clone ceiling rather than a
   band ceiling, and the reading is optimistic.

   **This does not invalidate P0.8** — `m39_bc_top` is genuinely hard (we
   *lose* to it at 0.41-0.47, unlike the grim beds we beat at 0.70), so it
   is a real opponent. But the saturation result says the mirror clone may
   also be softer than the pilots it was cloned from, which would make the
   true 900+ gap **larger** than −2.7pp. Hold the ≤10pp reading more
   loosely than the P0.8 entry does.

### Open question this creates (for Piotr, not resolved here)

If clone beds cannot represent high-band opponents, the ceiling question
cannot be settled offline by cloning at all, and slot 3's thesis rests on
an instrument with a known soft edge. Options worth considering later:
(a) accept clones as a lower bound on opponent strength and read gaps as
optimistic; (b) test the saturation directly — clone the SAME band twice
with different seeds and see whether bed strength varies at all;
(c) look for a non-BC bed construction (solver-backed or rules-backed) for
the high band. Not started — flagged.

---

## 2026-08-01 — CORRECTION: "bed saturation" is REFUTED. Cloning does capture skill.

The P0.9 entry above concluded that "BC cloning saturates — the
demonstrator's band does not transfer into bed playing strength." **That is
wrong.** The discriminating test is head-to-head between the two clones,
same deck both sides, which the entry above did not run:

| test | result |
|---|---|
| grim-1000+ clone **vs** grim-750 clone (n=2400) | **0.6229 ±0.019, z=+12.04** |

The 1000+ clone crushes the 750 clone. **Cloning captures the skill gap
with room to spare** — the two beds differ enormously; they simply lose to
*our agent* at the same rate. That is explanation (b) from the P0.9 entry
(matchup-specific), not (a) (saturation), and I asserted (a) without running
the test that separates them. Third over-claim of the day from a single
under-diagnosed measurement.

**A second hypothesis also weakened.** "The clone never learned our matchup"
does not survive contact with the harvest: grim pilots faced alakazam in
**31% of their games (304 seats, 286 at score ≥700)**. Our deck is their
second-most-common opponent.

### What the actual open question now is

Both grim clones lose to us at ~0.30-0.35 while live grim went 1-6 against
the M38 ship. Neither saturation nor missing-matchup-data explains it.

**And the gap is smaller than the P0.9 entry implied**, because that entry
quoted live grim as though it were ground truth. By G-9's own rule it is
not: **grim live is 1-6, n=7, CI [0.03, 0.51]**. The bed says we win 0.70;
the live 95% upper bound is 0.51. So the honest discrepancy is "≥19pp",
not the ~40pp implied — and it rests on seven games. I criticised the M38
post-mortem for ranking a milestone off that cell and then leaned on it the
same way.

Candidates still standing, none tested: the clone drifts off-distribution
once it errs and our agent punishes drift harder than the field does; grim's
edge vs alakazam lives in a few decisions that uniform cross-entropy
drowns; the live cell is simply an unlucky seven games.

---

## 2026-08-01 — D1 RESULT: the problem was never saturation. **Bed strength is a training lottery.**

D1 built `m39_bc_grim_vsus` — the grim list cloned from ONLY the seats where
grim faced alakazam, using the new `--opp-deck-hash` filter
(`rl/replay_bc.py`, +2 tests). First surprise: the filter dropped just **7 of
195 seats**. The unfiltered grim bed was **already ~96% matchup-specific**,
so "the clone never learned our matchup" is dead — it learned almost nothing
else.

Second surprise, and the real result. Three clones of the **same grim list**,
from **essentially the same data**, played against the same net (cont3) at
n=2400 each:

| clone | corpus | epochs | our WR vs it |
|---|---|---|---|
| `m39_bc_grim` | 195 seats | 10 | **0.6713** ±0.019 |
| `m39_bc_grim_b` | 195 seats (identical) | **9** | **0.6233** ±0.019 |
| `m39_bc_grim_vsus` | 188 seats (96% overlap) | 10 | **0.5460** ±0.020 |

**Spread 12.5pp against a binomial CI of ±2.0pp.** One fewer training epoch,
same data, moves bed strength 4.8pp. A 4% corpus change moves it 12.5pp.

**Bed play-strength is not a stable property of a corpus — it is a draw from
a lottery over training runs**, with a spread ~6× the sampling CI we have
been quoting.

### This dissolves the last two entries

- **"Saturation"** (already refuted by the head-to-head) is fully explained:
  `m39_bc_grim` 0.708 vs `m39_bc_topgrim` 0.699 looked like "band doesn't
  transfer", but those were two single draws from a ±6pp lottery landing
  near each other by chance. The head-to-head (topgrim beats grim 0.623,
  z=+12.04) was the true signal.
- **The bed-vs-live grim gap** needs no exotic explanation either. Our bed
  was a weak draw; a different draw of the same list gives 0.546, much
  closer to what the (7-game, CI [0.03,0.51]) live cell suggested.

### What is and is not damaged

**Safe — ship decisions.** The gate compares arm vs control on the SAME
fixed bed, so the bed's lottery draw is a constant that cancels in the
delta. **Ship A's +1.74pp (z=+3.42) stands.** Residual risk is second-order:
a rule could in principle help against a weak grim draw and not a strong
one, which the fix below also addresses.

**Damaged — every absolute bed number.** "We beat grim at 0.70", and by
extension P0.8's "we are only 2.7pp from the 900+ band", carry ~±6pp of
unquantified bed-construction variance on top of the ±2pp sampling CI. The
ceiling reading should be treated as one draw, not a measurement.

### The fix, and it is free

**Beds must be PANELS, not single nets.** Train K clones per family
(different epochs/splits — training is ~2 min) and gate against all K,
reporting the panel mean. Bed variance then falls as sqrt(K).

The cost is zero, because it is a *redistribution* of match budget rather
than an addition: instead of n=2400 against one draw, run n=800 against each
of three draws. Same compute, same total n, but the number now averages over
the lottery instead of sampling it once. Every bed battery in this milestone
spent its budget the wrong way.

Proposed **G-13: no bed is a single clone.** Gate rosters use >=3 draws per
family, the panel mean is the cell value, and the spread across draws is
reported — a family whose draws disagree by more than ~10pp is telling you
its cell is not measuring what you think.

---

## 2026-08-01 — SHIP A EXPORTED (awaiting Piotr's replay review — NOT submitted)

Piotr's call: take Ship A straight to export. Config:

| field | value |
|---|---|
| net | `m38_w9294_cont3.pt` (unchanged from the live sub) |
| deck | `alakazam_v2_h4` (md5 `ad014c58`, per decision 3) |
| **fix string** | **`conserve`** (was `telepath,deckguard,ash,conserve,benchfloor,racemode3`) |
| tarball | `dist/submission_neural_20260801_234656.tar.gz` (5285 KB) |

**Single variable vs the live sub 55146658: the fix string.** Same net, same
deck, no `rl/` behaviour change.

### Tier 1 — artifact correctness (new `scripts/ship_verify.py`, all PASS)

twin parity on all four twinned modules · deck md5 matches source · 60 cards
· **every fix name resolves** · bundle weights match the gated checkpoint.
Plus `tcg.shipping gate`: parity OK (max diff 1.79e-07), bundle isolation OK
(self-contained, no polars/torch), deck legal, gate game OK. And
`prize_semantics_probe` **PASS** (1144/1144 prize leaves positive).

The M37-audit's "silent fix-name ignore" hardening landed **as a pre-ship
check, not runtime code**. Raising a `ValueError` inside the agent would swap
a silent-typo risk for a live-crash risk — a bad trade with four slots left.
`ship_verify.py` gets the same protection with no production risk.

### Tier 2 — mechanism proof (G-11), and a CORRECTION

Fire rate over 60-90 games per bed across seeds (fires / states where our own
deck is in the demote zone):

| bed | fires / games | fires / trigger-true |
|---|---|---|
| top (900+ mirror) | 103 / 90 | **11%** |
| **grim** | 39 / 60 | **13%** |
| mirror | 47 / 90 | 6% |
| wall | **1 / 60** | **0.2%** |

**Correction to the P1-inv entry above:** it said conserve fires "0 times vs
wall and 0 vs grim". The grim half is **false** — that came from a 12-game
probe, and at 30 games/seed grim fires at 13%, the *highest* rate of any bed.
Wall is the genuinely inert one (1 fire in 473 trigger-true states).

This strengthens rather than weakens the ship: conserve-only's single largest
gate cell was **grim +2.9pp (z=+2.15)**, and we now know that is where the
rule most often acts. The false claim was also sitting in the shipped
`main.py` comment; corrected and the bundle re-packaged before QC.

Fourth correction today, all the same shape — a small-n probe read as if it
were a measurement. G-12 was written about match batteries; it applies to
probes too.

### Tier 4 — QC smoke test

16W-8L over 8 opponents x 3 games. No crashes, no timeouts, no 0-3 sweeps.

| opponent | | opponent | |
|---|---|---|---|
| tuned | 3W-0L | grim | 1W-2L |
| iono | 3W-0L | archaludon | 1W-2L |
| dragapult | 3W-0L | top900 | 1W-2L |
| wall | 3W-0L | prevship (M38) | 1W-2L |

Per VALIDATION.md Tier 4 this is **not** a strength measurement (n=3/opponent,
MDE ~100pp). Its value is that the roster now covers the live loss families
at all — M38's QC went 11W-1L against a roster containing **none** of them,
which is why its replay review had nothing relevant to look at. The grim /
archaludon / top900 legs are the replays worth Piotr's time.

**STOPPED HERE.** No submit. Awaiting manual replay review and explicit go.

---

## 2026-08-01 — SHIP A SUBMITTED: sub 55172160

Piotr's explicit go. `dist/submission_neural_20260801_234656.tar.gz` →
**submission 55172160**, status PENDING at submit.

**Single variable vs 55146658 (M38): the fix string, `gacfr3` → `conserve`.**
Same net (`m38_w9294_cont3`), same deck (`alakazam_v2_h4`, md5 `ad014c58`).

Offline expectation carried into the live read: **+1.74pp weighted
(z=+3.42)**, ≈ +12 implied ELO. `notebooks/model_monitor.ipynb` MODELS entry
added in this commit per the standing rule.

### Watch items (G-2 — ranked by live share x offline effect, with read-n)

| watch | offline | live threshold |
|---|---|---|
| **mirror** (.157) | +2.8pp | the M38 ship went 7-2 here; a drop below 0.50 at n>=30 says the stack was load-bearing in the mirror after all |
| **grim** (.122) | +2.9pp, largest cell | conserve fires most here (13% of trigger-true); WR < 0.30 at n>=30 contradicts the gate |
| **wall** (.136) | +2.8pp | conserve is inert here (0.2%), so any change is NOT this ship — it would be noise or drift |
| **rocket** (.034) | **-2.3pp** | the only clearly negative cell; low share, flagged not waived |

### G-10 dwell declaration

**Declared decision-read n = 150** (~3 days at the observed accrual rate,
+/-56 ELO). Rationale: the expected effect is ~+12 ELO and a 1-day read
carries +/-93 ELO, so a 1-day verdict could not distinguish this ship from
its parent in either direction. Piotr may override; the point of G-10 is that
the number is on the record BEFORE the sample is seen, not that it is fixed.

Reads permitted earlier for catastrophe detection and the G-11 mechanism
probe (does `conserve` fire in live replays at the ~11-13% rate the offline
probe measured?); no strength verdict before n=150.
