# M31 — bench economy as a deterministic rule (diary)

Plan: `docs/M31-plan.md`. Branch `feature/m31` (from `feature/m30` @ 2946a56, the
M30 gac ship 54929991). Piotr approved the final plan (2026-07-23) with two decisions
re-confirmed at kickoff: **base weights frozen at `m28_winners`** (rules-only
milestone) and **O8 drawfloor screens in parallel with O7, ship ≤1**.

## 2026-07-23 — M31 kickoff: plan finalized, branch cut, code claims verified

Three parallel code-verification sweeps ran before finalizing the plan; all material
claims hold (line numbers current, only cosmetic drift):

- **Override rules** (`rl/plan.py`): O1 telepath `:308-316` (keys `TELEPATH_ID=19`,
  gates on bench *space* `sum(occupied) < benchMax` — no minimum-bench threshold);
  constants `POFFIN_ID=1086`/`POKE_PAD_ID=1152`/`SACRED_ASH_ID=1129`/
  `DUDUNSPARCE_IDS={66}` `:331`/`FEZANDIPITI_ID=140`; O4 deckguard (≤6, demote 66),
  O5 ash (≤10, promote 1129), O6 conserve (≤6, demote 140). Twin byte-identical,
  `test_bundle_twins` at `tests/test_attach_override.py:223`.
- **O9 precedence bug CONFIRMED**: ash `:380-385` promotes on `deckCount<=10`
  ignoring `ranked[0]`, contradicting the docstring `:359-360`; tempo `:386-390` IS
  `ranked[0]`-gated. → M31 O9 = docstring fix + `test_ash_does_not_displace_o1_telepath`.
- **Id 305 (basic Dunsparce) is not referenced anywhere in `rl/plan.py`** — its
  ability semantics are genuinely unpinned (P0.2 is real work), distinct from id 66.
- **Measurement scripts**: per-prompt telepath over-counting confirmed
  (`live_postmortem.py:163-166`); `BehaviorTap` `offline_behavior.py:39`, `DrainTap`
  `deck_drain.py:55`; loss reason codes RESULT `type==23`, `2`=deck-out / `3`=benched
  (`deck_drain.py:149-152`). `offline_behavior.py` seeds only Python `random`, not the
  engine RNG → n=120 two-seed pooled for high-variance axes.
- **Reuse win**: the P0.6 bench counters (`bench_prompt_hist`, `min_bench_after_t3`,
  `missed_poffin_low`, supporter utilisation) already exist in `live_postmortem.py`
  and get *ported* into `offline_behavior.py`; only the **Hilda per-offer rate** is new.
- **Wiring/tests**: ship default `PKM_ATTACH_FIXES="telepath,deckguard,ash,conserve"`
  (`submission/main.py:107`), call sites `:220-223` (attach→play);
  `_MODEL_FIX_KINDS` `rl/matchrunner.py:64-80` (all gac-family kinds present;
  `gacb`/`gacd`/`poffinfloor`/`drawfloor` genuinely absent); `fn3`/`fn4` apply
  overrides `:506-511`/`:437-442`; `test_matchrunner_spec_kinds_parse:232` auto-covers
  new spec kinds.

**Structural thesis re-verified**: `decks/clone54618168.csv` = 9 basics in 60 (Abra×4,
Dunsparce×3, Fez ex×1, Shaymin×1). "Digging for board is genuinely hard" holds.

Weights-freeze rationale (re-confirmed with Piotr): the live record is one-sided —
every weights change since M26 was neutral-or-worse (M29 −65 ELO; M30 arm D killed
offline on the mirror-loss signature), the two live wins came from the deterministic
rule class (O1 → 720; gac → 818), and 54929991 @818 *is* `m28_winners`. No weights
candidate has cleared the resolved-better bar. Telepath ~72% is deferred to P0.4 as a
measurement axis for a future weights milestone.

Next: P0.1 live refresh + re-read at larger n.

## 2026-07-23 — P0.1 RESOLVED: cache grew to n=44, bench premise SURVIVES its kill check

Refresh fetched **18 new episodes** (44 listed, was 26) — the cache HAD grown since
the review, contra the plan's n=26 assumption. Re-read `live_postmortem.py 54929991`:

**Record: 44 games 25W–19L** (was 15W–10L @ n=26; incl. the self-validation W). The 18
new games are ~10W–9L — the sub is treading water at ~57%, slightly softer recently.

**Pre-registered bench kill/pivot check — both conditions FAILED to trigger, premise holds:**
- **bench-0-after-t3 gap PRESERVED**: 4/19 losses reached bench 0 after t3 (87673172,
  87676383, 87677432, 87679086) vs **0/25 wins**. The loss-vs-win gap did not close.
- **Poffin declined at bench ≤1 = 51 prompts = 1.16/game** — far above the ~0.5/game
  kill line (was 23 @ n=26). Also: `missed bench (bench<=1, basic playable, declined)`
  = 38 prompts, all at bench==1 (bench==0 declines still 0) — the plan deliberately does
  NOT force these (O7 is Poffin-dig, not force-bench-from-hand; counter-example risk).

→ **M31 proceeds on the bench axis** as planned.

**⚠ New signal worth flagging (not a pivot trigger, but important):** deck-outs rose to
**6/19 losses (31.6%)**, up from 2/10 (20%) @ n=26, and **spread beyond rocket** — the 4
new deck-outs are vs a Kangaskhan/Crustle wall (87692512, t37), the Alakazam **mirror**
(87693638, t18), Mega Lucario (87688658, t13), and a Cornerstone/Kirlia deck (87694004,
t44). Deck economy (target of the already-shipped O6 conserve) is now the single largest
loss category. Two readings: (a) small-sample escalation, or (b) conserve is not enough
in long grinds. Per plan discipline (`floor-gate-sample-size`) this is directional and
does NOT re-scope M31, but it is surfaced to Piotr — bench economy remains #1 for this
milestone; deck economy is the standing #2. Bench-outs held at 2 (87676383 t6, 87677432
t5 — both early, bench_end=0).

**Other engagement reads at n=44 (all consistent with the live gac rules working):**
dud ability at deck≤6 used 0/75, fez ability at deck≤6 used 0/32 (O4/O6 engaged), sacred
ash plays at deck 6–33 (O5 timing healthy), telepath attach 70.1% (stable, still ~14pp
below 54903635 — weights-borne, P0.4 axis). Supporter under-play persists/worsens: Hilda
5.8% per-offer (36/620), supporter-turns 59.5% (was 70.7%), supporters 9.6% per-offer
overall — the O8 target. Bench histogram: bench 0 3.7% · 1 7.3% · 2 17.7% · 3 26.4% ·
4 28.0% · 5 16.9% (bench ≤1 = 11.0% of MAIN prompts, ~198 events).

Next: P0.2 Dunsparce(305) ability probe.

## 2026-07-23 — P0.2 KILL: the Dunsparce (305) bench-ability hypothesis is FALSE

Built `scripts/ability_probe.py` (forced-use tap: always take the target
ability/PLAY, diff board state at the next MAIN prompt — reuses the DrainTap
pattern). Forced Dunsparce (305) ABILITY, side A = `model:m28_winners:clone54618168`:

- vs rule:lucario n=80: **0 offers**.
- vs rocket clone n=60: **0 offers** across 3042 MAIN prompts — Dunsparce active in
  253 of them. The ONLY ABILITY options offered came from **Dudunsparce (66)** and
  **Fezandipiti ex (140)** (253 each). Dunsparce (305) offers no ability.

Ground-truth check against the live bench-out loss the post-mortem cited (87676383,
`data/kaggle/raw/`): when Dunsparce is active at bench 0 there IS an "ABILITY" option,
but `_board_pokemon_id` resolves it to **None** because it is **`area=STADIUM index=0`**
— the in-play stadium's ability (Nighttime Mine), not Dunsparce's. Confirmed by dumping
o.area/o.index. Turn 4 (Dunsparce active, bench 0) had **no** ability option at all,
inconsistent with a Dunsparce-owned ability. (Same game, turn 6, Dudunsparce active
shows its real `area=ACTIVE board_id=66` deck-draw — the O4 deckguard target.)

**Verdict:** the post-mortem misattributed a STADIUM ability to Dunsparce. There is no
in-kit Dunsparce bench-filler; P0.2 adds/changes nothing. **O7 (Buddy-Buddy Poffin)
remains the sole bench-dig lever.** Kill diaried per "kills are as valuable as passes."
(The Nighttime Mine stadium ability is out of M31 scope — a possible deck-economy lever
for a later milestone, not a bench one.)

Next: P0.3 Poffin (1086) + Hilda (1225) semantics / deck cost.

## 2026-07-23 — P0.3 RESOLVED: Poffin is a clean bench-dig; Hilda is a moderate-cost draw

Forced-play probe (`ability_probe.py --kind play`), side A = `model:m28_winners`,
vs rule:lucario, all diffs clean same-turn:

**Poffin (1086)** — 170 uses, the O7 mechanism confirmed:
- bench count Δ **+1.27/use** (+1 60×, +2 78×, 0 32×); **all 216 benched cards are
  basics** — Dunsparce ×137, Abra ×79 (the ≤70-HP searchables; Fez ex/Shaymin never).
- deck cost **−1.27/use** (−2 78×, −1 60×, **0 32× = whiffs when no eligible basic left**);
  bounded at ≤2, matches the M30 1.5/use estimate. Poffin goes to discard (+1).
- **This is exactly the bench-dig O7 needs, with bounded deck cost.** Whiff rate 19%
  (0 basics found) — harmless (costs 0) but also no benefit; argues for a deck floor so
  O7 fires while eligible basics remain.

**Hilda (1225)** — 164 uses, a pure draw supporter:
- draws ~2 (deck Δ −2 125×, −1 32×, 0 7×; mean **−1.72/use**), hand net **+0.72/use**,
  **bench Δ 0** (never benches). Hand adds are generic (Dudunsparce, energy, evo line).
- Deck cost **1.72/use is MODERATE, not cheap** — cf. Poké Pad 1.0 / Dudunsparce 0.9 /
  Poffin 1.5 vs Dawn 2.9 / Fez 2.8 / Enriching 3.8. Cheaper than the Fez draw O6
  suppresses, but it still burns ~1.7 deck/use.

**O8 decision-relevant read:** with deck-outs now 31.6% of live losses and rising, a
forced Hilda feeds exactly that mode. O8 is NOT dropped (Piotr: screen both), but P0.3
prices Hilda as only moderately cheap → **P1 design refinement: O8 fires only when
hand-starved AND `deckCount ≥ D`** (a deck floor, same guard shape as O7), keeping it
out of the deck-out zone so it cannot fight O6 conserve. Whether even the deck-gated
version clears its deck-out guard is the P2 screen's job.

Next: P0.4 telepath per-turn recompute + bench histograms; P0.5/P0.6 offline counters.

## 2026-07-23 — P0.4: the telepath "13pp regression" is largely a per-prompt artifact

Recomputed telepath attach on the 44 live 54929991 episodes both ways:
- per-PROMPT: **138/197 = 70.1%** (the post-mortem number)
- per-TURN (dedup by (ep,turn)): **138/160 = 86.2%**

Same 138 attaches; the 59 prompt-level "misses" collapse to **22** turn-level misses —
37 were repeat-offers within turns that DID eventually attach telepath (energy is
once-per-turn, so a turn where the model plays a card first re-offers telepath and
inflates the per-prompt denominator, exactly as the plan predicted). Per-turn 86.2% is
on par with — slightly above — 54903635's per-prompt 84.9%. **The apparent telepath
regression is mostly a counting artifact; telepath discipline is not broken**, which
*reduces* the headroom a future weights milestone has on this axis. (Apples-to-apples
vs 54903635 per-turn, and the bench-histogram comparison, need that sub's cache
refreshed — deferred to the future weights milestone; not M31-blocking. Recorded as a
weights-selection axis per plan P0.4.)

Next: P0.5/P0.6 gac baseline on all 4 beds (running) → bed selection.

## 2026-07-23 — P0.5/P0.6 RESOLVED: bench beds = lucario + grim; gac baselines pinned

Extended `offline_behavior.py` with the bench-economy + supporter counters (ported the
bench-histogram / bench-0-after-t3 / Poffin-at-bench≤1 shapes from `live_postmortem.py`;
new Hilda per-offer + supporter-turn utilisation + RESULT-reason (2/3) loss split — the
reason capture reuses the `deck_drain.py` type-23 pattern). gac config
(`modelt-gac:m28_winners:clone54618168`) on all 4 beds, n=60 seed 1
(`runs/m31_p05_*.txt`):

| bed | wr | bench≤1 share | Poffin@≤1 | bench-out(3) L | bench-0-t3 L/W | deck-out(2) L | supp-util | Hilda/offer | tele |
|---|---|---|---|---|---|---|---|---|---|
| lucario | 0.683 | **17.9% (365)** | 17.9% | 4/19 | 5/19·11/41 | 0/19 | 87.2% | 6.9% | 88.4% |
| dragapult | 0.350 | 11.8% (286) | 30.8% | 3/39 | 5/39·1/21 | 2/39 | 82.2% | 8.5% | 75.3% |
| rocket | 0.467 | 4.9% (164) | 29.7% | **0/32** | 2/32·0/28 | **12/32** | 68.5% | 3.5% | 64.2% |
| grim | 0.583 | 7.7% (220) | 25.0% | **5/24** | 5/24·0/35 | 4/24 | 80.7% | 5.1% | 64.2% |

**Bed selection (P0.5):** the bench defect reproduces on **lucario** (21% bench-out
losses, 365 bench≤1 events — 2× any other bed → best power for O7's primary counter) and
**grim** (5/24 = 21% bench-out losses, the most absolute; also the live 0-3 bleed
family). **rocket produces ZERO bench-outs** (4.9% bench≤1) — it is the deck-drain bed
(12/32 deck-outs), exactly the M30 division of labour. → **P2 bench screen runs on
lucario + grim** (n=120, 2 seeds pooled). rocket is reserved for the P3 deck-out guard;
dragapult stays a P3 strength gate (M29 trap axis, not a screen bed).

**gac baseline pinned (P0.6, n=60 seed 1 — P2 will pool a 2nd seed to n=120):** the
numbers above are the relative reference for every O7/O8 claim. Live absolutes do not
transfer; only same-instrument deltas count. Note telepath per-turn here is 88.4%
(lucario), consistent with the P0.4 live per-turn 86.2% — the offline instrument is
per-prompt-decision but energy-attach is inherently per-turn.

**Threshold decisions for P1 (from P0.3 semantics + these baselines):**
- **O7 `poffinfloor`: bench-alive ≤ 1 AND deckCount ≥ 10 → promote Poffin (1086) PLAY.**
  Deck floor 10 keeps O7 strictly above the low-deck economy regime (deckguard/conserve
  ≤6, ash ≤10) so it never fights them; bench≤1 is an early-game state (deck≫10 — the
  live bench-outs were at deck 41/44), so the floor barely binds on real firings. P2
  confirms engagement (Poffin-at-bench≤1 must rise); if D=10 guts firing, lower it.
- **O8 `drawfloor`: hand-size ≤ 4 AND deckCount ≥ 10 AND Hilda (1225) offered → promote
  it.** Hand floor targets the under-play where drawing actually helps (not the
  full-hand hoarding cases where a draw does nothing); deck floor 10 keeps it out of the
  deck-out zone so it cannot fight conserve (the P0.3 concern). "Supporter not yet played
  this turn" is implicit — the engine only offers Hilda once per turn.

P0 COMPLETE. Next: P1 rule implementation + tests (no games).

## 2026-07-23 — P1 DONE: O7/O8 predicates + O9 fix landed, 648 tests green

`rl/plan.py` (+ byte-identical twin `submission/rl/plan.py`, re-verified):
- **O7 `poffinfloor`** (`PLAY_FIX_POFFINFLOOR`): at `bench-alive ≤ 1` AND `deckCount ≥
  10`, promote a legal Poffin (1086) PLAY. Slots into the promote chain after ash.
- **O8 `drawfloor`** (`PLAY_FIX_DRAWFLOOR`): at `hand ≤ 4` AND `deckCount ≥ 10`, promote
  a legal Hilda (1225) PLAY. After poffinfloor in the chain.
- Promote order (first match wins): **ash > poffinfloor > drawfloor > tempo**. The deck
  floor 10 keeps O7/O8 strictly above the low-deck economy regime (deckguard/conserve ≤6,
  ash ≤10) so they never fight those rules or the deck-out endgame.
- **O9 fix**: rewrote the `apply_play_overrides` docstring to state the REAL precedence —
  a PROMOTE (incl. ash) DOES displace an O1-promoted ATTACH in the same prompt, which is
  intended and benign (the manual attach re-offers next MAIN); only tempo is additionally
  `ranked[0]==END`-gated. No behavior change to ash (per plan: O9 is docstring + test).

`rl/matchrunner.py`: added `modelt-gacb` (gac + poffinfloor) and `modelt-gacd` (gac +
drawfloor) to `_MODEL_FIX_KINDS`.

`tests/test_attach_override.py`: +8 tests — O7 fire/guards, O8 fire/guards,
O7-wins-over-O8, ash-wins-over-O7-at-deck-10, O7/O8 compose-with-gac, and
`test_ash_takes_precedence_over_o1_telepath` pinning the real O9 precedence (named for
the actual behavior, not the plan's provisional `does_not_displace` — ash DOES displace;
noted). `test_matchrunner_spec_kinds_parse` auto-covers gacb/gacd.

**`uv run pytest` → 648 passed** (was 640; +8). No linter configured; new lines within
the file's style. Wiring smoke (gacb vs lucario n=6): Poffin-at-bench≤1 75% (3/4) vs the
gac baseline 17.9% — O7 mechanically engaged.

Next: P2 screens — {gac, gacb, gacd} × {lucario, grim} × 2 seeds, n=120 pooled.

## 2026-07-23 — P2 RESOLVED: O7 poffinfloor SURVIVES, O8 drawfloor KILLED

{gac, gacb, gacd} × {lucario, grim} × 2 seeds, n=120 pooled per config/bed
(`runs/m31_p2_*`). Deltas vs the gac baseline, two-proportion z:

**O7 `gacb` — SURVIVES its pre-registered bar on BOTH beds:**

| bed | bench≤1 share | Poffin@≤1 | deck-out(2) | bench-out(3) | bench-0-t3 L | wr |
|---|---|---|---|---|---|---|
| lucario | 18.3→**11.3%** z=−8.73 | 13.4→**89.5%** z=+14.63 | 0/47→0/30 | 27.7→23.3% | **14/47→1/30** | .608→**.750** |
| grim | 9.9→**6.1%** z=−7.61 | 27.4→**94.4%** z=+10.54 | 5.3→5.7% | 13.2→14.3% | **12/38→6/35** | .683→.700 |

- Primary (bench≤1 share DOWN **and** Poffin-at-bench≤1 UP): passes decisively both
  beds (z=−8.7/−7.6 and +14.6/+10.5). Mechanical engagement total — Poffin now played
  ~90–94% of bench≤1 offers.
- Guards (deck-out reason-2 and bench-out reason-3 not up): hold — deck-out flat (0→0
  luc, 5.3→5.7% grim = 2/35 vs 2/38, noise), bench-out down on luc (27.7→23.3%), flat
  on grim (5/35 vs 5/38).
- Advisory (the actual target defect): **bench-0-after-t3 losses nearly eliminated** —
  14/47→1/30 (lucario), 12/38→6/35 (grim). wr flat-to-up. → **gacb proceeds to P3.**
- ⚠ Watch-item for P3 mirror: telepath attach dips ~10pp on lucario (88.5→78.3%; grim
  −2.4pp). O7 promotes Poffin over the O1 telepath attach at bench≤1 (both fill board;
  Poffin benches 2 basics vs telepath's attach-search 1 + energy). Benign on the screen
  (wr ROSE), but the P3 mirror n=800 must confirm it costs no strength.

**O8 `gacd` — DROPPED (fails its own primary counter):**

| bed | supp-util | Hilda/offer | bench-out(3) |
|---|---|---|---|
| lucario | 85.0→83.6% z=−0.67 | 5.2→5.8% z=+0.67 | 27.7→35.7% |
| grim | 80.5→77.7% z=−1.45 | 6.1→6.1% z=+0.03 | 13.2→2.8% |

- Primary was "supporter-turn utilisation UP AND Hilda per-offer UP." Instead
  utilisation went DOWN both beds and Hilda per-offer is flat — the `hand≤4 & deck≥10`
  trigger fires ~6×/120 games (too rare; the model seldom holds ≤4 when Hilda is
  offered). Per pre-registration ("does not move its own primary counter → dropped"),
  **O8 is killed and not batteried.** Loosening the hand floor would fire Hilda in
  fuller-hand states at ~1.72 deck/use — the conserve conflict P0.3 flagged; not fishing
  for a passing threshold (M30/M26 discipline: don't tune to pass a noise gate). This is
  the 6th supporter-underplay attempt to die — the defect is weights-borne (M25/M27),
  not reachable by a deterministic deck-gated rule.

**M31 now carries one candidate: gacb (O7 poffinfloor).** Consistent with "ship ≤1".

Next: P3 battery on gacb only — floors first, mirror last, decoded by m31_decide.py.

## 2026-07-23 — P3 floors PASS for gacb → mirror launched

`scripts/m31_battery.sh floors gacb` (7 runs, n=200 each, --workers 8;
`runs/m31_gacb_*.jsonl`). Decoded (`m31_decide.py`), pins = LIVE gac:

| axis | pooled | pin | z | verdict |
|---|---|---|---|---|
| dragapult | 0.3375 n=400 | 0.3750 | −1.11 | ✓ non-inferior (gate) |
| kyogre | 0.9650 n=200 | 0.95 | +0.74 | ✓ floor (gate) |
| rocket (adv) | 0.6275 n=400 | 0.5675 | **+1.73** | above pin — O7 does not hurt the deck bed |
| grim (adv) | 0.6338 n=400 | 0.6687 | −1.04 | slightly soft, within noise (advisory) |

Gate floors (dragapult, kyogre) clear → **gacb alive**. Notable: rocket +1.73 above the
live-gac pin — the bench rule does NOT worsen the rising-deck-out matchup (P0.1 concern),
plausibly because developing board earlier shortens those grinds. dragapult non-inferior
on the M29 trap axis. → mirror phase (n=800, 4×200 vs rule:lucario) launched; the
telepath-dip watch-item from P2 resolves here.

## 2026-07-23 — P3 RESOLVED: gacb CLEARS its full bar (mirror non-inferior, point-above)

Mirror n=800 (`m31_decide.py`): **luc 0.7113 (0.700/0.690/0.760/0.695), pin 0.6750,
z=+1.57** — non-inferior AND point-above the live gac. **The P2 telepath-dip watch is
resolved: it cost NO mirror strength** — gacb is point-better on the mirror (board
development helps even the mirror; deferring one telepath attach to bench 2 basics is a
net gain). Full decode:

| axis | pooled n | pin | z | gate |
|---|---|---|---|---|
| mirror (luc) | 0.7113 / 800 | 0.6750 | **+1.57** | ✓ non-inferior |
| dragapult | 0.3375 / 400 | 0.3750 | −1.11 | ✓ non-inferior |
| kyogre | 0.9650 / 200 | 0.95 | +0.74 | ✓ floor |
| rocket (adv) | 0.6275 / 400 | 0.5675 | +1.73 | above pin |
| grim (adv) | 0.6338 / 400 | 0.6687 | −1.04 | soft (noise) |

**→ gacb CLEARS ITS STRENGTH BAR** (mirror non-inferior ✓, dragapult non-inferior ✓,
kyogre ✓) AND the P2 behavioural claim (bench≤1 share down, Poffin-at-bench≤1 up,
deck-out/bench-out not degraded). It is the M31 ship candidate. gacd was killed in P2, so
tie-break is moot (one qualifier). Per pre-registration this is a valid ship candidate —
but no submit without Piotr's QC review + explicit go.

Next: P4 — read verify-before-consequential-actions.md, build the bundle, QC vs a
bench-pressuring opponent, STOP for Piotr.

## 2026-07-23 — P4 executed, STOPPED at the human-QC gate (awaiting Piotr's go)

Read `verify-before-consequential-actions.md` first (per the hard rule). Then, the M30
ritual — verifier-first, identity not just validity:

- Ship default changed: `submission/main.py` `PKM_ATTACH_FIXES` default
  `"telepath,deckguard,ash,conserve"` → **`"telepath,deckguard,ash,conserve,poffinfloor"`**
  (O1+O4+O5+O6+**O7**, the M31 gacb arm). No test pins the string; full suite **648
  passed** after the change.
- `./build_submission.sh --checkpoint m28_winners.pt --deck clone54618168` → gate game OK
  (138 decisions) → **`dist/submission_neural_20260723_194112.tar.gz`** (no upload).
- Tarball verified independently: deck.csv md5 **8e8cf124** = clone54618168 ✓ · bundled
  `policy_weights.npz` **21/21 arrays byte-match `m28_winners.pt`** ✓ · bundle `main.py`
  default string = the 5-fix gacb string ✓ · bundle `rl/plan.py` carries
  `PLAY_FIX_POFFINFLOOR` and is byte-identical to the `submission/` twin ✓.
- QC opponent = **grim clone** (not rocket): O7 is a bench rule, so the opponent must
  pressure the bench — grim reproduced the most bench-outs in P0.5 (5/24) and is the live
  0-3 bleed family; rocket produces zero bench-outs (deck bed), so it would not exercise
  O7. (Mirror-image of M30's rocket pick for its deck rule.)
- QC: 3 games of the actual bundle vs the grim clone → **3W–0L**,
  `replays/m31_qc_gacb_000..002.html`.
- **STOPPED. No submit.** Awaiting Piotr's manual replay review and explicit go —
  sign-off covers the new `poffinfloor` rule, the default-string change, and the QC
  opponent choice.

**Decision package for Piotr:** gacb (O7 poffinfloor) clears its full pre-registered bar
— strength non-inferior everywhere (mirror 0.7113 z=+1.57 point-ABOVE live gac; dragapult
z=−1.11; kyogre 0.965; rocket adv +1.73) AND the behavioural claim (bench≤1 share down
z=−8.7/−7.6, Poffin-at-bench≤1 up to ~90%, deck-out/bench-out not degraded, bench-0-t3
losses 14→1 / 12→6). O8 drawfloor was killed in P2 (supporter under-play is weights-borne,
not rule-reachable). Standing caveat surfaced: the live deck-out rate rose to 31.6% at
n=44 (directional) — O7 does not worsen it (rocket +1.73) but does not target it either;
deck economy remains the #2 axis for a later milestone.

Live A/B watch-list if shipped (vs 54929991 @818, same base+deck, rule set only changes):
bench-0-after-t3 loss share (target: ~0), Poffin-at-bench≤1 play rate (expect a big jump),
bench-out loss count, telepath attach (the P2 −10pp offline point-read — if it reproduces
live and mirror record softens, the O7↔O1 precedence needs a look), deck-out share (the
rising n=44 signal).

## 2026-07-23 — M31 SHIPPED: sub 54935640 (Piotr's explicit go)

Piotr reviewed the QC and said "Let's submit!!!". Verifier-first per the hard rule:
final pre-submit identity check on the EXACT tarball
(`dist/submission_neural_20260723_194112.tar.gz`) — deck.csv md5 **8e8cf124** ✓, bundle
default fixes = `telepath,deckguard,ash,conserve,poffinfloor` ✓ — then
`uv run kaggle competitions submit`. **Submitted → sub 54935640, PENDING**, verified in
the submissions list (exact tarball filename + description).

Note the incumbent **54929991 now reads publicScore 791.1** (settled from the ~818 peak as
the ladder resolved) — that, not 818, is the live A/B comparator for gacb. Same base
(`m28_winners`) + same deck (clone54618168); the ONLY change vs 54929991 is the added O7
`poffinfloor` rule — a clean single-variable live A/B.

Live A/B watch-list (vs 54929991 ~791, same base+deck): bench-0-after-t3 loss share
(target ~0), Poffin-at-bench≤1 play rate (expect a big jump), bench-out loss count,
telepath attach (P2 offline −10pp point-read — if it reproduces live and the mirror-family
record softens, the O7↔O1 precedence needs a look), deck-out share (the rising n=44 #2
axis O7 does not target).
